# /tdd brief — ST.5b: commitment-card display completion (RcdoBreadcrumb · OutcomePill + readOnly)

## Feature
Two CommitmentList card-display gaps, both render-only + Tailwind/token-native: (1) a **read-only RcdoBreadcrumb** on each commitment card — RC › DO › SO when linked (from `c.supportingOutcomeBreadcrumb`), and a **missing-SO warning** variant when unlinked; (2) the now-reachable **RECONCILED read-only polish** — render `c.reconciliationOutcome` as a tone-skinned **OutcomePill** + mute the card when the plan is RECONCILED. Second ST.5 slice (completes the IC commitment-card composition).

## Use case + traceability
- **Task ID:** ST.5 (Phase ST — IC workspace visual composition; slice b)
- **Spec source (binding visual):** `docs/design/cadence-design-system/ui_kits/weekly-commit/CommitmentCard.jsx` (RcdoBreadcrumb mount line ~123; the `RECON_CHOICES` tone map lines ~6-12; readOnly OutcomePill lines ~127-133) + `components.css` `.wc-rcdo` (sunken chip: `surface-sunken` bg, `border`, `text-meta`, ink-secondary; SO bold mono ink-primary; separator › in ink-muted) + `.wc-rcdo--missing` (warning tone bg/fg/border + icon + "No Supporting Outcome linked"). **Architecture:** `ARCHITECTURE.md §5` (RcdoBreadcrumbDto — RC→DO→SO labels for display), `§3` (reconciliation outcomes; rule #1 — a LOCKED plan's planned commitments all link an SO, so the missing variant is a DRAFT/unplanned-only nudge), `§7` (styling-SoT). Cadence `.wc-*` = visual spec only (forbidden #3).
- **Related context:** ST.3 (`statusTaxonomy.ts` §7 single-source — extend with the outcome map), ST.4 (`62b4b92` card hairline/accent), ST.5a (`e49e46a` stepper/lock-glyph/redundancy — RECONCILED-current-plan-viewability confirmed at its Step 7.5, which unblocks the OutcomePill). The `SupportingOutcomePicker` (9.5/9.7) shows the breadcrumb **in the edit form**; this slice adds the **read-only display on the card** (the list-view gap). `RcdoBreadcrumbDto` + `ReconciliationOutcome` are B.1/B.5/B.6 (in `dtos.ts`), rendered verbatim.

## Acceptance criteria
- [ ] A new presentational component (e.g. `commitment/RcdoBreadcrumb.tsx` — confirm name at 2.5) renders `RcdoBreadcrumbDto` as **RC title › DO title › SO title** (SO emphasized — bold/mono, `text-ink-primary`), token-native sunken chip (`bg-surface-sunken border border-border rounded-md text-meta text-ink-secondary`, separators `›` in `text-ink-muted`). Titles render React-escaped.
- [ ] When a commitment has **no `supportingOutcomeBreadcrumb`** (unlinked), the component renders the **missing-SO warning** variant (`bg-tone-warning-bg border-tone-warning-border text-tone-warning-fg` + an icon + "No Supporting Outcome linked"). (Per rule #1 this only occurs pre-lock — DRAFT / unplanned; confirm scope at 2.5.)
- [ ] `commitment/CommitmentList.tsx` mounts the RcdoBreadcrumb per card (between the title row and the chess atoms, per the Cadence card order).
- [ ] `shared/lib/statusTaxonomy.ts` gains an **outcome tone map** (`RECONCILIATION_OUTCOME_TAXONOMY` — the §7 single source): `COMPLETED→success`, `PARTIALLY_COMPLETED→warning`, `BLOCKED→failure`, `CANCELED→neutral`, `CARRIED_FORWARD→info` (per Cadence `RECON_CHOICES`); **unknown → renders nothing** (LESSONS §7).
- [ ] An **OutcomePill** renders `c.reconciliationOutcome` as a tone-skinned pill (glyph+text+tone — color never the only signal) when set; reached on the RECONCILED current-plan view (confirmed reachable, ST.5a 7.5). `outcomeNote` rendered (React-escaped) when present.
- [ ] The commitment card is **muted/read-only** when `planState === 'RECONCILED'` (e.g. reduced-emphasis ink/opacity); a DRAFT/LOCKED/RECONCILING card is not muted.
- [ ] **Render-only — NO enum/Appendix-A change** (consumes `supportingOutcomeBreadcrumb`/`reconciliationOutcome` verbatim). **No `.wc-*` CSS / no second stylesheet / no hex / no arbitrary `[…]` hatches** (forbidden #3). Both `[data-theme]` values render; `yarn` typecheck + lint + prettier + full Vitest pass.

## Files expected to touch
**New:**
- `apps/wc-web/src/features/commitment/RcdoBreadcrumb.tsx` (+ `.test.tsx`) — the read-only breadcrumb chip + missing-SO variant

**Modified:**
- `apps/wc-web/src/shared/lib/statusTaxonomy.ts` — `+ RECONCILIATION_OUTCOME_TAXONOMY` (outcome→tone+label[+icon])
- `apps/wc-web/src/features/commitment/CommitmentList.tsx` (+ test ripple) — mount RcdoBreadcrumb; render OutcomePill + readOnly muting when RECONCILED
- `apps/wc-web/src/app/theme/surfaceSkin.test.ts` — add the new surfaces to the structural guard

If the OutcomePill warrants its own small component vs inline, **your call** (flag at 2.5).

## RED test outline (Step 2)
1. **`rcdo_breadcrumb_renders_rc_do_so_when_linked`** (`RcdoBreadcrumb.test.tsx`, render) — given a `RcdoBreadcrumbDto`, renders RC › DO › SO titles with the SO emphasized.
2. **`rcdo_breadcrumb_renders_missing_warning_when_unlinked`** (render) — given no breadcrumb, renders the warning-tone "No Supporting Outcome linked" variant (icon + text + warning tone).
3. **`commitment_list_mounts_breadcrumb_per_card`** (`CommitmentList.test.tsx`, render) — a commitment row renders its linked-SO breadcrumb; an unlinked DRAFT row renders the missing variant.
4. **`outcome_pill_maps_each_outcome_to_tone`** (render) — `COMPLETED→success`, `PARTIALLY_COMPLETED→warning`, `BLOCKED→failure`, `CANCELED→neutral`, `CARRIED_FORWARD→info`; each renders glyph+text+tone; unknown → nothing.
5. **`reconciled_card_is_muted_and_shows_outcome_pill`** (render) — a RECONCILED commitment with `reconciliationOutcome` renders the OutcomePill + the card carries the muted/readOnly class; a DRAFT card has neither.
6. **`no_wc_star_css_or_hardcoded_token_introduced`** (extend `surfaceSkin.test.ts` `TOUCHED_SURFACES` with `RcdoBreadcrumb.tsx` + `statusTaxonomy.ts` if not present) — no `.wc-*`/hex/arbitrary-`[…]`.

## Cross-doc invariant impact
- **NONE.** Render-only; consumes B.5 `RcdoBreadcrumbDto` + B.6 `reconciliationOutcome`/`supportingOutcomeBreadcrumb` verbatim. `RECONCILIATION_OUTCOME_TAXONOMY` is a visual map (the §7 single-source), not contract. No Appendix-A/B change.

> **Implementer never edits `apps/wc-web/CLAUDE.md`, `ARCHITECTURE.md`, `MVP_TASKS.md`, or `apps/wc-web/LESSONS.md`.**

## Things to flag at Step 2.5
1. **Missing-SO warning scope.** **My default vote:** render the missing variant whenever `!supportingOutcomeBreadcrumb` — effectively DRAFT/unplanned (rule #1 guarantees a LOCKED+ planned commitment is linked). The card-level nudge complements the form's alert (different surface). Confirm (alt: only render the breadcrumb-when-present, no list-level warning).
2. **Outcome tone map.** **My default vote:** `RECONCILIATION_OUTCOME_TAXONOMY` in `statusTaxonomy.ts` (the §7 single-source, alongside PLAN_STATE/REVIEW/RISK/WORKTYPE/ALIGNMENT/SYNC), tones per Cadence `RECON_CHOICES` (COMPLETED→success / PARTIALLY_COMPLETED→warning / BLOCKED→failure / CANCELED→neutral / CARRIED_FORWARD→info), with a pinned Heroicon each. Confirm tones + whether to add icons.
3. **readOnly muting trigger.** **My default vote:** mute on `planState === 'RECONCILED'` (the plan-level read-only state); the OutcomePill renders whenever `reconciliationOutcome` is set (independent of muting). Confirm vs. keying muting on the outcome presence.
4. **CARRIED_FORWARD successor note — partial (no data path).** Cadence shows "→ went to week of X / view successor ›" for a carried-forward outcome, but the DTO carries `carryForwardSourceCommitmentId` (the INCOMING link), not an outgoing successor/week. **My default vote:** render the CARRIED_FORWARD pill + `outcomeNote`; **defer** the successor-week/navigation note (no data path) — carry-forward it. Confirm.
5. **Bundle size.** RcdoBreadcrumb (new component) + OutcomePill/muting are bundled as "card display completion." If it feels too big for one slice at RED, **flag** — I'm fine splitting (RcdoBreadcrumb / OutcomePill) into two commits.

## Dependencies + sequencing
- **Depends on:** ST.5a (`e49e46a`) — landed; its 7.5 finding (RECONCILED current plan viewable) unblocks the OutcomePill. ST.3/ST.4 — landed.
- **Blocks/feeds:** ST.6 (manager composition), ST.7 (a11y + `/design-review`). The `RECONCILIATION_OUTCOME_TAXONOMY` may be reused by ST.6 manager surfaces.
- **Not blocked.** (Disputed-accent + dispute strip remain with 9.11a/B.6 — not in scope.)

## Estimated commit count
**1 commit (split to 2 if large) — NOT safety-critical** (render-only; same IC card surface; two related display features). RED→2.5→GREEN per feature, one Step-10 commit (or split RcdoBreadcrumb / OutcomePill if Q5 says so). Exact fidelity → ST.7 `/design-review`.

## Lessons-logged candidates anticipated
- Likely none new — `RECONCILIATION_OUTCOME_TAXONOMY` is natural growth of the §7 single-source pattern (the Nth taxonomy). Flag if the read-only-vs-input rendering split (form choices vs display pill for the same enum) reads as a reusable convention.

## How to invoke
1. **Read this brief** + `CommitmentList.tsx` (current) + `RcdoBreadcrumbDto`/`WeeklyCommitmentDto` in `dtos.ts` + the Cadence `CommitmentCard.jsx` (breadcrumb + readOnly outcome) + `components.css` `.wc-rcdo`.
2. **Run `/tdd ST.5b commitment-card display completion`** (session already oriented).
3. **Step 0/1** — confirm Feature + Files; Tailwind/token-native.
4. **Step 2.5** — send the test write-up + Q1–Q5 answers (esp. Q1 missing-SO scope, Q2 outcome tones, Q4 carried-forward defer, Q5 bundle/split). Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
5. **Step 7.5** — reachability: breadcrumb on every card; OutcomePill + muting on the RECONCILED current-plan view (`/weekly-commit`). Flag gaps.
6. **Step 9** — categorized summary + ship/no-ship + draft commit; confirm cross-doc invariant = NONE + no `.wc-*`/hardcoded-token + the carried-forward-successor-note disposition.
