# /tdd brief — ST.8c IC page + commitment-card fidelity (mockup §C)

## Feature
Bring the IC **"My Weekly Commit"** page + commitment card up to mockup fidelity: the **page title + breadcrumb**, a **bordered plan-header card** (week + status pill + "N planned · M unplanned" + review badge + owner on top; **4-node stepper + primary actions** on the bottom), the **GOLD** Start-reconciliation button (currently brand-blue) + corrected **button order**, and the commitment card's **plain description line** + **chips-above-SO-box** order (currently reversed) + **dropping the monospace** on the SO/objective text. Closes **doc 024 §C**. **Render-only / standalone-demo — NO contract change** (the `description` field already exists on `WeeklyCommitmentDto` B.6; it's simply not rendered today).

## Use case + traceability
- **Task ID:** **ST.8c** (styling-fidelity round). Render-only.
- **Architecture sections it implements:** none new — renders the existing **Appendix B.5/B.6** (`WeeklyPlanDto`/`WeeklyCommitmentDto`) contracts. The `description` line consumes the already-present `description` field (no DTO change). Controls stay gated on the server `allowedActions[]` (wc-web LESSONS §11) — **no client-side re-derivation of lifecycle/eligibility** (the gold button is still gated on `START_RECONCILIATION`).
- **Authoritative mockup reference (CANON):** `docs/design/cadence-design-system/ui_kits/weekly-commit/WeeklyPlanView.jsx` + `CommitmentCard.jsx`.
  - **`PlanLifecycleBar`** (WeeklyPlanView.jsx 9–48): a `plan-head` card — **top row** = week label · `PlanStatePill` · "N planned · M unplanned" counts · `managerReview` status `Badge` · spacer · owner (avatar + name); **bottom row** = the 4-node `stepper` (DRAFT/LOCKED/RECONCILING/RECONCILED, done-checks + active + connector bars) · spacer · the **primary action(s)**.
  - **Primary actions by state** (50–94): DRAFT → "Lock plan" (primary, gated); **LOCKED → "Add unplanned work" (secondary, LEFT) + "Start reconciliation" (`variant="warning"` = GOLD/amber, `HiClipboardCheck`, RIGHT)**; RECONCILING → "Add unplanned work" + "Close week" (success). **The GOLD = the `warning` tone token, not blue.**
  - **`CommitmentCard`** (CommitmentCard.jsx 92–140) order: `hd` (lock icon if locked + **title** + Unplanned badge) → **`<p className="desc">{c.desc}</p>` plain description** → **`ChessChips`** (Priority · WorkType · Confidence · Alignment) → **then** missing-SO alert OR `RcdoBreadcrumb` (the SO box, **NOT monospace**) → recon/outcome/dispute. Section headers (WeeklyPlanView 122–133): "Planned · locked baseline" / "Unplanned · added after lock" / "Commitments".
- **Related context:** current surface = `src/features/plan/WeeklyPlanView.tsx` (h1 + raw-ISO week range + plan StatusBadge; `PlanLifecycleBar` with the stepper **above** a separate action row) + `src/features/plan/PlanLifecycleBar.tsx` (**Start-reconciliation = `bg-brand-600` blue**) + `src/features/commitment/CommitmentList.tsx` (card: title → `RcdoBreadcrumb` **ABOVE** chips, **no desc line**) + `src/features/commitment/RcdoBreadcrumb.tsx` (SO title in **`font-mono`**). Token-native — no hex / no `.wc-*` (`surfaceSkin.test.ts` guards all four). **Lands AFTER ST.8a** (the shell owns the global breadcrumb). The IC page breadcrumb "My Weekly Commit › Week of …" is rendered by the ST.8a app-shell; this slice owns the **page title** + the in-surface composition.

## Acceptance criteria (what "done" means)
- [ ] **Page title** "My Weekly Commit" (replacing "Weekly commitments") + the week range rendered as the friendly label ("Week of Jun 1–7, 2026", reusing the existing `WeekRangeLabel`) not raw ISO.
- [ ] **Plan-header card** — `PlanLifecycleBar` reframed into a single bordered card: **top row** = week label + plan status pill + "N planned · M unplanned" counts + the `managerReview` status badge (when present) + owner identity (avatar + name); **bottom row** = the 4-node stepper + the primary action(s). The planned/unplanned/review summary line is prominent (not loose text).
- [ ] **GOLD Start-reconciliation** — the Start-reconciliation button uses the **warning/gold tone token** (matching the canon `variant="warning"`), **not** `bg-brand-600`. Still **gated on the server `START_RECONCILIATION` allowedAction** (no client re-derivation — LESSONS §11). **Button order:** "Add unplanned work" (secondary) **left**, the gold primary **right** (matching the canon LOCKED/RECONCILING rows).
- [ ] **Commitment-card description line** — render the commitment's `description` as a plain text line **below the title, above the chips** (React-escaped; no `dangerouslySetInnerHTML`). Omit cleanly when empty.
- [ ] **Chips-above-SO-box order** — the chess chips row (Priority · WorkType · Confidence · Alignment) renders **above** the `RcdoBreadcrumb` SO box (the current order is reversed). The missing-SO alert keeps its position (in place of the SO box).
- [ ] **Drop monospace on SO/objective text** — `RcdoBreadcrumb` renders the SO/objective titles in the **regular** body weight, not `font-mono`.
- [ ] **Section headers** (per the canon) — "Commitments" (DRAFT) / "Planned · locked baseline" + "Unplanned · added after lock" (RECONCILING) section captions, if not already present.
- [ ] **Token-native + REQ-I-008** — no hex / no `.wc-*` (the gold = a tone token, not `#…`); `surfaceSkin.test.ts` + `boundary.test.ts` green.
- [ ] **Visual exactness** (both themes, all four lifecycle states) verified in **ST.8d** gstack canon-compare — this slice pins the structural/order/gating changes; pixel fidelity is the real-browser gate.
- [ ] `yarn nx lint && typecheck && test wc-web` clean.

## Files expected to touch
**Modified:**
- `src/features/plan/WeeklyPlanView.tsx` — page title "My Weekly Commit" + friendly week label + section headers.
- `src/features/plan/PlanLifecycleBar.tsx` — reframe into the plan-header card (top row + bottom row); the GOLD Start-reconciliation tone + button order. **No change to the `allowedActions` gating** (LESSONS §11/§19).
- `src/features/commitment/CommitmentList.tsx` — add the description line; move the chips row above the `RcdoBreadcrumb` SO box.
- `src/features/commitment/RcdoBreadcrumb.tsx` — drop `font-mono` on the SO/objective text.
- test files: `PlanLifecycleBar.test.tsx`, `CommitmentList.test.tsx`, `RcdoBreadcrumb.test.tsx`, `WeeklyPlanView.test.tsx` additions.

If implementation needs files beyond this list, **flag at Step 2.5**.

## RED test outline (Step 2)
1. **`start_reconciliation_is_gold_and_gated`** — when the plan exposes `START_RECONCILIATION`, the button renders with the warning/gold tone class (not `bg-brand-600`) and is positioned right of "Add unplanned work"; when the action is absent, the button does not render (gating preserved).
   - Why: §C.2 + LESSONS §11 — restyle must not weaken server-authoritative gating.
2. **`commitment_card_renders_description`** — a commitment with `description` renders the plain description line; one without renders no empty line.
   - Why: §C.3 — the description field exists (B.6) but wasn't rendered.
3. **`chips_render_above_so_box`** — in the card DOM, the chess-chips row precedes the `RcdoBreadcrumb` SO box (assert order, e.g. via `compareDocumentPosition` or query order).
   - Why: §C.3 — the current order is reversed.
4. **`so_text_not_monospace`** — `RcdoBreadcrumb` SO/objective text does not carry `font-mono`.
   - Why: §C.3.
5. **`plan_header_card_composition`** — the plan-header card renders the week label + status pill + "N planned · M unplanned" counts + (when present) the review badge + owner, with the stepper + primary actions in the bottom row.
   - Why: §C.2 — the bordered header-card framing + prominent summary line.
6. **`week_label_is_friendly`** — the page renders "Week of …" (via `WeekRangeLabel`), not raw ISO.
   - Why: §C.1.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** **NONE** — `description` already on B.6; everything else is render order/tone. No `dtos.ts`/`ARCHITECTURE.md` change.
- **Orchestrator doc rows to write hot (Step 9 routing):** none. Orchestrator stages the ST.8c task row at `/orchestrate-end`.

## Things to flag at Step 2.5
1. **Stepper extraction (rule-of-three).** Reframing `PlanLifecycleBar`'s stepper does NOT add a 3rd consumer — `PlanLifecycleBar` + `DisputePanel` remain the 2 stepper consumers (the Carry-forward "extract a shared `<Stepper>` at the 3rd" stays un-triggered). **My default vote: do NOT extract** — reframe in place; keep the SPREAD residual for the 3rd consumer.
2. **Owner identity in the plan-header card.** The IC's own plan-header shows the owner (themselves). **My default vote: render the owner avatar+name from the current user** (reuse the ST.8b `Avatar`); it reads as identity confirmation. Flag if you'd omit it for the self-view.
3. **Gold tone token.** Map the canon `variant="warning"` to the existing `warning` tone token (the same amber the risk/review taxonomy uses). **My default vote: reuse the `warning` tone token** — do NOT introduce a new "gold" token or a hex. Confirm the token reads as gold/amber in both themes (ST.8d verifies visually).
4. **Section headers scope.** The canon shows "Planned · locked baseline" / "Unplanned · added after lock" in RECONCILING. **My default vote: add the section captions** (small, render-only) for fidelity. Flag if the current list already groups planned/unplanned adequately.

## Dependencies + sequencing
- **Depends on:** **ST.8a** (shell owns the breadcrumb) + the landed IC surfaces (`WeeklyPlanView`/`PlanLifecycleBar`/`CommitmentList`/`RcdoBreadcrumb`). Independent of ST.8b (different surface) — can land in either order after ST.8a.
- **Blocks:** **ST.8d** final canon-compare QA.

## Estimated commit count
**1** — one cohesive IC-surface fidelity slice. Render-only; **no safety invariant** (the lifecycle gating is unchanged — the restyle preserves the `allowedActions` gate). Do NOT bundle with a feature slice.

## Lessons-logged candidates anticipated
- **Convention candidate** — possibly none new (reinforces LESSONS §7 single-render-map + §11 server-gated controls — a restyle that preserves gating).
- **Architecture-doc note** — none (render-only).

## How to invoke
> Not the first slice of the session — jump straight to `/tdd`.
1. **Read this brief + the mockup `WeeklyPlanView.jsx` + `CommitmentCard.jsx` (CANON) + doc 024 §C.**
2. **Run `/tdd st8c-ic-page-card-fidelity`.**
3. **Step 2.5** — send the per-test write-up + answers to the 4 design questions; wait for the magic-words header.
4. **Step 9** — confirm the gating/§11 posture held through the restyle.
