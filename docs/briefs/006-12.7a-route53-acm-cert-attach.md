# /tdd brief — Route53 + ACM certs + CloudFront cert/alias attach (12.7a)

> **Infra brief — NOT red-green TDD** (see brief 001 banner). Verify via `fmt`/`init -backend=false`/`validate`/`tflint`; real `init`/`plan`/`apply` are HITL. Run `/tdd 12.7a-route53-acm-cert-attach`; substitute "author `.tf` → `validate` passes" for RED→GREEN.
>
> **Task 12.7 is SPLIT into 12.7a + 12.7b** (orchestrator decision — the IRSA/OIDC least-privilege core is too safety-dense to bundle with cert/DNS plumbing). **This is 12.7a** (DNS/TLS). 12.7b (IRSA + GitHub OIDC + CI deploy role + CI EKS access entry) follows as a separate commit.

## Feature
Provision DNS + TLS for the custom domains: a Route53 zone lookup, the **two ACM certs** (CloudFront cert in **us-east-1** via the aliased provider; ALB cert **regional**), both **DNS-validated**, the `wc.${ROOT_DOMAIN}`→CloudFront alias record, and the in-place **edit of `s3_cloudfront.tf`** to attach the validated us-east-1 cert + `wc.` alias to the 12.6 distribution. **Also re-enables tflint `terraform_unused_declarations`** (all three muted forward-declarations now have consumers).

## Use case + traceability
- **Task ID:** 12.7 (part a of the split)
- **Architecture sections:** `§12` (Route53 records; ACM CloudFront cert in us-east-1 + ALB cert regional; `ROOT_DOMAIN` required), `Appendix C.6` (`route53_acm.tf`), `RISK-010` (CloudFront cert in us-east-1).
- **Requirements:** REQ-O-008 (custom domains over TLS), REQ-O-011 (region/cert posture).
- **Depends on:** 12.1 (the `aws.us_east_1` aliased provider + `ROOT_DOMAIN` var), 12.6 (the `aws_cloudfront_distribution.assets` to attach the cert/alias to).
- **⚠️ OPEN FINDING (escalated to lead/human):** the `api.wc.${ROOT_DOMAIN}`→**ALB** alias is NOT in this slice — the ALB is controller-created at deploy time (post-terraform), so that alias can't be a pure-terraform record. Pending the human's A/B/C decision (recommended: CI-scripted in 12.11). **This slice creates the ALB CERT (regional, DNS-validated — independent of the ALB) but NOT the api.wc alias record.**

## Acceptance criteria
- [ ] `route53_acm.tf`:
  - [ ] `data "aws_route53_zone" "root"` for `var.ROOT_DOMAIN` (the hosted zone pre-exists — HITL-registered; this slice manages records, not the zone). Output `hosted_zone_id = data.aws_route53_zone.root.zone_id`.
  - [ ] `aws_acm_certificate "cloudfront"` for `wc.${var.ROOT_DOMAIN}`, **`provider = aws.us_east_1`**, `validation_method = "DNS"` (RISK-010).
  - [ ] `aws_acm_certificate "alb"` for `api.wc.${var.ROOT_DOMAIN}`, **default (regional) provider**, `validation_method = "DNS"`.
  - [ ] DNS validation records (`aws_route53_record` via `for_each` over each cert's `domain_validation_options`) + `aws_acm_certificate_validation` for **both** certs (the us-east-1 one needs `provider = aws.us_east_1`).
  - [ ] `aws_route53_record "wc"` — alias A record `wc.${var.ROOT_DOMAIN}` → CloudFront: `alias { name = aws_cloudfront_distribution.assets.domain_name, zone_id = "Z2FDTNDATAQYW2" (CloudFront's fixed hosted-zone id), evaluate_target_health = false }`.
- [ ] **Edit `s3_cloudfront.tf`** — swap the `viewer_certificate` block to `{ acm_certificate_arn = aws_acm_certificate_validation.cloudfront.certificate_arn, ssl_support_method = "sni-only", minimum_protocol_version = "TLSv1.2_2021" }` and add `aliases = ["wc.${var.ROOT_DOMAIN}"]` to the distribution (the deferred 12.6→12.7 attach, LESSONS §10).
- [ ] **Re-enable `terraform_unused_declarations`** in `.tflint.hcl` (remove the mute) — `ROOT_DOMAIN` (certs + alias), `rds_minor_version` (12.3), and the `aws.us_east_1` alias (CloudFront cert) now ALL have consumers. Consumes the 12.1 carry-forward. `tflint` must stay rc=0 with the rule active.
- [ ] Outputs: `hosted_zone_id`, `alb_cert_arn` (= `aws_acm_certificate_validation.alb.certificate_arn` — consumed by the 12.8 ingress), optionally `cloudfront_cert_arn` (live; append-only LESSONS §3).
- [ ] **Verification (agent-side):** `fmt -check` clean; `init -backend=false` resolves; `validate` passes; `tflint` rc=0 **with unused_declarations re-enabled**.
- [ ] **HITL-deferred:** real `plan`/`apply`; the ACM DNS-validation wait (blocks at apply until DNS resolves); the api.wc→ALB alias (pending the Finding).
- [ ] Cross-doc invariant: **none** (DNS/cert infra; no Appendix A model).

## Files expected to touch
**New:** `infra/terraform/route53_acm.tf`.
**Modified:** `infra/terraform/s3_cloudfront.tf` (viewer_certificate + aliases — the 12.6→12.7 attach), `infra/terraform/outputs.tf` (+`hosted_zone_id`, `alb_cert_arn`), `infra/terraform/.tflint.hcl` (remove the unused_declarations mute).

If you need files beyond this, **flag at Step 2.5**.

## Verification outline (Step 2 substitute)
1. `fmt -check -recursive` → rc=0.
2. `init -backend=false` → resolves (the `aws.us_east_1` aliased provider is now actually used).
3. `validate` → "Success!" — the edited distribution references the validated cert; no undeclared refs.
4. `tflint` → rc=0 **with `terraform_unused_declarations` re-enabled** (verify all three former forward-declarations now resolve).
5. **Negatives (plan-time → HITL):** the CloudFront cert is in `us-east-1` (aliased provider) and the ALB cert in `var.region`; a cert without DNS validation records would fail.

## Cross-doc invariant impact
- **Model field changes:** none.
- **Orchestrator doc rows (Step 9):** I'll route the tflint-rule re-enable (closes the 12.1 carry-forward); a Convention candidate likely (the us-east-1-aliased-provider cert pattern; DNS-validated certs). The `api.wc` Finding outcome I reconcile per the human's decision.

> Implementer never edits orchestrator-territory files — flag at Step 9.

## Things to flag at Step 2.5
1. **Q1 — Route53 zone: data source (not created).** `data "aws_route53_zone" "root"` assumes the hosted zone for `ROOT_DOMAIN` pre-exists (HITL-registered the domain). **My default vote:** data source (we manage records, not the zone). Confirm; note the HITL prereq (the zone must exist before apply).
2. **Q2 — cert ARN reference in the distribution.** Use `aws_acm_certificate_validation.cloudfront.certificate_arn` (NOT the raw `aws_acm_certificate.cloudfront.arn`) so CloudFront only gets the cert once DNS-validated. **My default vote:** the `_validation` resource's `certificate_arn`. Confirm.
3. **Q3 — re-enable unused_declarations now.** Confirm all three (ROOT_DOMAIN, rds_minor_version, aws.us_east_1) resolve with the rule active → `tflint` rc=0. If anything still trips, flag it (don't silently keep the mute — that's the LESSONS §1 anti-pattern).
4. **Q4 — api.wc→ALB alias is OUT of this slice (Finding-deferred).** Confirm you author the ALB CERT (regional, DNS-validated) but NOT the api.wc alias record. The alias lands per the human's A/B/C decision (likely CI-scripted in 12.11). Surface at Step 9 so I reconcile the tracker once the human rules.
5. **Q5 — `alb_cert_arn` output.** The 12.8 ALB ingress needs the regional cert ARN (annotation `alb.ingress.kubernetes.io/certificate-arn`). Export it. Confirm the output name.

## Dependencies + sequencing
- **Depends on:** 12.1 (us_east_1 alias + ROOT_DOMAIN), 12.6 (the CloudFront distribution to attach to).
- **Blocks:** 12.7b (independent, but same task), 12.8 (ingress needs `alb_cert_arn`), 12.11 (deployed smoke needs `wc.` resolving + — pending Finding — `api.wc`).

## Estimated commit count
**1 commit** (12.7a). Standalone within the 12.7 split. Not the safety core (that's 12.7b — IRSA/OIDC); 12.7a is DNS/TLS/cert plumbing + the RISK-010 us-east-1 cert posture.

Conventional commit (I finalize at Step 9): `build(infra): Route53 zone + ACM certs (CloudFront us-east-1, ALB regional) + CloudFront cert/alias attach (12.7a)`.

## Lessons-logged candidates anticipated
- **Convention candidate** — "the us-east-1-aliased-provider is used for the CloudFront ACM cert (RISK-010); ALB cert is regional; both DNS-validated via `aws_acm_certificate_validation`; reference the `_validation.certificate_arn` (not the raw cert arn) in consumers so they wait for validation."
- **Carry-forward closure** — re-enabling tflint `terraform_unused_declarations` (the 12.1 carry-forward) — I'll tick it.
- **Finding reconciliation** — the api.wc→ALB alias mechanism (per the human's decision).

## How to invoke
1. Read end-to-end — especially Q4 (api.wc deferral — Finding) + Q3 (re-enable the tflint rule).
2. Pre-flight: cwd; `terraform`/`tflint`. Infra LESSONS §1–§10 apply (§1 re-enable discipline; §3 don't-reference-the-ALB; §10 cert-attach).
3. **Run `/tdd 12.7a-route53-acm-cert-attach`** — RED/GREEN → "author `.tf` → `validate` passes."
4. Step 0 / Step 1 — confirm (1 NEW + 3 MODIFIED, incl. editing s3_cloudfront.tf + re-enabling the tflint rule).
5. **Step 2.5** — `SendMessage` me the design write-up + Q1–Q5. Spike the cert/validation + the tflint re-enable. Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
6. **Step 9** — categorized summary + ship/no-ship + draft commit; confirm the tflint rule re-enabled (rc=0), the cert attach to the 12.6 distribution, and the api.wc deferral (for tracker reconciliation).
