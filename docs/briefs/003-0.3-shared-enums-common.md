# /tdd brief — `:shared` enums + common base entities + Clock/Org-time config

## Feature
Create the 16 typed Java enums (constant sets mirroring `ARCHITECTURE.md` Appendix A / Appendix B.1 **exactly**), the `common/` base-entity scaffolding (`AbstractAuditingEntity`, `PersistableUuidEntity`), and the time config (`ClockConfig` injectable `java.time.Clock`, `OrgTimeConfig` org-tz + Mon–Sun week resolver) in the `:shared` module — no domain entities, no business logic.

## Use case + traceability
- **Task ID:** 0.3 (Phase 0 — Monorepo & toolchain foundation)
- **Architecture sections it implements:** Appendix A (model/contract inventory — enum-bearing models), **Appendix B.1 (shared enum wire vocabulary — the canonical value sets)**, Appendix C.2 (`:shared` file tree), §3 (state machines that consume `PlanState`/`ReviewStatus`/`DisputeStatus`), §17 (injectable `Clock` for derived state).
- **Requirements:** REQ-O-016, REQ-D-010 (status columns are `VARCHAR`+`CHECK` mirrored by Java enums).
- **Related context:** Slices 0.1 (`555c2a8`) + 0.2 (`d888f92`) landed the build. 0.2 left a throwaway `BuildSkeletonMarker`(+test) in each module for coverage — **this slice replaces the `:shared` one with real typed code** (keep the api/worker placeholders; they go in 0.4/0.5).

## ⚠ This is the first CROSS-DOC INVARIANT slice
The 16 enum value sets **are cross-doc invariants** mirrored in Appendix A / Appendix B.1. The value-set assertion tests below ARE the executable enforcement of that contract. Per the routing matrix, **you do not edit `ARCHITECTURE.md` / `apps/wc-api/CLAUDE.md`** — flag the invariant at Step 9 (categorized `Cross-doc invariant change → NEW`) and I write the cross-doc table row(s) + confirm Appendix A/B.1 alignment hot during the same session. Commits stagger: your Step-10 lands code+tests; my round commit lands the doc rows.

## Scope boundary
- ✅ 16 enums, `AbstractAuditingEntity`, `PersistableUuidEntity`, `ClockConfig`, `OrgTimeConfig`, their tests; remove the `:shared` `BuildSkeletonMarker`(+test).
- ❌ **No `@Entity` domain classes** (Employee, WeeklyPlan, etc.) → Phase 1 (1.x). No Flyway SQL → Phase 1. No repositories. No `WcApiApplication`/`WcSyncWorkerApplication` → 0.4/0.5. The `common/` base classes are `@MappedSuperclass` scaffolding with **no domain behavior**.

## The 16 enums — exact value sets (from Appendix B.1 / task 1.1; copy verbatim)
- `PlanState{DRAFT,LOCKED,RECONCILING,RECONCILED}`
- `RoleType{IC,MANAGER}`
- `CommitmentKind{PLANNED,UNPLANNED}`
- `Priority{P0,P1,P2}`
- `WorkType{STRATEGIC,MAINTENANCE,BLOCKER,UNPLANNED}`
- `Confidence{HIGH,MEDIUM,LOW}`
- `AlignmentStatus{ALIGNED,NEEDS_REVIEW,MISALIGNED}`
- `ReviewStatus{NOT_REVIEWED,REVIEWED_WITH_DISPUTES,REVIEWED}` — **NO `OVERDUE`** (derived only)
- `DisputeStatus{OPEN,IC_RESPONDED,RESOLVED}`
- `ReconciliationOutcome{COMPLETED,PARTIALLY_COMPLETED,BLOCKED,CANCELED,CARRIED_FORWARD}`
- `FlagType{NEEDS_REVISION,MISALIGNED}`
- `CommentTargetType{PLAN,COMMITMENT}` — **only two** (not 4)
- `SyncRelatedType{WEEKLY_PLAN,MANAGER_REVIEW_WEEK}` — `MANAGER_REVIEW_WEEK` (not `MANAGER_REVIEW`)
- `EventKind{IC_PLANNING,IC_RECONCILIATION,MANAGER_REVIEW_BLOCK}`
- `SyncStatus{PENDING_PUBLISH,QUEUED,SYNCING,SYNCED,FAILED,RETRY_REQUESTED}`
- `RiskBadge{MISALIGNED,NEEDS_REVIEW,BLOCKED,CARRY_FORWARD,UNREVIEWED,OVERDUE_REVIEW}` — exactly 6

## Acceptance criteria
- [ ] All 16 enums exist under `com.st6.wc.enums`; each `.values()` equals the exact Appendix B.1 set (count + spelling).
- [ ] Negative pins hold: `ReviewStatus` has no `OVERDUE`; `CommentTargetType` is exactly `{PLAN,COMMITMENT}`; `SyncRelatedType` has `MANAGER_REVIEW_WEEK` (not `MANAGER_REVIEW`); `valueOf("OVERDUE")` / `valueOf("MANAGER_REVIEW")` throw `IllegalArgumentException`.
- [ ] `RiskBadge` contains exactly the 6 enumerated badges.
- [ ] `AbstractAuditingEntity` is a `@MappedSuperclass` exposing `created_by/created_at/updated_by/updated_at`; `PersistableUuidEntity` provides a UUID PK + `@Version` token base. Lombok `@Getter/@Setter` only (**never `@Data`**) — passes the `forbidLombokData` gate.
- [ ] `ClockConfig` exposes an injectable `java.time.Clock` bean (system default) that is **overridable with a fixed `Clock`** in tests.
- [ ] `OrgTimeConfig` exposes the org timezone (default `America/Chicago`, env-overridable) and a **Monday–Sunday week resolver** that returns the correct `week_start_date` (Monday) for arbitrary dates incl. weekend boundaries.
- [ ] `./gradlew check` green (all gates incl. JaCoCo ≥80% on `:shared` — the real classes now provide coverage; Lombok `@Generated` methods are JaCoCo-excluded via the 0.2 `lombok.config`).
- [ ] Cross-doc invariant flagged at Step 9 (NEW — enum vocabulary).

## Files expected to touch
**New:**
- `apps/wc-api/shared/src/main/java/com/st6/wc/enums/` — the 16 enums.
- `apps/wc-api/shared/src/main/java/com/st6/wc/common/AbstractAuditingEntity.java`, `PersistableUuidEntity.java`.
- `apps/wc-api/shared/src/main/java/com/st6/wc/config/ClockConfig.java` (**`:shared`** — see Q1).
- `apps/wc-api/shared/src/main/java/com/st6/wc/common/OrgTimeConfig.java`.
- `apps/wc-api/shared/src/test/java/com/st6/wc/...` — enum value-set tests + Clock/OrgTime tests.

**Modified:**
- `apps/wc-api/shared/build.gradle` — likely add `jakarta.persistence-api` (for `@MappedSuperclass`/`@Version`/`@Column`) if not already on the JPA baseline, and `spring-boot-starter-test` (testImplementation) for the `ClockConfig` Spring-context test. Confirm the dep baseline at Step 2.5.

**Removed:**
- The `:shared` `BuildSkeletonMarker`(+test) from 0.2 (real classes replace it for coverage).

If implementation needs files beyond this list, **flag at Step 2.5** before going GREEN.

## RED test outline (Step 2)
> Unlike 0.1/0.2 these are **genuine red-green deterministic tests** (JUnit 5).

1. **`enum_value_sets_match_appendixB1`** — parameterized over all 16 enums: assert `values()` (as a name-set) equals the exact expected set above.
   - Why: Appendix B.1 / REQ-D-010 — enums are the executable mirror of the `VARCHAR`+`CHECK` vocabulary.
2. **`reviewstatus_has_no_overdue`** + **`commenttargettype_is_two`** + **`syncrelatedtype_uses_week`** — targeted negatives; `valueOf("OVERDUE")` and `valueOf("MANAGER_REVIEW")` throw.
   - Why: the three highest-risk drift points (§3 OVERDUE-derived; §11 flat comments; §10 per-manager/week key).
3. **`riskbadge_exactly_six`** — count + membership.
4. **`clock_bean_injectable_and_overridable`** — minimal Spring context exposes a `Clock`; a `@TestConfiguration` fixed `Clock` overrides it (proves deterministic-time testability for §17 derived state).
5. **`orgtime_default_and_week_resolver`** — default tz is `America/Chicago`; week resolver maps sample dates to the correct Monday `week_start_date` (assert a mid-week date, a Monday, a Sunday → all map to that week's Monday).
6. **`base_entities_shape`** — reflectively assert `AbstractAuditingEntity` is `@MappedSuperclass` with the 4 audit fields and `PersistableUuidEntity` has a UUID id + `@Version` field; assert no `@Data` present (belt-and-suspenders with the `forbidLombokData` gate).

## Cross-doc invariant impact (I write the docs at Step 9)
- **Model field changes:** NEW — the enum vocabulary (16 enums) becomes a tracked cross-doc invariant.
- **Orchestrator doc rows to write hot:** add a cross-doc-invariants row in `apps/wc-api/CLAUDE.md` for the **enum vocabulary** (mirrors Appendix A / Appendix B.1), and confirm Appendix B.1 ↔ code alignment. No `ARCHITECTURE.md` field edits expected (the enums already exist in B.1) — this is a code-catches-up-to-contract slice, so the doc work is recording the invariant, not changing the contract.

## Things to flag at Step 2.5
1. **`ClockConfig` module placement — `:shared` vs `:api`.** I found a cross-doc inconsistency: Appendix C.2, task 0.3, and `MVP_TASKS.md:397` put `ClockConfig` in **`:shared`**; task 1.1's Files line (`MVP_TASKS.md:181`) is the outlier putting it in `:api`. My default vote: **`:shared`** (`com.st6.wc.config.ClockConfig`) — the `Clock` is cross-cutting (consumed by `:api` SLA/derivation services AND `:worker` time-based transitions), and `:shared` is depended on by both. A `@Configuration @Bean` works in `:shared` (it has spring-context via the JPA baseline). I'll reconcile task 1.1's outlier line. **Push back if you want `:api`** (e.g. to keep `:shared` free of Spring `@Configuration`).
2. **`OrgTimeConfig` week resolver — functional vs stub.** Task 0.3 says "stub," but 1.1 wants a working Mon–Sun resolver and it's trivial deterministic logic. My default vote: **implement it functionally now with tests** (cheap, and it satisfies 1.1 too) rather than a hollow stub.
3. **0.3 ↔ 1.1 overlap.** Task 1.1 (Phase 1) re-specifies the same enums + common + Clock/OrgTime. My default: **implement them fully and correctly here**; at Phase 1 entry I (orchestrator) mark 1.1's enum/common/config items as **satisfied-by-0.3** (1.1 becomes a verify-against-Appendix-A check, not a re-write). You won't redo this. Flagging so you build it as the real thing, not a throwaway.
4. **`:shared` Spring `@Configuration` + test deps.** Confirm `:shared` `build.gradle` has (or add) `jakarta.persistence-api` + `spring-boot-starter-test` (testImplementation) for the base-entity annotations and the `ClockConfig` context test. My default: **add what's missing to `:shared` build.gradle**, keeping deps minimal (no web/security).
5. **Lombok on the base superclasses.** `@Getter/@Setter` + `@MappedSuperclass` (no `@Data`; `@Builder`/`@SuperBuilder` is awkward on an abstract auditing superclass and not needed yet). My default: **`@Getter/@Setter` only on the superclasses**; defer `@SuperBuilder` to when concrete entities land (Phase 1). Confirm JaCoCo excludes Lombok `@Generated` so the superclass getters/setters don't drag coverage.

## Dependencies + sequencing
- **Depends on:** 0.2 (`d888f92`) — `:shared` module + gates exist.
- **Blocks:** Phase 1 (entities/migrations bind these enums + extend the base superclasses verbatim); 0.4/0.5 (apps may inject the `Clock`).

## Estimated commit count
**1 — standalone. Do NOT bundle.** This is the first cross-doc-invariant slice; keeping it isolated gives clean traceability for the enum-vocabulary ↔ Appendix A/B.1 pairing (per the brief-template "don't bundle a cross-doc invariant change" rule). The 0.4+0.5 bundle comes next.

## Lessons-logged candidates anticipated
- **Cross-doc invariant change (NEW)** — the enum vocabulary as a tracked invariant (→ `apps/wc-api/CLAUDE.md` cross-doc table; I write it).
- **Architecture-doc note** — the `ClockConfig` `:shared`-vs-`:api` reconciliation (task 1.1 outlier) + the 0.3↔1.1 overlap resolution.
- **Convention candidate** — the enum value-set parameterized-test pattern (Phase 1 entities reuse it to pin DB `CHECK`-constraint ↔ enum alignment).

## How to invoke
> Session already oriented (slices 0.1/0.2 ran in it) — jump straight to `/tdd`.
1. **Read this brief end-to-end** — note the cross-doc-invariant handling + the 5 Step-2.5 questions.
2. **Run `/tdd shared-enums-common`.**
3. **Step 2.5** — send the design write-up + answers to Q1–Q5. Do not go GREEN until I sign off (the `ClockConfig` placement Q1 + dep-baseline Q4 want a quick confirm).
4. **Step 9** — flag the enum vocabulary as `Cross-doc invariant change → NEW`; I write the table row + Appendix alignment confirmation hot.
