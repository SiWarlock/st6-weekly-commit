# /tdd brief — ST.6b: CommandCenter dense-table + filter chips

## Feature
Two CommandCenter visual-composition gaps, render-only + Tailwind/token-native: (1) **dense table** — zebra-striped rows + the alignment counts rendered as **tone-colored count pills** (glyph + count + tone, accessible label) instead of plain "label: N" text; (2) **filter chips** — `CommandCenterFilters` shows the active filters as removable chips + a clear-all, on top of the existing controls. Second ST.6 sub-slice; `/tdd`.

## Use case + traceability
- **Task ID:** ST.6 (Phase ST — manager-surface visual composition; slice b)
- **Spec source (binding visual):** `docs/design/cadence-design-system/ui_kits/weekly-commit/CommandCenter.jsx` (dense table — zebra rows, `.wc-count` mono count pills, filter chips) + `components.css` `.wc-count` (mono numeral+glyph pill) / the filter-chip spec. **Architecture:** `ARCHITECTURE.md §9` (manager command-center projection — the per-row counts), §7 (styling-SoT). REQ-S-005 (count pills must be glyph+count+tone with an accessible label — never color-alone).
- **Related context:** 9.9 (`CommandCenter` rows/filters/mark-reviewed; the `COUNTS` array maps misaligned/needsReview/blocked/carryForward/unresolvedDispute → row fields), ST.4 (`62b4b92` — sticky thead + hover rows + `max-w-content-max`; `CommandCenter.tsx` already in the `surfaceSkin` guard), `statusTaxonomy.ts` §7 (the RISK tone map already covers MISALIGNED/NEEDS_REVIEW/BLOCKED/CARRY_FORWARD — reuse it for the pill tones). The Cadence `.wc-*` = visual spec only (forbidden #3).

## Acceptance criteria
- [ ] `manager/CommandCenter.tsx` rows are **zebra-striped** (alternating row background via a token — e.g. even rows a faint `bg-surface-raised`/sunken, odd rows base), on top of the existing `hover:bg-surface-hover` + sticky thead.
- [ ] The **alignment counts** (the `COUNTS` array: misaligned / needsReview / blocked / carryForward / unresolvedDispute) render as **tone-colored count pills** — each pill carries the **count number + a category glyph + the §7 tone** (reuse the existing RISK taxonomy tones for MISALIGNED/NEEDS_REVIEW/BLOCKED/CARRY_FORWARD; pin the dispute-count tone at 2.5), with an **accessible label** (`title`/`aria-label`, e.g. "Misaligned: 1") so it's never color-alone (REQ-S-005). Replaces the current plain "label: count" text.
- [ ] `manager/CommandCenterFilters.tsx` renders the **active filters as removable chips** (token-native: `bg-brand-soft text-brand-400` chip + an `×` clear affordance per chip) below/with the existing controls, plus a **"Clear all"**. The underlying controls keep working (chips are an additive display + per-filter clear).
- [ ] **Render-only — NO enum/Appendix-A change** (consumes the B.11 row counts + filter params verbatim). **No `.wc-*` CSS / no second stylesheet / no hex / no arbitrary `[…]` hatches** (forbidden #3). Both `[data-theme]` values render. `yarn` typecheck + lint + prettier + full Vitest pass.

## Files expected to touch
**Modified:**
- `apps/wc-web/src/features/manager/CommandCenter.tsx` (+ `.test.tsx`) — zebra rows + tone count pills for the alignment column
- `apps/wc-web/src/features/manager/CommandCenterFilters.tsx` (+ `.test.tsx`) — active-filter chips + clear-all
- `apps/wc-web/src/app/theme/surfaceSkin.test.ts` — add `CommandCenterFilters.tsx` to the structural guard (`CommandCenter.tsx` is already in it)

If a count-pill helper or a tiny count-pill component is wanted (vs inline), your call — flag at 2.5. Avatar disc (no component exists) is **optional** — see Q2.

## RED test outline (Step 2)
1. **`command_center_rows_are_zebra_striped`** (`CommandCenter.test.tsx`, render) — consecutive data rows carry alternating background classes (assert via a `data-row-parity`/`even`-`odd` hook or the alternating class).
2. **`alignment_counts_render_as_tone_pills`** (render) — each alignment count renders as a pill with the count number + the §7 tone class + an accessible label (`title`/`aria-label` containing the category name + count); NOT plain "Misaligned: 1" text. (Confirm zero-count handling at 2.5.)
3. **`filter_chips_reflect_active_filters_and_clear`** (`CommandCenterFilters.test.tsx`, render) — setting a filter renders a removable chip naming it; clicking the chip's `×` clears that filter (chip disappears + the control resets); "Clear all" present and clears all chips.
4. **`no_wc_star_css_or_hardcoded_token_introduced`** (extend `surfaceSkin.test.ts` with `CommandCenterFilters.tsx`) — no `.wc-*`/hex/arbitrary-`[…]`.

## Cross-doc invariant impact
- **NONE.** Render-only; consumes B.11 `ManagerCommandCenterRowDto` counts + the filter params verbatim. The pill tone map reuses the existing §7 RISK taxonomy (visual, not contract). No Appendix-A/B change.

> **Implementer never edits `apps/wc-web/CLAUDE.md`, `ARCHITECTURE.md`, `MVP_TASKS.md`, or `apps/wc-web/LESSONS.md`.**

## Things to flag at Step 2.5
1. **Count-pill form + tone map + zero handling.** **My default vote:** tone-colored pills reusing the §7 RISK tones (MISALIGNED→failure, NEEDS_REVIEW→warning, BLOCKED→failure, CARRY_FORWARD→info), each with count + glyph + an `aria-label`/`title` (accessible, not a cryptic bare initial). Pin the **dispute-count** tone (no RISK value for it — failure or accent?). **Zero counts:** render muted (e.g. `0` in `text-ink-muted`) or omit — confirm (default: muted, so the manager sees the full set at a glance).
2. **Avatar disc — optional, default SKIP.** No `Avatar` component exists; the Cadence 26px initials disc is a nicety. **My default vote:** skip for 6b (keep the name text) — author a tiny initials disc only if you think it's quick + worth it. Confirm.
3. **Filter-chip wiring.** **My default vote:** derive the active-filter chips from the controlled filter state; each chip's `×` patches that filter key to undefined (resets to page 0, consistent with the existing filter-change behavior); "Clear all" resets to the default params. Confirm the reset semantics.
4. **Bundle size.** Zebra + count-pills (CommandCenter) + chips (CommandCenterFilters) are bundled as "CommandCenter visual polish." If it feels too big at RED, **flag** — I'm fine splitting (table-polish / filter-chips) into two commits.

## Dependencies + sequencing
- **Depends on:** 9.9 (`CommandCenter`/`CommandCenterFilters`) + ST.4 (`62b4b92` sticky/hover/widths) — landed. ST.6a (`41f1052`) — landed.
- **Blocks/feeds:** ST.6c (manager Drawers — review + drilldown), ST.7 (a11y + `/design-review`).
- **Not blocked.** (At-a-glance SLA strip deferred — backend `summary` dep, lead-confirmed Carry-forward; DisputePanel disputes-blocked. Neither is in 6b.)

## Estimated commit count
**1 commit (split to 2 if large) — NOT safety-critical** (render-only; same `features/manager/` area; related table-polish features). RED→2.5→GREEN per feature, one Step-10 commit (or split table / chips). Exact fidelity → ST.7 `/design-review`.

## Lessons-logged candidates anticipated
- Likely none new — reuses the §7 RISK taxonomy + token utilities. Flag if the "count → tone pill with accessible label" or the "active-filter chip + clear" pattern reads as a reusable convention.

## How to invoke
1. **Read this brief** + `CommandCenter.tsx` (the `COUNTS` map + alignment column, ~line 58-72 / 165-178) + `CommandCenterFilters.tsx` (current controls) + `statusTaxonomy.ts` (the RISK tone map) + the Cadence `CommandCenter.jsx` (dense table + `.wc-count` + filter chips).
2. **Run `/tdd ST.6b commandcenter dense-table filter-chips`** (session already oriented).
3. **Step 0/1** — confirm Feature + Files; Tailwind/token-native.
4. **Step 2.5** — send the test write-up + Q1–Q4 (esp. Q1 pill tone map + zero-handling, Q3 chip reset semantics). Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
5. **Step 7.5** — reachability: dense table + chips on `/manager/command-center` (already-wired). Flag gaps.
6. **Step 9** — categorized summary + ship/no-ship + draft commit; confirm cross-doc invariant = NONE + no `.wc-*`/hardcoded-token + the avatar disposition.
