# /tdd brief — Cluster add-ons install + consistency: Secrets Store CSI Driver + ASCP + external-dns (12.2b)

> **Infra brief — NOT red-green TDD** (see brief 001 banner). Terraform `helm_release`s verify via `terraform fmt`/`init -backend=false`/`validate`/`tflint` rc=0 (+ optional offline `helm template` render; + `kubeconform` re-run on the remaining `infra/k8s/*` after the external-dns.yaml removal). Run `/tdd 12.2b-secrets-store-csi-driver-ascp`; substitute "author `.tf` → validate/lint passes" for RED→GREEN. **Posture: author + validate only — NO `helm install` / `kubectl apply` (HITL).**
>
> **Origin + scope (read first):** a **Finding** at 12.9 Step-2.5 — the decomposition installed the ALB controller (`helm_release`) but never the **Secrets Store CSI Driver / ASCP** that 12.6/12.7b/12.8/12.9 ride on (without it the deploy fails: no `SecretProviderClass` CRD, volumes won't mount). The **lead then directed consistency** across the three cluster add-ons (ALB controller, external-dns, CSI driver). Since the ALB controller + CSI driver are effectively helm-only (CRDs/webhooks), the consistent mechanism is **Terraform `helm_release` for all cluster add-ons** — so this slice ALSO **converts external-dns from its 12.8 raw manifest to a `helm_release`** (removing `infra/k8s/external-dns.yaml`). See `MVP_TASKS.md` task **12.2b** + LESSON **§17**.

## Feature
Author `infra/terraform/addons.tf` with three `helm_release`s — Secrets Store CSI Driver + AWS provider (ASCP) into `kube-system`, and external-dns into `wc` — establishing the convention **"platform controllers install via Terraform `helm_release`; app workloads stay kubectl manifests."** Remove the now-redundant `infra/k8s/external-dns.yaml` (its SA/Deployment/RBAC become chart-managed; the external-dns IRSA role + the ingress hostname annotation are unchanged).

## Use case + traceability
- **Task ID:** 12.2b (cluster add-ons; ROUND-2 gap-fix + consistency).
- **Architecture sections:** `§12` (CSI Driver + ASCP secret delivery; per-workload IRSA; external-dns owns api.wc — Decision 2), `Appendix C.6` (terraform layout — **gains `addons.tf`**), `Appendix C.7` (k8s layout — **drops `external-dns.yaml`**), REQ-S-010 / REQ-O-008 / RISK-016.
- **Decisions/lessons:** `docs/decisions/001` D2 (external-dns) + D3 (boundary — N/A to helm resources, only the IRSA role carries it, already committed); LESSONS §4 (pin charts vs the live registry), §5 (helm provider configured in 12.2), **§17** (install-don't-just-consume + one-mechanism consistency).
- **Depends on (committed):** 12.2 (cluster + OIDC + configured helm provider + ALB-controller pattern), 12.7b/12.8 (the external-dns IRSA role `external_dns_role_arn` + its trust `sub = system:serviceaccount:wc:external-dns`).
- **Blocks:** the real apply of 12.8/12.9 + 12.11 (add-on install must precede `kubectl apply`).

## Acceptance criteria
- [ ] `addons.tf` — three `helm_release`s:
  - **`secrets_store_csi_driver`** — chart `secrets-store-csi-driver`, repo `https://kubernetes-sigs.github.io/secrets-store-csi-driver/charts`, namespace `kube-system`; values **`syncSecret.enabled=false`** (rule #7) + `enableSecretRotation` per ★Q-rotation.
  - **`secrets_store_csi_driver_provider_aws`** (ASCP) — chart `secrets-store-csi-driver-provider-aws`, repo `https://aws.github.io/secrets-store-csi-driver-provider-aws`, namespace `kube-system`.
  - **`external_dns`** — chart `external-dns`, repo `https://kubernetes-sigs.github.io/external-dns/`, namespace `wc`; values reproduce the 12.8 config: `provider=aws`, `policy=upsert-only`, `registry=txt`, `txtOwnerId=wc`, `sources=[ingress]`, `domainFilters=[<ROOT_DOMAIN>]`, `serviceAccount.name=external-dns` + `serviceAccount.annotations."eks.amazonaws.com/role-arn"=<the external-dns IRSA role ARN>` (reference the committed role; the trust `sub` is `wc:external-dns`).
- [ ] **Chart versions pinned** against the live registry/Artifact Hub at author time (LESSONS §4) — name the three verified versions inline (literals, like the ALB chart 3.3.0 pin).
- [ ] **Remove `infra/k8s/external-dns.yaml`** — `git rm`. The external-dns **IRSA role stays in `iam_irsa.tf`** (with its `ci_boundary`); the `ingress-api.yaml` `external-dns.alpha.kubernetes.io/hostname` annotation is unchanged (external-dns still reads it).
- [ ] ASCP needs **no new IAM** (uses the requesting pod's IRSA). No `permissions_boundary` on any helm resource (D3 applies to `aws_iam_role`s only — none created here).
- [ ] **Verification:** `terraform fmt -check -recursive`/`init -backend=false`/`validate`/`tflint` rc=0 (`unused_declarations` stays green); **`kubeconform -strict` on the remaining `infra/k8s/*` still passes** (external-dns.yaml removed cleanly, no dangling reference); optional `helm template` offline sanity render. Real `helm install`/`kubectl apply` = HITL.
- [ ] Cross-doc invariant: none. **Orchestrator writes (Step 9):** the **Appendix C.6** add (`addons.tf`) + **Appendix C.7** removal (`external-dns.yaml`) notes.

## Files expected to touch
**New:** `infra/terraform/addons.tf`.
**Deleted:** `infra/k8s/external-dns.yaml`.
**Possibly modified:** `variables.tf` only if you parameterize chart versions (★Q-pin default: literals, no var). No change to `iam_irsa.tf` (the external-dns role stays as-is) or `ingress-api.yaml`.

## Verification outline (Step 2 substitute)
1. `terraform fmt -check -recursive` / `init -backend=false` (helm provider + graph resolve) / `validate` / `tflint` → rc=0.
2. `kubeconform -strict -summary -ignore-missing-schemas infra/k8s/*.yaml` → still all valid after the external-dns.yaml removal (no orphaned reference).
3. **Structural assertions:** three `helm_release`s (2× `kube-system` + external-dns in `wc`); `syncSecret.enabled=false`; external-dns SA annotation = the committed external-dns IRSA role ARN; chart versions are pinned literals; no `aws_iam_role`/policy created in `addons.tf`.

## Things to flag at Step 2.5
1. **★Q-pin — three chart versions.** Verify `secrets-store-csi-driver`, `secrets-store-csi-driver-provider-aws`, and `external-dns` current chart versions against the live registry/Artifact Hub (Context7) and pin literals. **Default vote:** hardcoded `version = "x.y.z"` per release (like ALB 3.3.0). Report the three you verified.
2. **★Q-rotation — `enableSecretRotation`.** **Default vote:** enable with `rotationPollInterval≈2m` (rotated creds refresh without pod restart — production-correct, cheap). Flag if you'd keep the chart default (off) for MVP.
3. **★Q-extdns-ns — external-dns namespace.** The committed IRSA trust `sub` is `wc:external-dns`, so the chart installs into **`wc`** with SA `external-dns` (no IRSA re-touch). **Default vote:** keep `wc` (zero churn to the committed role). Moving it to `kube-system` would re-touch the IRSA trust (`iam_irsa.tf`) — avoid unless you see a reason. Confirm.
4. **Q-alb-relocate — file consistency for the committed ALB controller.** The ALB-controller `helm_release` currently lives in `eks.tf`; the three new add-ons go in `addons.tf`. **Default vote:** leave the ALB controller where it is (avoid re-churning committed `eks.tf` for a cosmetic move — the *mechanism* is already consistent: all four are `helm_release`s). Optionally note "future cleanup: consolidate into `addons.tf`." Flag your call — if you'd rather move it now for full file-consistency, that's acceptable (it's a safe relocation within the root module).
5. **Q-home — `addons.tf` vs per-add-on files.** **Default vote:** one `addons.tf` for the three (cohesive "cluster add-ons" file). Confirm.

## Cross-doc invariant impact
- **Model field changes:** none.
- **Orchestrator writes (Step 9):** **Appendix C.6** += `addons.tf`; **Appendix C.7** −= `external-dns.yaml` (the explainer note at C.7 line ~920 updates to "external-dns installs via `helm_release` in `addons.tf`"). A **Convention candidate** is already banked as LESSON §17 (install-don't-just-consume + one-mechanism).

## Dependencies + sequencing
- **Depends on:** 12.2 (cluster + helm provider), 12.7b/12.8 (external-dns IRSA role + trust sub).
- **Blocks:** real apply of 12.8/12.9 + 12.11 (the add-ons must install before `kubectl apply`; the 12.11 pipeline ordering already runs `terraform apply` before `kubectl apply`, so this is satisfied — note it in the 12.11 brief).

## Estimated commit count
**1 commit (cohesive).** Three `helm_release`s + one manifest removal — all "cluster add-on install" work, same area. Not a root-CLAUDE safety-rule slice (the IRSA least-privilege + boundary + no-etcd-sync safety already live in 12.6/12.7b/12.8/12.7c; this slice changes *install mechanism*, not IAM — `syncSecret.enabled=false` keeps the rule-#7 posture). Conventional commit (I finalize at Step 9): `build(infra): cluster add-ons via helm_release — CSI driver + ASCP + external-dns (12.2b)`.

## Lessons-logged candidates anticipated
- Already banked: **LESSON §17** (install-don't-just-consume + one-mechanism consistency). Surface confirmation at Step 9; flag if the spike reveals a refinement.
- **Architecture-doc note** — the Appendix C.6/C.7 edits (orchestrator writes).

## How to invoke
1. Read end-to-end — ★Q-pin + ★Q-rotation + ★Q-extdns-ns are load-bearing; the external-dns conversion is the non-obvious part.
2. Pre-flight: cwd `infra/`; `terraform`/`tflint`/`kubeconform` present; verify the three chart versions against the live registry (Context7/Artifact Hub) before authoring (LESSONS §4). Infra LESSONS §1–§17 apply.
3. **Run `/tdd 12.2b-secrets-store-csi-driver-ascp`** — substitute "author `.tf` + remove the manifest → validate/lint/kubeconform pass" for RED/GREEN.
4. Step 0 / Step 1 — confirm the file set (NEW `addons.tf`; DELETE `external-dns.yaml`).
5. **Step 2.5** — `SendMessage` me the design write-up + Q-pin/Q-rotation/Q-extdns-ns/Q-alb-relocate/Q-home answers (+ confirm fmt/validate/tflint + the kubeconform re-run green). Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
6. **Step 9** — categorized summary + ship/no-ship + draft commit; surface the three verified chart versions (toolchain) + the Appendix C.6/C.7 edits (I write them) + confirmation LESSON §17 holds. Strict explicit `git add`/`git rm` (the new `addons.tf` + the removed `external-dns.yaml`); no `-A`.
