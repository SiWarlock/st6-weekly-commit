# /tdd brief — 2.7 CORS config + `GET /api/me` (Phase-2 closer)

> **Security-touching + first real endpoint + first DTO contract.** CORS is a §16 security boundary (no wildcard, never relaxed in demo); `GET /api/me` is the first `/api/**` controller — it exercises the 2.6 chain end-to-end and proves persona-switch doesn't bypass scoping (REQ-F-032). **Standalone commit; recommended ad-hoc `security-reviewer` at Step 7→8.** Completes Phase 2.

## Feature
`CorsConfig` (exact-origin allow-list wired into the security chain, `allowCredentials=false`, `OPTIONS` preflight bypasses auth, never relaxed in demo) + `GET /api/me` returning the `MeDto` (B.3) for the resolved `UserPrincipal`.

## Use case + traceability
- **Task ID:** 2.7
- **Architecture sections it implements:** §12 + Appendix D.2 (CORS allow-list, line ~990), §16 (RISK-008 corollary — demo must not relax CORS), §5 (`GET /api/me` E1), Appendix B.3 (`MeDto` contract), §6 (relationship-driven `isManager`, REQ-F-032 persona-switch-still-scoped), Appendix F.7 (dev port 5173). Requirements: REQ-F-031, REQ-F-032, REQ-S-008.
- **Related context:** consumes the 2.6 chain (`UserPrincipal` in the `SecurityContext`, the per-mode chains, `ProblemDetailsExceptionHandler`), 2.4 (`isManager`), the `Employee` entity. **First DTO across the API boundary** — establishes the DTO-mirrors-Appendix-B pattern (forbidden-pattern #3: never return entities).

## Acceptance criteria (what "done" means)
- [ ] `CorsConfig` exposes a `CorsConfigurationSource` (exact-origin allow-list from `app.cors.allowed-origins`/`CORS_ALLOWED_ORIGINS` → `https://wc.${ROOT_DOMAIN}` + dev `http://localhost:5173`); **no wildcard**; methods `GET/POST/PATCH/DELETE/OPTIONS`; allowed headers `Authorization, Content-Type, X-Demo-Employee-Id`; **`allowCredentials=false`** (bearer transport).
- [ ] CORS is wired into **both** 2.6 `SecurityFilterChain`s (`http.cors(...)`); **preflight `OPTIONS` bypasses JWT/auth**; **demo mode does NOT widen** the allow-list (same list both modes).
- [ ] `GET /api/me` returns `MeDto` (B.3: `employeeId, email, displayName, role, persona, isManager, timezone`) for the resolved principal — `role` from the authoritative `Employee.role`, `isManager` relationship-driven (2.4), `persona` = `email` in `auth0` mode / the active persona key in demo mode.
- [ ] `GET /api/me` is **authenticated** (returns the caller's own identity — no `DomainAuthorizationService` call needed; self by definition); unauthenticated → `401` problem+json (via 2.6).
- [ ] **REQ-F-032:** a persona switch yields a **different** `MeDto` (different `employeeId`/`role`) AND the central authorizer (2.5) still denies cross-persona resource access — proven by composing the switched principal with `DomainAuthorizationService` (no Phase-3 endpoint needed).
- [ ] **DTO, never entity** across the boundary (forbidden-pattern #3); `MeDto` is a record.
- [ ] `./gradlew check` green. (Dedicated ad-hoc `security-reviewer` **downgraded to OPTIONAL / skip-by-default** — see the Security-reviewer section; the CORS properties are deterministically test-pinned and the persona proof composes already-reviewed 2.5/2.6.)
- [ ] **Phase 2 acceptance** (the §2 closer criteria): no domain endpoint reachable without the authn chain + central authorizer; security suite green; CORS exact-origin + no-demo-relax.

## Files expected to touch
**New:**
- `apps/wc-api/api/src/main/java/com/st6/wc/config/CorsConfig.java` — the `CorsConfigurationSource` bean.
- `apps/wc-api/api/src/main/java/com/st6/wc/me/MeController.java` — `GET /api/me`.
- `apps/wc-api/api/src/main/java/com/st6/wc/me/dto/MeDto.java` — the B.3 record.
- `apps/wc-api/api/src/main/java/com/st6/wc/me/MeService.java` (or a mapper) — `UserPrincipal` + `Employee` → `MeDto`.
- Tests: `me/MeControllerTest`, `config/CorsConfigTest` (or fold CORS assertions into the chain tests), `me/MeServiceTest`, + the REQ-F-032 integration test.

**Modified:**
- `apps/wc-api/api/src/main/java/com/st6/wc/config/SecurityConfig.java` — add `http.cors(...)` to both chains (Step-2.5 Q1).
- `apps/wc-api/api/src/main/resources/application*.yml` — `app.cors.allowed-origins` / `CORS_ALLOWED_ORIGINS` (+ prod/demo/local).

## RED test outline (Step 2)
1. **`getMe_ic_returnsCorrectDto`** — authenticated IC → `MeDto` with `role=IC`, `isManager=false`, correct email/displayName/timezone.
2. **`getMe_manager_isManagerTrue`** — a manager with an active direct report → `isManager=true`.
3. **`getMe_unauthenticated_401`** — no auth → `401` problem+json (composes 2.6).
4. **`getMe_returnsDtoNotEntity`** — response is the `MeDto` shape (no entity leakage; no lazy-init/over-exposure).
5. **`preflightOptions_allowedOrigin_succeedsWithoutAuth`** — `OPTIONS` from `https://wc.${ROOT_DOMAIN}` → 200, no auth required, correct `Access-Control-Allow-*` headers.
6. **`cors_devOriginLocalhost5173_allowed`** — dev origin allowed (F.7).
7. **`cors_disallowedOrigin_notEchoed`** — a disallowed origin is NOT reflected in the response CORS headers (no wildcard).
8. **`cors_allowCredentialsFalse`** — `Access-Control-Allow-Credentials` is false/absent.
9. **`cors_demoMode_doesNotWiden`** — the allow-list is identical in demo mode (RISK-008 corollary).
10. **`personaSwitch_yieldsDifferentMeDto`** — demo mode, two persona keys → two different `MeDto`s (different `employeeId`/`role`).
11. **`personaSwitch_authorizerStillDenies` (REQ-F-032, integration, Testcontainers)** — switch to persona-A, then assert `DomainAuthorizationService` denies persona-A accessing persona-B's seeded resource (404) — persona-switch does NOT bypass scoping. (Composes 2.5; no Phase-3 endpoint needed.)

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** **NEW — `MeDto` is the E1 response contract (Appendix B.3).** This is the **first DTO across the boundary**. Flag at Step 9 categorized as `Cross-doc invariant change`; **I (orchestrator) add the `MeDto`↔B.3 row to the `apps/wc-api/CLAUDE.md` cross-doc table** (the executable DTO mirrors B.3; fields bind to `Employee`/relationship in Appendix A) at `/orchestrate-end`. B.3 already exists in `ARCHITECTURE.md` — no Appendix edit needed unless a field shape diverges.
- Implementer does NOT edit `CLAUDE.md`/`ARCHITECTURE.md` — flag categorized; I write the row.

## Things to flag at Step 2.5
1. **CORS wiring.** A `CorsConfigurationSource` bean + `http.cors(Customizer)` in `SecurityConfig`'s **both** chains (so preflight bypasses auth in-chain), vs a `WebMvcConfigurer`. Default: **`CorsConfigurationSource` bean wired via `http.cors(...)`** (keeps CORS in the security chain; the §12 contract wants preflight-OPTIONS-bypasses-JWT, which the chain handles). Confirm it touches both mode chains identically (no demo widening).
2. **`persona` resolution — CONFIRMED `email` in BOTH modes** (the prior backend pair's Step-2.5 choice, orchestrator-confirmed; baked in so you don't re-litigate). Rationale: simplest stable per-identity key, no `MeService`→mode coupling, and a persona switch → different email → different `MeDto` (satisfies REQ-F-032). Locked to email-both; only raise it if you have a strong reason for demo→`employeeId.toString()`.
3. **`GET /api/me` authorization.** It returns the caller's OWN identity → **authenticated-only, no `DomainAuthorizationService` call** (self by definition). Default: **yes** — just the coarse authn gate; the authorizer isn't invoked for `/me`. Confirm (don't add a self-authz check that's a no-op).
4. **`MeService` vs inline mapper.** A `MeService` (`@Service`) vs a static mapper. Default: **a small `MeService`/mapper** that takes the `UserPrincipal` + loads the `Employee` (for email/displayName/timezone) → `MeDto`. Confirm whether it reloads the `Employee` (the principal carries only id/role/isManager) — yes, one `findById` for the display fields.

## Dependencies + sequencing
- **Depends on:** 2.6 (the chain + `UserPrincipal`-in-context + `ProblemDetailsExceptionHandler`), 2.4 (`isManager`), 2.5 (the authorizer, for the REQ-F-032 proof), the `Employee` entity.
- **Blocks:** Phase 3+ (the first real controller pattern + the DTO-boundary convention); the frontend `meApi`/`useCurrentUser` (9.5, already built against the B.3 contract) consumes this endpoint.
- **Closes Phase 2.**

## Estimated commit count
**1 — standalone, do NOT bundle.** It's the Phase-2 closer, carries the **first DTO cross-doc invariant** (wants its own traceable commit), touches the CORS **security boundary**, and modifies the 2.6 `SecurityConfig`. Moderate size (~8-10 files). Dedicated security-reviewer **optional/skip** (see section — context-budget + cycle-locked at the 2.7 boundary).

## Lessons-logged candidates anticipated
- **Convention candidate** — the first controller+DTO pattern: thin `@RestController` → `MeService` → `MeDto` record (entity never crosses the boundary, forbidden-pattern #3); `CorsConfigurationSource` wired via `http.cors()` into both mode chains (exact-origin, `allowCredentials=false`, preflight bypass, no demo widening).
- **Architecture-doc note candidate** — Phase 2 complete: the full authn→authz→DTO request path is live; persona-switch-still-scoped (REQ-F-032) proven.
- **Future TODO — operational** — (carry over from 2.6) the JPA-auditing populator (its own slice); the manager-phase `MANAGER_ROLE_REQUIRED`-must-route-through-authorizer guard.

## Security-reviewer (ad-hoc — OPTIONAL / skip-by-default for 2.7)
**Downgraded to optional (impl context-budget + cycle-locked at the 2.7 boundary).** Rationale: 2.7's security surface is **config + a thin read endpoint**, not novel authz/bypass logic. The CORS properties (no-wildcard origin; `allowCredentials=false`; demo does NOT widen the allow-list; preflight bypass; disallowed origins not reflected — RISK-008/§16) are **deterministically pinned by the CORS test matrix**, and the REQ-F-032 persona-switch proof composes 2.5/2.6's **already-security-reviewed** authz. So the marginal value of a dedicated adversarial pass is low. **Default: skip.** If anything CORS-subtle or entity-leak-ish surfaces during GREEN, run it then + escalate any rule-#3/#5/CORS-bypass issue to me.

## How to invoke
1. Read this brief end-to-end; answer the 4 Step-2.5 questions.
2. **Run `/tdd 2.7-cors-and-me-endpoint`** → Step 1 confirm files → Step 2.5 `SendMessage` your per-test write-up + answers to me; wait for `APPROVED.`/`TWEAK:`/`ADD:` before GREEN.
3. **Step 7→8** — security-reviewer **optional/skip** (see the Security-reviewer section); run only if a CORS-subtle / entity-leak concern surfaces during GREEN.
4. **Step 9** — categorized summary (incl. the `MeDto` cross-doc flag) + ship/no-ship + draft commit message (+ reviewer findings only if you ran it).
