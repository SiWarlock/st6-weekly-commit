# /tdd brief — MARK_REVIEWED plan-read affordance + real `unresolvedDisputeCount` (6.8)

## Feature
Close the two remaining gaps on the **manager plan-read** (E3/E4, `GET /api/plans/current` + `GET /api/plans/{id}`): (1) emit the **`MARK_REVIEWED`** affordance on `ManagerReviewDto.allowedActions` when the viewer is the plan owner's **active direct manager** and the review is in a markable state (the E16 enforcement now exists → "no affordance without enforcement" is satisfied); (2) replace the hardcoded **`unresolvedDisputeCount = 0`** with the **real** count. Both reuse machinery already in `PlanMapper` — the §35 `viewerIsDirectManager` (resolved once) and the 5.3b per-commitment nested disputes (already loaded) — so **no new query / no N+1 / no new authz**.

## Use case + traceability
- **Task ID:** 6.8 (the Phase-6 manager plan-read affordance carry-forward — `origin: 5.2`, projection-side resolved 6.2, plan-read half open).
- **Architecture sections:** Appendix B.7 (`ManagerReviewDto` — `allowedActions[]` + `unresolvedDisputeCount`), B.1/F.4/§15 (`AllowedAction.MARK_REVIEWED` — computed affordance vocab; "no affordance without enforcement"), §35/§24/§31 (per-viewer affordances on a shared read — resolve `viewerIsDirectManager` ONCE at the `PlanMapper` root, thread booleans, **affordance MIRRORS — not shares — the void-throw authorizer**, subset-pinned), §3/REQ-F-013 (review status; `REVIEWED_WITH_DISPUTES` not-overdue), §5 (E16 `POST /api/manager/reviews/{reviewId}/mark-reviewed` — the enforcing slice, 3.5/5.2), §9 (the projection `unresolved_dispute_count`, now correct from 6.2).
- **Related context (all exist — verified at HEAD `6ec8c8a`):** `PlanMapper` (`plan/mapper/PlanMapper.java:59` resolves `viewerIsDirectManager` ONCE via `findByDirectReportEmployeeIdAndActiveTrue`; line 65-71 maps the `commitmentDtos` with their **5.3b nested disputes**; line 84 calls `reviewMapper.toDto(review, 0)`); `ReviewMapper.toDto(review, unresolvedDisputeCount)` (`review/mapper/ReviewMapper.java:28` — emits `allowedActions=List.of()` today; docstring: "MARK_REVIEWED is emitted by the slice that enforces it" + "count 0 until the disputes slice wires it"); `AllowedAction.MARK_REVIEWED` (`action/AllowedAction.java:21`); `AllowedActionResolver` (the affordance computation seam); the E16 `ReviewService.markReviewed` enforcement (the affordance must mirror its precondition — §24/§31 subset).

## Acceptance criteria
- [ ] **`MARK_REVIEWED` affordance** emitted on `ManagerReviewDto.allowedActions` **iff** the viewer is the plan owner's **active direct manager** (`viewerIsDirectManager`, the existing §35 boolean) **AND** the review is in a markable state — the precondition **MIRRORS** the E16 `ReviewService.markReviewed` enforcement (§24/§31 subset-pin: affordance-eligible ⟹ E16 accepts; read `ReviewService.markReviewed` to derive the exact markable predicate — see Step-2.5 Q2). Thread `viewerIsDirectManager` (+ whatever the predicate needs) into `ReviewMapper.toDto`.
- [ ] **No affordance leak (§35, load-bearing):** the **IC owner** viewer (and any non-direct-manager) sees **empty** `ManagerReviewDto.allowedActions` (never `MARK_REVIEWED`). **TRACE every caller** of `PlanMapper`/`ReviewMapper` to confirm no affordance leaks to an unentitled viewer (E3/E4 only; the lifecycle lock/start/close responses are owner-only — confirm they don't surface a manager affordance).
- [ ] **Real `unresolvedDisputeCount`** on `ManagerReviewDto` — **derived from the already-loaded `commitmentDtos`** (count those whose nested `dispute` is present, i.e. an `OPEN`/`IC_RESPONDED` dispute, 5.3b) → **no new query, no N+1** (§35 "reuse an already-resolved child for derived conditions"). Replaces the hardcoded `0` at `PlanMapper:84`.
- [ ] The derived count **equals the §9 projection `manager_plan_summary.unresolved_dispute_count`** for the same plan (both count `OPEN`/`IC_RESPONDED` disputes, deduped per commitment) — assert parity in a test (sanity that the plan-read derivation and the projection agree; pick the derive-from-loaded-disputes source per Step-2.5 Q3).
- [ ] **No new authz, no new endpoint** — rides the existing E3/E4 authorizer (the plan read is already IDOR-safe); the affordance only MIRRORS the E16 authorizer's precondition (never calls it). `REVIEWED`/`REVIEWED_WITH_DISPUTES` reviews are not markable (no `MARK_REVIEWED`); a `DRAFT` plan has no review (null `managerReview`, unchanged).
- [ ] `./gradlew check` clean. **No security-reviewer** (a read-affordance slice on the already-authorized E3/E4 read — not a rule-#3 surface; the §35 affordance-leak TEST is the safety pin).

## Files expected to touch
**Modified:**
- `apps/wc-api/api/src/main/java/com/st6/wc/plan/mapper/PlanMapper.java` — derive `unresolvedDisputeCount` from `commitmentDtos`; pass it + `viewerIsDirectManager` (+ predicate inputs) into `reviewMapper.toDto(...)`.
- `apps/wc-api/api/src/main/java/com/st6/wc/review/mapper/ReviewMapper.java` — `toDto` gains `viewerIsDirectManager` (or a `canMarkReviewed` boolean); emit `MARK_REVIEWED` in `allowedActions` per the mirrored precondition; update the docstring (the affordance + count are now wired).
- (Possibly) `apps/wc-api/api/src/main/java/com/st6/wc/plan/AllowedActionResolver.java` — if the markable predicate belongs there (mirroring how `canLock` lives there), keep the affordance↔enforcement single-source.
- Tests: `PlanByIdEndpointTest` / the plan-read mapper tests — the manager-sees-MARK_REVIEWED, IC-does-not (leak guard), the real-count parity, the `REVIEWED`/`DRAFT` negatives.

Flag at Step 2.5 if more (e.g. the markable predicate needs a `ReviewService`/`AllowedActionResolver` seam).

## RED test outline (Step 2)
1. **`planRead_directManager_seesMarkReviewedAffordance`** — manager views a direct report's LOCKED plan with a `NOT_REVIEWED` review → `managerReview.allowedActions` contains `MARK_REVIEWED`.
2. **`planRead_icOwner_noMarkReviewedAffordance`** — the IC owner views their own plan → `managerReview.allowedActions` is **empty** (the §35 leak guard). **Load-bearing.**
3. **`planRead_reviewedReview_noMarkReviewedAffordance`** — a `REVIEWED` / `REVIEWED_WITH_DISPUTES` review → no `MARK_REVIEWED` (mirrors the E16 precondition; subset-pin).
4. **`planRead_realUnresolvedDisputeCount`** — a plan with N commitments carrying `OPEN`/`IC_RESPONDED` disputes → `managerReview.unresolvedDisputeCount == N` (not 0), derived from the loaded nested disputes; **parity** with the projection `unresolved_dispute_count`.
5. **`planRead_nonDirectManager_noAffordance_noLeak`** — a manager who is NOT this report's active manager (or an unrelated manager) → empty `allowedActions` (the affordance mirrors the active-relationship scope).
6. **`planRead_draftPlan_nullReview_unchanged`** — a DRAFT plan → `managerReview == null` (the 3.3a behavior unchanged; no regression).

## Cross-doc invariant impact
- **None new** — `ManagerReviewDto` (B.7) fields are unchanged (`allowedActions` + `unresolvedDisputeCount` already exist; this slice POPULATES them). At Step 9: a CLAUDE note that the B.7 `ManagerReviewDto` row's "`allowedActions` empty / count 0" caveat is now **resolved** (the MARK_REVIEWED affordance + real count wired) — orchestrator-writes. No Appendix edit.

## Things to flag at Step 2.5
1. **Where `MARK_REVIEWED` lives** — my default: `ManagerReviewDto.allowedActions` (the review's own affordances, B.7), NOT the plan-level `WeeklyPlanDto.allowedActions` (which is the IC-owner's `LOCK` etc.). Confirm.
2. **The markable precondition** — read `ReviewService.markReviewed` and MIRROR its exact precondition (status set? any dispute/lock condition?). My default: `MARK_REVIEWED` iff `viewerIsDirectManager && status ∈ {the E16-acceptable set}` (likely `NOT_REVIEWED` at least; confirm whether a `REVIEWED_WITH_DISPUTES` can be re-marked). **Subset-pin** the affordance (eligibility ⟹ E16 accepts) per §24/§31 — do NOT reuse the void-throw authorizer; mirror it as a boolean.
3. **`unresolvedDisputeCount` source** — my default: **derive from the loaded `commitmentDtos`' nested disputes** (count non-null `dispute`) — no extra query, reuses §35. The projection value is the parity check, not the source. Confirm vs reading the projection summary (one extra query; couples the plan-read to the projection).
4. **Caller trace (§35)** — enumerate every `PlanMapper.toDto` caller (E3 current, E4 by-id, the lifecycle lock/start/close responses) and confirm none surfaces `MARK_REVIEWED` to an unentitled viewer (the lifecycle responses are owner-only → `viewerIsDirectManager=false` → no affordance; confirm).

## Dependencies + sequencing
- **Depends on:** the §35 `viewerIsDirectManager` threading (5.5b — in `PlanMapper`); the 5.3b nested disputes (loaded per-commitment); the E16 enforcement (3.5/5.2); `AllowedAction.MARK_REVIEWED` (3.3a vocab). All landed.
- **Blocks:** nothing. Closes the last open Phase-6 manager-read carry-forward (`origin: 5.2`).

## Estimated commit count
**1** — two mapper edits + their tests, all reuse. No new query/authz/endpoint/migration. **No security-reviewer** (read-affordance on an already-authorized read; the §35 affordance-leak test is the safety pin).

## Lessons-logged candidates anticipated
- Likely **none new** — this is a textbook §35 application (per-viewer affordance + reuse-loaded-child-for-derived-count). Flag only if the markable-precondition mirroring surfaces a reusable subtlety.

## How to invoke
> **Continue the SAME implementer session** (no `/session-start`). `/tdd markreviewed-plan-read-affordance`.
1. Read this brief + `PlanMapper` (the `viewerIsDirectManager` resolve + the `reviewMapper.toDto(review, 0)` call) + `ReviewMapper` + `ReviewService.markReviewed` (to mirror the precondition) + §35 (the affordance-leak discipline).
2. `/tdd markreviewed-plan-read-affordance`; Step 2.5 — answer the 4 design Qs (esp. Q2 the mirrored precondition + Q3 the count source). Send via `SendMessage`; pause for `APPROVED.`/`TWEAK:`/`ADD:`.
3. Step 9 — surface the CLAUDE B.7-row caveat-resolved note; **no security-reviewer**. `git add` your slice files only.
