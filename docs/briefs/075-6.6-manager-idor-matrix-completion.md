# /tdd brief — manager-read IDOR matrix completion + IC team-heatmap denial (6.6)

## Feature
Complete the §6/§17 IDOR denial coverage for the **Phase-6 manager READ surfaces (E13/E14/E15)** at the HTTP/endpoint layer, with **positive `AUTHORIZATION_DENIED` audit-row assertions** (not just status codes), and pin the **IC team-heatmap denial** (REQ-F-030/UX-005) as a coherent "an IC has no team surface" story across all three manager reads. The service-level `AuthorizationIdorMatrixTest` (task 2.5) already pins the §6 denial set at the authz-**service** layer; this slice closes the **endpoint-level + positive-audit** gaps the Phase-6 reads opened, and **drains the 6.5a security-reviewer's one optional LOW** (the E13 IC-denial audit row not positively asserted).

## Use case + traceability
- **Task ID:** 6.6.
- **Architecture sections:** §6 (the required denial set, line 163 — each `403`/`404` + an `AUTHORIZATION_DENIED` `audit_event`; cross-owner/cross-team/missing → `404`, capability/role → `403`+code), §16/§17 (the invariant→test traceability + the IDOR matrix — **SAFETY-CRITICAL rule #3 / RISK-001/016**), §15 (rule #7 — **no resource text in any audit column**), §5/§9 (E13/E14/E15). **REQ-F-030/UX-005** (an IC has no team surface). REQ-T (per-denial-case coverage).
- **Related context (all exist — verified at HEAD `cce700e`):** `AuthorizationIdorMatrixTest` (service-level, comprehensive — covers all 8 §6 cases via direct `authz.authorize*` calls; **DON'T duplicate it**); the manager-read endpoint tests `ManagerCommandCenterEndpointTest` (E13), `ManagerHeatmapEndpointTest` (E14, has `heatmap_icDenied403` + audit-teardown), `ManagerHeatmapDrilldownEndpointTest` (E15, has `drilldown_notOwnCell_404AndAudit` asserting `auditEvents.count()>0`) — they pin denial STATUS but (per the 6.5a LOW) **not the positive audit row**. The **§38 6.5b addendum** (denial endpoints write audits → audit-teardown + **assert the specific `AUTHORIZATION_DENIED` row, not a bare `count()>0`**) — this slice CONSUMES it. `deny403(…, HEATMAP, …, MANAGER_ROLE_REQUIRED)` / `deny404(…, HEATMAP_CELL, …)` shapes (action `AUTHORIZATION_DENIED`, `REQUIRES_NEW`). `AuditEventRepository`.

## Acceptance criteria
- [ ] **Gap analysis FIRST (Step 1 → reported at Step 2.5):** a table of every §6 denial case × the Phase-6 manager-read endpoints (E13/E14/E15), marking for each whether (a) the status code is pinned, (b) the **positive `AUTHORIZATION_DENIED` audit row** is pinned, (c) no-audit-on-missing is pinned. ADD only the missing positive-audit + endpoint cases — **do not re-test** what `AuthorizationIdorMatrixTest` (service-level) or the existing endpoint tests already pin.
- [ ] **IC team-heatmap denial (REQ-F-030/UX-005), endpoint-level:** an IC (`isManager=false`) → `GET /api/manager/command-center` (E13) and `GET /api/manager/heatmap` (E14) → **`403 MANAGER_ROLE_REQUIRED`**; → `GET /api/manager/heatmap/{cellId}/drilldown` (E15) → **`404`** (the own-cell authorizer; an IC owns no cell). Each writes **exactly one `AUTHORIZATION_DENIED` audit row** — positively assert action + the resource `entityType` (+ reason/code where applicable); **no SENTINEL/resource-text leak** (§15).
- [ ] **6.5a LOW drained:** the E13 IC-denial endpoint test positively asserts the `AUTHORIZATION_DENIED` audit row (not just the `403`).
- [ ] **E15 manager-not-own-cell:** upgrade `drilldown_notOwnCell_404AndAudit`'s `count()>0` to assert the **specific** `AUTHORIZATION_DENIED` + `HEATMAP_CELL` row (per the §38 6.5b addendum) — so accumulation can't make a stale assertion pass.
- [ ] **Missing-resource → no audit** (§25 anti-spam): E15 missing cell → `404` no-audit (exists — keep/strengthen); confirm E13/E14 have no per-resource missing case (they're manager-scoped LIST reads — a non-direct-report's rows simply don't appear, no per-resource `404`).
- [ ] If the gap analysis surfaces an endpoint that **denies but does NOT write the audit** (a real §6 hole) → that's a **production fix** — flag at Step 2.5; **escalate** if it's a safety gap (a denial without the §6-required audit).
- [ ] `./gradlew check` clean. **Ad-hoc security-reviewer** (the IDOR matrix IS the security surface — confirm completeness, the assertions have teeth, no leak, the 403-vs-404 mapping matches §6).

## Files expected to touch
**Modified (tests):**
- `ManagerCommandCenterEndpointTest` (E13 — the IC-denial positive audit assertion; the 6.5a LOW).
- `ManagerHeatmapEndpointTest` (E14 — IC-denial positive audit).
- `ManagerHeatmapDrilldownEndpointTest` (E15 — upgrade the not-own-cell audit assertion to the specific row; IC→`404`+audit).
- Possibly `AuthorizationIdorMatrixTest` (extend ONLY if a service-level manager-read case is missing — likely not).

**New (Step-2.5 decision):** a consolidated HTTP-level `ManagerReadIdorMatrixTest` IF the additions read better consolidated than strengthened in-place.

**Production:** none expected — flag at Step 2.5 if the gap analysis finds a real audit-gap.

## RED test outline (gap-driven; representative)
1. `commandCenter_icDenied_writesAuthorizationDeniedAudit` — IC → E13 → `403 MANAGER_ROLE_REQUIRED` + exactly one `AUTHORIZATION_DENIED` audit (action + `entityType` + no leak). **[the 6.5a LOW]**
2. `heatmap_icDenied_writesAudit` — IC → E14 → `403` + the positive audit row.
3. `drilldown_icDenied_404_writesAudit` — IC → E15 → `404` (owns no cell) + the positive audit row.
4. `drilldown_notOwnCell_writesSpecificDenialAudit` — upgrade: assert the specific `AUTHORIZATION_DENIED` + `HEATMAP_CELL` row (replaces/augments the `count()>0`).
5. (gap-driven) any §6 endpoint case the table flags as unpinned.

## Cross-doc invariant impact
- **None** (test-coverage slice; no DTO/enum/schema change). At Step 9: a CLAUDE note that the §6/§17 manager-read IDOR coverage is **complete** (orchestrator-write); the §16/§17 invariant→test traceability rows land in MVP_TASKS at `/orchestrate-end`.

## Things to flag at Step 2.5
1. **The gap table** (the core Step-1 deliverable) — which §6 × endpoint cases are already pinned vs net-new. This scopes the slice.
2. **Consolidate vs strengthen-in-place** — a new `ManagerReadIdorMatrixTest` (HTTP-level) vs strengthening the per-endpoint tests. My default: **strengthen in-place** (keeps each endpoint's denial coverage co-located, mirrors how the existing endpoint tests carry their own denial cases) + consolidate only if it removes real duplication.
3. **Any real audit-gap** (an endpoint denying without auditing) → production fix; **escalate if it's a safety hole** (a §6-required audit missing in production, not just in tests).
4. **The audit-row assertion shape** — confirm the `entityType` each denial writes (`authorizeTeamHeatmapAccess` → `deny403(…, HEATMAP, …)`; `authorizeHeatmapCellAccess` → `deny404(…, HEATMAP_CELL, …)`) so the positive assertions match production, and reuse the `AuthorizationIdorMatrixTest`'s SENTINEL-no-leak pattern.

## Dependencies + sequencing
- **Depends on:** E13/E14/E15 (6.5a/6.5a-2/6.5b — all landed); `AuthorizationIdorMatrixTest` + the authz service (Phase 2).
- **Blocks:** nothing hard. Runs **before 6.7** (rebuild). Completes the §6 manager-read IDOR coverage — the §16/§17 traceability deliverable.

## Estimated commit count
**1** — a focused test-hardening slice; no new production unless the gap analysis finds an audit-gap. The IDOR matrix IS the security surface → **ad-hoc security-reviewer** (confirm completeness + teeth + 403/404-vs-§6 mapping).

## Lessons-logged candidates anticipated
- Likely **none new** — the **§38 6.5b addendum** already pre-captured the denial-audit-teardown + assert-the-specific-row guidance, which this slice consumes. Flag only if a genuinely new pattern surfaces (e.g. a 403-vs-404 mapping subtlety on the team-heatmap surface).

## How to invoke
> **Continue the SAME implementer session** (no `/session-start`). `/tdd manager-idor-matrix-completion`.
1. Read this brief + **§6** (the denial set, line 163) + the existing `AuthorizationIdorMatrixTest` (service-level — your non-duplication baseline) + the 3 manager-read endpoint tests' current denial coverage + the **§38 6.5b addendum**.
2. **Step 1 = the gap analysis** (the §6 × E13/E14/E15 coverage table) — this scopes the slice.
3. `/tdd manager-idor-matrix-completion`; Step 2.5 — present the **gap table** + the consolidate-vs-in-place call + any audit-gap found. Send via `SendMessage`; pause for `APPROVED.`/`TWEAK:`/`ADD:`.
4. Step 9 — surface the CLAUDE §6/§17-coverage note; expect the ad-hoc security-reviewer. `git add` your slice files only.
