# /tdd brief — ST.6c: manager Drawers (review + drilldown)

## Feature
Swap the two manager "expand-in-place" surfaces to **Flowbite Drawers** (right-slide, raised surface + shadow + scrim), per the Cadence overlay spec: (1) the CommandCenter **review** surface (currently a per-row inline `<tr>` expand) → a review Drawer; (2) the Heatmap **cell drilldown** (currently an inline panel) → a drilldown Drawer. Render-only structural reskin (the functional content — `ManagerRowReview` / the drilldown groups — is unchanged; only the container becomes a Drawer). Third/last ST.6 sub-slice; this is the ST.4-deferred "drawer/modal + scrim" overlay item. `/tdd`.

## Use case + traceability
- **Task ID:** ST.6 (Phase ST — manager-surface visual composition; slice c)
- **Spec source (binding visual):** `docs/design/cadence-design-system/ui_kits/weekly-commit/overlays.jsx` (Drawer: right-slide, raised surface, shadow, scrim, header + close, scrollable body) + `CommandCenter.jsx` (review Drawer) + `Heatmap.jsx` (drilldown Drawer). **Architecture:** `ARCHITECTURE.md §7` (styling-SoT, Flowbite-native). **The Flowbite `Drawer` is already themed** in `apps/wc-web/src/app/flowbiteTheme.ts:~52-56` (`bg-surface-raised shadow-drawer`, z-index) — use it (don't hand-roll an overlay).
- **Related context:** 9.9 (`CommandCenter` per-row review expand via `ManagerRowReview` — lazy `getPlanById` → `MarkReviewedAction`), 9.10 (`HeatmapCellDrilldown` inline panel — SO→commitment groups + pager), ST.6a/6b (`41f1052`/`86a2173` — heatmap volume-fill, command-center dense table). The functional review/drilldown logic is correct + tested; this slice changes only the **container** (inline → Drawer). The Cadence `.wc-*`/`.drawer` CSS is the visual spec only (forbidden #3) — use the themed Flowbite `Drawer`.

## Acceptance criteria
- [ ] **Review Drawer:** in `manager/CommandCenter.tsx`, the per-row review surface opens in a Flowbite `Drawer` (`placement="right"`, `onClose` clears `expandedPlanId`, scrim/backdrop) instead of the inline detail `<tr>`. The Drawer body renders the existing `ManagerRowReview` (lazy `getPlanById` → `MarkReviewedAction`) unchanged; a header identifies the report (name + week). Server-authoritative gating unchanged.
- [ ] **Drilldown Drawer:** in `manager/HeatmapGrid.tsx` + `manager/HeatmapCellDrilldown.tsx`, the cell drilldown opens in a Flowbite `Drawer` (`placement="right"`, `onClose` clears `selectedCellId`, scrim) instead of the inline panel. The Drawer body renders the existing drilldown groups + pager unchanged; a header identifies the cell (report × DO).
- [ ] Both Drawers: open when the trigger is active (`expandedPlanId`/`selectedCellId` set), close via `onClose` + the scrim/close button, and are keyboard-dismissible (Flowbite default — confirm focus/escape at 7.5/ST.7).
- [ ] The existing **view-states** (Loading/Error/Empty) + pagination inside the drilldown/review render inside the Drawer body unchanged; `safeMessage`/IDOR-safe error behavior preserved (no regression).
- [ ] **Render-only — NO enum/Appendix-A change** (no data/contract change; only the container). **No `.wc-*` CSS / no second stylesheet / no hex / no arbitrary `[…]` hatches** (forbidden #3) — the themed Flowbite `Drawer` + token utilities. Both `[data-theme]` values render. `yarn` typecheck + lint + prettier + full Vitest pass.

## Files expected to touch
**Modified:**
- `apps/wc-web/src/features/manager/CommandCenter.tsx` (+ `.test.tsx`) — inline review `<tr>` → review Drawer
- `apps/wc-web/src/features/manager/HeatmapGrid.tsx` + `manager/HeatmapCellDrilldown.tsx` (+ `.test.tsx`) — inline drilldown panel → drilldown Drawer
- `apps/wc-web/src/app/theme/surfaceSkin.test.ts` — add `HeatmapCellDrilldown.tsx` to the guard (CommandCenter/HeatmapGrid already in)

If the Flowbite `Drawer` theme needs a tweak (e.g. width/header slots) in `flowbiteTheme.ts`, **flag at Step 2.5** (that's a themed-primitive change, orchestrator-aware).

## RED test outline (Step 2)
1. **`command_center_review_opens_in_drawer`** (`CommandCenter.test.tsx`, render) — clicking a row's Review trigger renders the review surface inside a `[data-cy="review-drawer"]` (Drawer), not an inline detail `<tr>`; `ManagerRowReview` content present; `onClose` clears it.
2. **`heatmap_drilldown_opens_in_drawer`** (`HeatmapGrid.test.tsx` / `HeatmapCellDrilldown.test.tsx`, render) — selecting a cell renders the drilldown inside a `[data-cy="drilldown-drawer"]` (Drawer), not the inline panel; the SO→commitment groups + pager present; `onClose` clears `selectedCellId`.
3. **`drawer_preserves_view_states_and_gating`** (render) — the existing Loading/Error/Empty + server-gating inside the Drawer body still render (e.g. an error `safeMessage` shows inside the Drawer; mark-reviewed still self-gates) — no regression from the container swap.
4. **`no_wc_star_css_or_hardcoded_token_introduced`** (extend `surfaceSkin.test.ts` with `HeatmapCellDrilldown.tsx`) — no `.wc-*`/hex/arbitrary-`[…]`; the Drawer is the themed Flowbite primitive.

## Cross-doc invariant impact
- **NONE.** Render-only structural reskin; no DTO/enum/contract change — the review/drilldown data + gating are untouched. No Appendix-A/B change.

> **Implementer never edits `apps/wc-web/CLAUDE.md`, `ARCHITECTURE.md`, `MVP_TASKS.md`, or `apps/wc-web/LESSONS.md`** — except `flowbiteTheme.ts` is `apps/wc-web/` code (yours), but flag a Drawer-theme change at Step 9 if it's notable.

## Things to flag at Step 2.5
1. **Drawer test approach.** Flowbite `Drawer` portals + has an open/closed state. **My default vote:** render with the trigger active (drawer open), assert the body content + a `data-cy` on the Drawer; assert `onClose` clears the trigger state. Confirm your testing-library approach (Flowbite Drawer may need the `open` prop controlled + a backdrop query).
2. **Header content.** **My default vote:** review Drawer header = report display-name + week; drilldown Drawer header = report × DO titles (+ optionally the cell's risk badges). Keep it minimal; the body is the existing component. Confirm.
3. **Split review / drilldown?** Two Drawer swaps in one slice. **My default vote:** keep as one ST.6c (same Drawer pattern, same `features/manager/` area). If it feels big (Flowbite Drawer wiring × 2 + view-state preservation), **flag** — I'm fine splitting review-Drawer / drilldown-Drawer into two commits.
4. **Focus/escape/scrim a11y.** Flowbite Drawer gives focus-trap + escape-close by default. **My default vote:** rely on the Flowbite default for 6c; the formal a11y verification (focus-visible, escape, scrim contrast) rides the **ST.7** `/design-review` pass. Confirm.

## Dependencies + sequencing
- **Depends on:** 9.9/9.10 (the review expand + drilldown panel) + ST.6a/6b — landed. The Flowbite Drawer theme (ST.1) — present.
- **Blocks/feeds:** ST.7 (a11y + comprehensive `/design-review` — Drawer focus/escape/scrim verification rides there). Completes ST.6.
- **Not blocked.** (SLA strip deferred — backend `summary`; DisputePanel disputes-blocked. Neither is 6c.)

## Estimated commit count
**1 commit (split to 2 if large) — NOT safety-critical** (render-only structural reskin; the functional review/drilldown is unchanged + already tested). RED→2.5→GREEN per Drawer, one Step-10 commit (or split). Exact overlay fidelity (animation, scrim, focus) → ST.7 `/design-review`.

## Lessons-logged candidates anticipated
- Possibly a small one: "expand-in-place → themed Flowbite Drawer swap preserving the inner view-states/gating" as a reusable manager-overlay pattern (a §-note if the Drawer wiring has a reusable shape). Flag at Step 9.

## How to invoke
1. **Read this brief** + `CommandCenter.tsx` (the review expand, ~line 196-205) + `HeatmapGrid.tsx`/`HeatmapCellDrilldown.tsx` (the drilldown) + `flowbiteTheme.ts` (the Drawer theme) + the Cadence `overlays.jsx` (Drawer spec).
2. **Run `/tdd ST.6c manager drawers`** (orient first if you're a fresh session — `/session-start` + skim this round's ST.6a/6b commits `41f1052`/`86a2173` + briefs 046/047 for the manager-surface context).
3. **Step 0/1** — confirm Feature + Files; the Flowbite Drawer (themed) + token-native approach.
4. **Step 2.5** — send the test write-up + Q1–Q4 (esp. Q1 Drawer test approach, Q3 split). Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
5. **Step 7.5** — reachability: review Drawer on `/manager/command-center`, drilldown Drawer on `/manager/heatmap` (already-wired triggers). Flag gaps; note focus/escape behavior for ST.7.
6. **Step 9** — categorized summary + ship/no-ship + draft commit; confirm cross-doc invariant = NONE + no `.wc-*`/hardcoded-token + any `flowbiteTheme.ts` Drawer-theme tweak.
