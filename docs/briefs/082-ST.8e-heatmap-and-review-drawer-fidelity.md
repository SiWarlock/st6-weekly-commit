# /tdd brief — ST.8e Heatmap + review-drawer canon fidelity (ST.8d canon-compare findings)

## Feature
Close the **heatmap (~8)** + **manager review-drawer (~5)** canon deviations the ST.8d canon-compare surfaced (NOT in doc 024 — found under the "mockup-is-canon + NON-EXHAUSTIVE" directive). The heatmap gains **per-cell volume bars**, a **Row-total column**, **no-coverage 0-cell** styling, the **"RCDO Coverage Heatmap"** title + **Rally-Cry line** + **DO-id column prefixes** + the **legend** + the **manager self-plan footnote row**; the review drawer gains the **header avatar + status pills + lock-timestamp**, the **"COMMITMENTS (N PLANNED)"** caption, and the **derived-status helper**. **Render-only / standalone-demo — NO contract change** (the deterministic bits compute from the existing `HeatmapCellDto` counts; no backend dep). **Lead away-mode decision (2026-06-04, doc 024):** GO, with the carve-out + bound-it below.

## Use case + traceability
- **Task ID:** **ST.8e** (styling-fidelity round — the heatmap+drawer leg, lead-approved away-mode per doc 024 "Decisions resolved"). Render-only.
- **Architecture sections it implements:** none new — renders the existing **Appendix B.12** (`HeatmapResponseDto`/`HeatmapCellDto`) + **B.11/B.7** (the command-center review drawer) contracts. The volume bars + row-total + no-coverage are **client derivations over the existing counts** (demo + production both — no backend dep, unlike D-1's strip which is demo-only).
- **Authoritative mockup reference (CANON):** `docs/design/cadence-design-system/ui_kits/weekly-commit/Heatmap.jsx` (the grid — `volMeta`/`VolBars`/`rowTotalText`/`HeatCell`/the legend/the self-plan row) + the **ReviewDrawer** in `CommandCenter.jsx` (the review drawer — `dhead` avatar+meta-pills+Locked-timestamp, the "Commitments (N planned)" caption, the "Status is derived from the unresolved-dispute count" helper, the `rev-commit` rows). `overlays.jsx`/`kit.css` for the drawer/cell tokens.
- **Related context:** current surfaces = `src/features/manager/HeatmapGrid.tsx` (renders DO-columns + risk badges + the cell drilldown via `HeatmapCellDrilldown`, but MISSING volume bars / row-total / no-coverage styling / rally-cry / DO-id prefixes / legend / self-plan row / the canon title) + the command-center review drawer (`ManagerRowReview`, lazy-mounted in the `CommandCenter` `Drawer`). Token-native — no hex / no `.wc-*` (`surfaceSkin.test.ts` guards `HeatmapGrid.tsx` + `HeatmapCellDrilldown.tsx`; add the new derivation module + the review-drawer surface). **Lands after ST.8d.**

## Acceptance criteria (what "done" means)
**Heatmap:**
- [ ] **Per-cell volume bars** — 3 bars filled to `volBars(commitmentCount)`: `0`→0 bars (the no-coverage cell), `1`→1, `2–3`→2, `4+`→3 (the canon `volMeta` thresholds); decoupled from risk (volume = load, neutral).
- [ ] **No-coverage 0-cell** — `commitmentCount===0` renders the dashed "no coverage" cell (minus-circle + "no coverage"), distinct from a 0-risk populated cell.
- [ ] **Row-total column** — a trailing "Row total" column whose badge text + tone derive from `rowTotal(row cells)`: `draftOnly & total≤1`→"N · not locked" (neutral); `risky===0`→"N clean" (success); else→"N · M at risk" (warning if any cell `count≥4`, else failure).
- [ ] **Title + rally-cry + DO-id prefixes** — h1 "RCDO Coverage Heatmap"; a "Rally Cry: "…"" line (from the RCDO tree); each DO column header prefixes the **DO id** above its title.
- [ ] **Legend** — the risk-badge legend (MISALIGNED/BLOCKED/OVERDUE_REVIEW + NEEDS_REVIEW/CARRY_FORWARD/UNREVIEWED with their meanings) + the volume legend (0/1/2–3/4+ thresholds + the "Volume = load · Risk = badges · no single health score" note).
- [ ] **Manager self-plan footnote row** — the "(you · self plan) — excluded from the team roll-up" row with a view-my-plan link. (See Step-2.5 Q2 — render footnote; bound-it if it needs real manager-plan state.)

**Review drawer (command-center):**
- [ ] **Header** — report avatar + name, the plan-state + review-status **pills**, the lock-timestamp ("Locked …").
- [ ] **"COMMITMENTS (N PLANNED)" caption** + the **"Status is derived from the unresolved-dispute count"** helper near the mark-reviewed action.
- [ ] **CARVE-OUT (lead):** **KEEP the existing inline per-commitment flag UX** (Flag-type + note + Open-dispute) — do NOT rework to the canon `FlagModal`. Document the divergence in the commit/Step-9.

**Both:**
- [ ] **Token-native + REQ-I-008** — the volume-tier backgrounds map to Cadence tokens (no hex; see Step-2.5 Q1); `surfaceSkin.test.ts` + `boundary.test.ts` green; renders existing B.12/B.11/B.7 verbatim (no `dtos.ts`/`ARCHITECTURE.md` change).
- [ ] **BOUND IT (lead):** if any single gap balloons into structural/feature work beyond a reasonable render-fidelity fix, **DEFER it + flag the orchestrator** (no silent expansion).
- [ ] Vitest green (existing + the new deterministic pins); pixel fidelity verified in the gstack browser (the ST.8d harness).
- [ ] `yarn nx lint && typecheck && test wc-web` clean.

## Files expected to touch
**New:**
- `src/features/manager/heatmapDerive.ts` — pure `volBars(count)` + `rowTotal(cells)` (+ the no-coverage predicate), exported for unit tests (mirrors `commandCenterRow.ts`).
- `src/features/manager/heatmapDerive.test.ts`.

**Modified:**
- `src/features/manager/HeatmapGrid.tsx` — title/rally-cry/DO-id-prefixes; the volume bars + no-coverage cell; the Row-total column; the legend; the self-plan footnote row.
- `src/features/manager/ManagerRowReview.tsx` (the review-drawer body) — header avatar+pills+timestamp, the caption, the derived-status helper (KEEP the inline flag UX).
- `src/shared/lib/statusTaxonomy.ts` — **if** a volume-tier tone map is added (Step-2.5 Q1).
- `src/app/theme/surfaceSkin.test.ts` — TOUCHED_SURFACES += the new surfaces if not already covered.
- test files alongside.

If implementation needs files beyond this list, **flag at Step 2.5**.

## RED test outline (Step 2)
1. **`vol_bars_from_count`** — `volBars(0)`→0 (no-coverage), `volBars(1)`→1, `volBars(2)`/`volBars(3)`→2, `volBars(4)`+→3. Why: the canon `volMeta` thresholds; volume decoupled from risk.
2. **`no_coverage_cell_at_zero`** — `commitmentCount===0` renders the dashed no-coverage cell (not a populated 0-risk cell). Why: the canon `HeatCell` 0-branch.
3. **`row_total_tone_and_text`** — `rowTotal` over a fixture row: all-clean→"N clean"/success; with a risky cell→"N · M at risk"/failure; with a `count≥4` cell→warning; draft-only+≤1→"N · not locked"/neutral. Why: the canon `rowTotalText`.
4. **`review_drawer_header_composition`** — the drawer header renders the avatar + name + plan-state pill + review-status pill + lock-timestamp. Why: the canon `dhead`.
5. **`review_drawer_keeps_inline_flag_ux`** — the inline per-commitment Flag control still renders (the carve-out — NOT replaced by a modal). Why: lead carve-out; regression guard.

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field changes:** **NONE** — derivations over existing B.12 counts; renders B.11/B.7 verbatim. No `dtos.ts`/`ARCHITECTURE.md` change.
- **Orchestrator doc rows to write hot (Step 9 routing):** none for cross-doc. Orchestrator stages the ST.8e task row at `/orchestrate-end`.

## Things to flag at Step 2.5
1. **Volume-tier tokens.** The canon backgrounds use `--vol-gap`/`--vol-light`/`--vol-normal`/`--vol-heavy` (+ borders). **My default vote: add a small token-native volume-tier scale to the theme** (4 tiers, mapped to existing neutral/surface tokens — NO hex) consumed by `heatmapDerive`/the cell; keep it out of `RISK_TAXONOMY` (volume ≠ risk — the canon decouples them; cf. LESSONS §25 per-surface maps). Confirm whether existing tokens suffice or a 4-tier scale is warranted.
2. **Manager self-plan footnote row.** **My default vote: render it as a static footnote** ("(you · self plan) — excluded from the team roll-up" + a view-my-plan link) — the demo manager's own plan state can be static/derived; if it needs a real manager-plan fixture (MGR_1 own plan), that's the **BOUND-IT** trigger → flag + defer the dynamic part.
3. **Heatmap drilldown drawer.** The cell-drilldown (`HeatmapCellDrilldown`, 9.10) already exists; the canon `DrilldownDrawer` adds the SO-group breadcrumb + "Showing 1–N of N". **My default vote: fold minor drilldown fidelity in IF trivial; bound-it + flag if it's more than a caption/breadcrumb tweak.**
4. **Row-total = derived, not a DTO field.** `rowTotal` aggregates the row's `HeatmapCellDto`s client-side (no backend field). Confirm this is acceptable (it is — pure derivation; no server roll-up needed).

## Dependencies + sequencing
- **Depends on:** ST.8d (the in-scope polish + the canon-compare baseline) + the landed heatmap (9.10) + review drawer (9.14) surfaces. **Soft-depends on 9.15** (the mutable db makes the drawer's live review state reflect transitions).
- **Blocks:** the round seal (`/orchestrate-end`) — ST.8e is the last styling slice.

## Estimated commit count
**1** (or **2** if Step-2.5 splits heatmap vs review-drawer — they're distinct surfaces; default bundle since both are this round's last fidelity leg + share the canon-compare context). Render-only; no safety invariant.

## Lessons-logged candidates anticipated
- **Convention candidate** — possibly the volume-tier token scale (a §25-adjacent per-surface map) if it generalizes; else none new.
- **Architecture-doc note** — none (render-only).

## How to invoke
> Not the first slice of the session — jump straight to `/tdd`.
1. **Read this brief + the canon `Heatmap.jsx` + the `ReviewDrawer` in `CommandCenter.jsx` + doc 024's "Decisions resolved" (the carve-out + bound-it).**
2. **Run `/tdd st8e-heatmap-and-review-drawer-fidelity`.**
3. **Step 2.5** — send the per-test write-up + answers to the 4 design questions (esp. Q1 volume tokens + Q2 self-plan bound-it).
4. **Step 9** — flag any volume-token convention + any BOUND-IT deferrals + confirm the inline-flag-UX carve-out held.
