# /tdd brief — V6 demo seed: fixture plans + lifecycle state matrix (SOURCE only) (10.3–10.5)

## Feature
The **V6 Flyway demo-seed migration** (`db/demo-seed/V6__seed_fixture_plans_and_state.sql`) — the deterministic **source** fixtures for the demo's lifecycle state matrix: one plan per persona spanning every plan state (DRAFT/LOCKED/RECONCILING/RECONCILED), their commitments (linked + deliberately-unlinked), manager reviews, disputes (OPEN + RESOLVED), the two-week carry-forward chain, and the Outlook sync records (FAILED + SYNCED + MANAGER_REVIEW_BLOCK). **SOURCE rows only** — the `manager_plan_summary`/`manager_heatmap_cell` projections are **NOT hand-seeded** here; they populate on deploy via the existing 6.7 `ProjectionRebuildRunner` (a separate infra task — see Dependencies). Lives in `db/demo-seed/` (Opt-1, same location as V5), loaded by the migrate profile, never by the shared test harness.

## Use case + traceability
- **Task ID:** 10.3 (+ 10.4 sync records + 10.5 carry-forward, bundled — one V6 file). **10.6 (projection literals) is REPLACED** by rebuild-on-deploy (lead-endorsed; see Cross-doc + Dependencies). Wave-1 slice 2 of the deployed-demo phase.
- **Architecture sections:** §3 (plan lifecycle + derived states), §9/§17 (projections + rebuild), §10 (sync records), Appendix E Part 2 (the **fixture STATE MATRIX**, lines 1117–1135 — the binding fixture spec), F.2 (sync pointer), F.3 (review SLA = 17:00 org-tz next business day). MVP_TASKS 10.3–10.6.
- **Related context (all exist):** **V5** (`db/demo-seed/V5__…` — the 7 personas Dana `d0000000-…-01` + Priya/Marco/Aisha/Tomas/Grace/Sam `…-02`…`-07`, + 6 relationships) — V6 plans reference these employee UUIDs. **V4** (`db/migration/V4__seed_rcdo.sql` — the SO UUIDs `c0000000-…-01` SO-1.1 … `…-09` SO-3.3) — commitment links reference these. The lifecycle services (`PlanLifecycleService`, `ReviewService`/`ReviewStatusDeriver`, `DisputeService`, `CommitmentService`) + the entities — V6 must seed source rows **consistent with what those services produce** (so the derived reads match the matrix). The `V5SeedPersonasMigrationTest` shape (isolated Testcontainer, `db/migration`+`db/demo-seed` locations) — mirror it.

## The fixture STATE MATRIX (Appendix E Part 2 — the binding spec; current week `2026-06-01` Mon, anchor "now" = `2026-06-02` morning CT)
| Persona | Plan (week) | State | Review (STORED status) | Disputes | Commitments / notable |
|---|---|---|---|---|---|
| R1 Priya `…02` | 2026-06-01 | **LOCKED** | NOT_REVIEWED, `review_due_at`=2026-06-02 17:00 CT → derived **NOT overdue** | none | 3 planned, all linked: SO-1.1`…01`, SO-2.3`…06`, SO-3.2`…08`; sync `IC_PLANNING` **SYNCED** |
| R2 Marco `…03` | 2026-06-01 | **LOCKED** | NOT_REVIEWED, `review_due_at`=2026-05-29 17:00 CT → derived **OVERDUE** | none | sync `IC_PLANNING` **FAILED** (`GRAPH_FORBIDDEN`, safe_message, retry_count=1, graph_event_id NULL) |
| R3 Aisha `…04` | 2026-06-01 | **LOCKED** | REVIEWED → derived **REVIEWED_WITH_DISPUTES** (SLA satisfied) | **1 OPEN** `flag_type=MISALIGNED`, manager_note set, ic_response NULL, on a `alignment_status=NEEDS_REVIEW` commitment | linked planned commitments |
| R4 Tomas `…05` | 2026-06-01 | **LOCKED** | REVIEWED → derived **REVIEWED** | **1 RESOLVED** (`OPEN→IC_RESPONDED→RESOLVED`, resolved_at + ic_response set) | linked planned commitments |
| R5 Grace `…06` | 2026-06-01 | **RECONCILING** | REVIEWED | none | outcomes partly recorded (COMPLETED/PARTIALLY_COMPLETED/BLOCKED); 1 **UNPLANNED** (`work_type=UNPLANNED`, linked SO-2.2`…05`); carry-forward target (below) |
| R6 Sam `…07` | 2026-06-01 | **DRAFT** | (none — not locked) | none | 2 planned, **1 deliberately UNLINKED** (`supporting_outcome_id` NULL), 1 linked |
| Dana `…01` (self) | prior wk (her own) | **RECONCILED** | n/a (no manager over Dana) | none | fully reconciled, all outcomes set |

**Carry-forward chain (R5, REQ-D-006/E-005):** prior-week `2026-05-25` R5 plan **RECONCILED**, contains `C_src` (*"Draft the activation-onboarding runbook"*, linked SO-1.2`…02`, `reconciliation_outcome='CARRIED_FORWARD'`). Current-week RECONCILING R5 plan contains `C_next` with `carry_forward_source_commitment_id = C_src.id`, `commitment_kind='PLANNED'`, linked SO-1.2. Prior-week locked baseline unchanged by C_next.
**Sync records (REQ-E-004):** R2 FAILED (above) + R1 SYNCED (success contrast) + Dana `MANAGER_REVIEW_BLOCK` keyed `(owner=Dana, week_start_date='2026-06-01')` via the partial-unique, `status='SYNCED'`.

## Acceptance criteria
- [ ] `db/demo-seed/V6__seed_fixture_plans_and_state.sql` — ordered after V5; fixed literal UUIDs; fixed timestamps anchored to the week grid above; idempotent `INSERT … ON CONFLICT (<key>) DO NOTHING`; parent-first insert order (plan → commitment → review → dispute → sync).
- [ ] **STORED columns produce the matrix's DERIVED reads.** Store `manager_review.status` ∈ {NOT_REVIEWED, REVIEWED} + `review_due_at` such that the **read-time derivation** (`ReviewStatusDeriver`, injectable `Clock` @ 2026-06-02 morning) yields: R2 OVERDUE, R1 NOT-overdue, R3 REVIEWED_WITH_DISPUTES (REVIEWED + open dispute), R4 REVIEWED. **Never store OVERDUE / REVIEWED_WITH_DISPUTES** (rule #6 — derived).
- [ ] Disputes: R3 one OPEN (MISALIGNED) on a NEEDS_REVIEW commitment; R4 one RESOLVED (full `OPEN→IC_RESPONDED→RESOLVED` lifecycle, `resolved_at`+`ic_response` populated). At most one unresolved dispute per commitment (rule #6 partial-unique holds).
- [ ] Carry-forward: `C_next.carry_forward_source_commitment_id == C_src.id`; both link SO-1.2; prior-week R5 RECONCILED, current-week RECONCILING; `C_src.reconciliation_outcome='CARRIED_FORWARD'` (single-outcome).
- [ ] Sync records: R2 exactly one FAILED IC_PLANNING (safe_message, NULL graph_event_id); R1 one SYNCED; Dana one MANAGER_REVIEW_BLOCK (partial-unique grain holds).
- [ ] R6 DRAFT with one UNLINKED planned commitment (NULL SO) — the "can't lock unlinked" demo fixture. R1 has 3 linked planned commitments.
- [ ] **NO projection rows seeded** (`manager_plan_summary`/`manager_heatmap_cell` left empty by V6 — populated by the rebuild Job on deploy; see Dependencies). Do NOT hand-write projection literals.
- [ ] Idempotent — re-applying V6 inserts 0 rows.
- [ ] V6 migration test (isolated Testcontainer, `db/migration`+`db/demo-seed`, mirror `V5SeedPersonasMigrationTest`) — all the above pinned. `./gradlew check` clean. **No security-reviewer** (seed; no auth-logic change).

## Files expected to touch
**New:** `apps/wc-api/shared/src/main/resources/db/demo-seed/V6__seed_fixture_plans_and_state.sql`; a V6 migration test `apps/wc-api/shared/src/test/java/com/st6/wc/migration/V6FixtureStateMigrationTest.java`.
If the V6 file grows unwieldy, **flag at Step 2.5** the V6-vs-V6+V7 split (see Q1).

## RED test outline (Step 2)
1. **`v6_seedsPlanPerPersonaInMatrixState`** — 7 plans (R1-R6 current-week + Dana prior-week) in the matrix's states; counts/links per the table.
2. **`v6_derivedReviewStatesMatchMatrix`** — with `Clock` @ 2026-06-02 morning: R2 OVERDUE, R1 not-overdue, R3 REVIEWED_WITH_DISPUTES, R4 REVIEWED (the read-time derivation, NOT stored).
3. **`v6_disputesOpenAndResolved`** — R3 one OPEN MISALIGNED (ic_response NULL), R4 one RESOLVED (resolved_at + ic_response set); the unresolved-dispute partial-unique holds.
4. **`v6_carryForwardChainLinksToSource`** — `C_next.source == C_src.id`, both SO-1.2, prior RECONCILED / current RECONCILING, `C_src` CARRIED_FORWARD.
5. **`v6_syncRecordsFailedSyncedAndReviewBlock`** — R2 FAILED (safe_message, NULL graph_event_id), R1 SYNCED, Dana MANAGER_REVIEW_BLOCK (grain unique).
6. **`v6_r6DraftHasUnlinkedCommitment`** — R6 DRAFT, 1 planned commitment with NULL SO.
7. **`v6_idempotentReRun`** — re-apply → 0 new rows.
8. **`v6_noProjectionRowsSeeded`** — `manager_plan_summary`/`manager_heatmap_cell` empty after V6 (population is the rebuild Job's job).

## Cross-doc invariant impact (orchestrator writes hot at Step 9)
- **Model field changes:** none (all entities pre-exist).
- **Architecture edits (orchestrator, Step 9):** **Appendix E Part 2 line 1135** currently says projections are "seeded as literal values … the rebuild job must reproduce them identically" — **EDIT** to: projections are NOT hand-seeded; they populate on deploy via the `ProjectionRebuildRunner` (truncate+recompute-from-source, §9/§17/LESSONS §40), which is the population mechanism (the source fixtures ARE the truth). **MVP_TASKS 10.6** task reframed from "hand-seed projection literals" → "populate via rebuild Job on deploy" (orchestrator rewrites + ticks 10.3/10.4/10.5). The `rebuild==seed` carry-forward stays deferred but its framing shifts (rebuild IS the seed-population, so the literal-equality test is moot — note in Carry-forward).
- Same `db/migration`→`db/demo-seed` location correction as V5 (already being written for V5).

## Things to flag at Step 2.5
1. **One V6 file vs V6+V7 split.** My default vote: **one V6 file** (the fixture matrix is one logical unit; Flyway versions are immutable once applied, so a single complete file is cleanest). Split sync+carry-forward into V7 ONLY if the single file becomes unreviewable — your call as author.
2. **UUID-prefix scheme for the new entity types** (plan/commitment/review/dispute/sync). My default: continue V4/V5's per-entity-type hex-prefix convention (e.g. plan=`f…`, commitment=`10…`/`11…`, review=`12…`, dispute=`13…`, sync=`14…`) with sequential suffixes in logical order. Pick a scheme that's readable + collision-free; confirm at 2.5.
3. **Stored review status for R3/R4.** REVIEWED_WITH_DISPUTES + OVERDUE are DERIVED — store `status=REVIEWED` for R3/R4/R5 and `NOT_REVIEWED` for R1/R2, with `review_due_at` set so the deriver yields the matrix. Confirm the deriver's exact predicate (you built `ReviewStatusDeriver`) so the stored source is right. My default: store the minimal real status + let derivation do the rest.
4. **R5 reconciliation outcomes detail** — the matrix says "partly recorded (COMPLETED/PARTIALLY_COMPLETED/BLOCKED)" + 1 UNPLANNED (SO-2.2). Pick a concrete distribution across R5's commitments that produces a BLOCKED badge (for the heatmap) without over-specifying. Confirm at 2.5.

## Dependencies + sequencing
- **Depends on:** V5 (personas + relationships — the plan owners); V4 (SO link targets); all lifecycle entities/services (built).
- **Blocks / pairs with:** **NEW infra task 12.14 — rebuild-projections Job + deploy.yml step** (a `job-rebuild-projections.yaml` reusing the wc-api image with `--app.job=rebuild-projections`, run after the migration Job; reuses the 6.7 runner). The demo's command-center/heatmap show data ONLY after that Job runs against the V6-seeded source. I'm coordinating 12.14 with the infra impl separately — **V6 does not block on it** (V6 lands its source; the projection population is the infra wiring). The deferred `rebuild==seed` literal test is discharged-by-reframe (the rebuild IS the population).
- **Blocks:** the deployed demo's manager surface; the test-track 11.9 persona-UUID reconciliation.

## Estimated commit count
**1** (or 2 if you split V6/V7 at Q1). A large seed + its test. No auth-logic change → no security-reviewer. NOT safety-critical (it seeds source the services already enforce; no new enforcement path).

## Lessons-logged candidates anticipated
- **Architecture-doc note** — the rebuild-on-deploy projection-population decision (vs hand-seeded literals) — orchestrator writes the Appendix-E/§9 edit hot.
- **Convention candidate** — seeding source fixtures whose *derived* reads (OVERDUE/REVIEWED_WITH_DISPUTES) must be produced by read-time derivation under a pinned `Clock`, never stored (extends the V5 seed pattern). Flag if reusable.

## How to invoke
> Implementer session already oriented (V5 just shipped). Jump straight in.
1. Read this brief + Appendix E Part 2 (1117–1135) + the V5 SQL (for UUID/anchor conventions) + `ReviewStatusDeriver`/`DisputeService` (so the stored source yields the derived states).
2. `/tdd v6-fixture-plans-and-state`; Step 2.5 — answer Q1–Q4. Send via `SendMessage`; pause for `APPROVED.`/`TWEAK:`/`ADD:`.
3. Step 9 — surface the Appendix-E/§9/10.6 rebuild-on-deploy reframe (orchestrator writes). `git add` only your V6 SQL + test.
