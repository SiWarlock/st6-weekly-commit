# /tdd brief — Flyway V3: synchronous projection tables

## Feature
Author `V3__projection_tables.sql` — the 2 manager read-model projection tables (`manager_plan_summary`, `manager_heatmap_cell`) with their count columns, the `risk_badges text[]` enumerated vocabulary, unique constraints, indexes, and FKs — proven on Testcontainers PG16 (reusing the 1.2/1.3 harness).

## Use case + traceability
- **Task ID:** 1.4 (Phase 1). Last of the Flyway migration trio (V1 core → V2 partial uniques → V3 projections).
- **Architecture sections:** §9 (manager projections / synchronous read models — table shapes only), Appendix A (ManagerPlanSummary, ManagerHeatmapCell rows). Appendix B.1 (`RiskBadge` 6-value vocabulary).
- **Requirements:** REQ-D-001, REQ-D-009 (UUID PKs), REQ-D-010 (`risk_badges` vocabulary).
- **Related context:** 1.3 (`a2230c5`) is in; reuse the Flyway+Testcontainers PG16 harness (LESSONS §5, `testcontainers-bom:1.21.4`). These are **synchronous projection** tables (§9) — written transactionally by the `ProjectionService` (a much later phase), not by triggers.

## ⚠ Cross-doc invariant slice (NEW — 2 projection models) + a safety-rule clarification
This adds the 2 Appendix-A projection models. Cross-doc invariant NEW (you flag at Step 9; I write the rows). **Pre-empt a false safety flag:** `manager_plan_summary.is_review_overdue boolean` is the **§9 projection read-model column** (Appendix A lists it explicitly) — it is **NOT** a violation of safety rule #6 ("OVERDUE is derived, never stored"). Safety rule #6 governs `manager_review.status` (which correctly has **no** `OVERDUE` enum value) and the **read-time** overdue derivation in the service/command-center layer. The projection's `is_review_overdue` is a denormalized read-model field whose population/recompute/staleness semantics belong to the projection-service + command-center phases (much later). **For this DDL slice it is just a column (`default false`) — no behavior, no derivation logic.**

## Scope boundary
- ✅ `V3__projection_tables.sql` (the 2 projection tables: columns, `risk_badges text[]` vocabulary enforcement, uniques, indexes, FKs) + its Testcontainers test (reuse the harness).
- ❌ No projection-population logic / `ProjectionService` (later phase) — DDL only, no behavior. No JPA entities (1.5). No seed. No `:api` Flyway autoconfig.

## The 2 projection tables (per §9 / Appendix A)
1. **`manager_plan_summary`** — one row per manager/report/week: `manager_employee_id`, `employee_id`, `weekly_plan_id`, `week_start_date`, `plan_state`, `review_status` (nullable), `review_due_at`, `is_review_overdue boolean default false`, the **7 count columns** (planned/unplanned/misaligned/needs_review/blocked/carry_forward/unresolved_dispute), `updated_at`; `unique(manager_employee_id, employee_id, week_start_date)`.
2. **`manager_heatmap_cell`** — manager × report × week × Defining Objective: `manager_employee_id`, `employee_id`, `week_start_date`, `defining_objective_id`, `commitment_count` + the same 7 count columns, `risk_badges text[] not null default '{}'` (elements constrained to the 6-badge `RiskBadge` vocabulary), `updated_at`; `unique(manager_employee_id, employee_id, week_start_date, defining_objective_id)`.

## Acceptance criteria
- [ ] `V3__projection_tables.sql` migrates clean **after V1/V2** on Testcontainers PG16; both projection tables exist with all count columns; `risk_badges` is a Postgres `text[]`.
- [ ] `risk_badges` enforces the 6-badge vocabulary (see Q1); a valid array (`{MISALIGNED,OVERDUE_REVIEW}`) round-trips; empty defaults to `'{}'`; an out-of-vocab element is rejected.
- [ ] Both unique constraints fire on duplicate keys; FKs to employee/weekly_plan/defining_objective wired; the §9/DATA_MODEL supporting indexes present.

## RED test outline (Step 2) — reuse the harness
1. **`v3_migrates_clean_after_v1_v2`** — V1→V2→V3 apply clean; both projection tables present with all count columns; `risk_badges` column type is `text[]` (ARRAY).
2. **`risk_badges_vocabulary`** — a valid `{MISALIGNED,OVERDUE_REVIEW}` round-trips; empty defaults `'{}'`; an out-of-vocab element (e.g. `{NOT_A_BADGE}`) is **rejected** (CHECK violation 23514, per Q1).
3. **`summary_unique`** — duplicate `(manager_employee_id, employee_id, week_start_date)` → 23505.
4. **`heatmap_unique`** — duplicate `(manager_employee_id, employee_id, week_start_date, defining_objective_id)` → 23505.
5. **`projection_fks`** (integration) — a row with a non-existent `defining_objective_id` (heatmap) / `weekly_plan_id` (summary) → 23503.

## Cross-doc invariant impact (I write the docs at Step 9)
- **Model field changes:** NEW — `ManagerPlanSummary` + `ManagerHeatmapCell` projection models.
- **Orchestrator doc rows:** add a projections cross-doc row in `apps/wc-api/CLAUDE.md` (or extend the core-schema row) for the 2 projection models + the `risk_badges` vocabulary; confirm §9/Appendix A ↔ V3. No `ARCHITECTURE.md` field edits (Appendix A/§9 specify these).

## Things to flag at Step 2.5
1. **`risk_badges` vocabulary enforcement — DB CHECK vs entity converter.** My default vote: **DB-level CHECK via the array-containment operator** — `CHECK (risk_badges <@ ARRAY['MISALIGNED','NEEDS_REVIEW','BLOCKED','CARRY_FORWARD','UNREVIEWED','OVERDUE_REVIEW']::text[])`. DB-enforced (consistent with the VARCHAR+CHECK posture), testable by inserting an out-of-vocab element, and extends the enum↔CHECK discipline to the array. The task's "or asserted at the entity converter boundary" fallback is the alternative if a DB array CHECK proves awkward — flag if so, but DB CHECK is preferred.
2. **`is_review_overdue` — DDL only, no behavior (confirm not a safety flag).** Per the clarification above: it's the §9 projection column (`boolean default false`), not a safety-rule-#6 issue. My default: **create the column, no logic**; don't flag it as a safety contradiction.
3. **Test class.** New `V3ProjectionTablesTest` reusing the harness (V1→V2→V3 migrate). My default: **new class** (keeps V1/V2/V3 test concerns separate + bisectable), same `com.st6.wc.migration` package (SpotBugs exclusion covers it).
4. **`risk_badges` enum↔CHECK pin.** If you take the DB CHECK (Q1), consider a test asserting the CHECK's allowed set equals `RiskBadge.values()` (the array-vocabulary analogue of the V1 `enum↔CHECK` test) so `RiskBadge` and the array CHECK can't drift. My default: **include it** (cheap, consistent with LESSONS §3/§5).

## Dependencies + sequencing
- **Depends on:** 1.2/1.3 (V1+V2 + the harness); FKs reference `employee`/`weekly_plan`/`defining_objective` (V1 tables).
- **Blocks:** 1.5 (JPA entities — `ManagerPlanSummary`/`ManagerHeatmapCell` map these), and the manager-projection + command-center phases (§9) that populate + read them.

## Estimated commit count
**1 — standalone.** Cross-doc invariant (2 NEW projection models). Completes the V1/V2/V3 migration trio.

## Lessons-logged candidates anticipated
- **Convention candidate** — the `text[]` enumerated-vocabulary pattern (`<@` containment CHECK + the array-vocabulary↔enum pin) — folds into LESSONS §5.
- **Cross-doc invariant** — the 2 projection models (→ `apps/wc-api/CLAUDE.md`; I write it).

## How to invoke
> Session already oriented — jump to `/tdd`.
1. **Read end-to-end** — note the `is_review_overdue` clarification (not a safety flag) + the 4 Step-2.5 questions.
2. **Run `/tdd flyway-v3-projection-tables`.**
3. **Step 2.5** — send per-test designs + Q1–Q4 (the `risk_badges` enforcement Q1 wants a confirm). Do not go GREEN until I sign off.
4. **Step 9** — flag the 2 projection models (cross-doc NEW); ship/no-ship + draft commit + done-with-slice hash.

> **Context note:** you were ~66% at the last ping (~1 slice to ACTION). Run 1.4 through Step 10 normally — slice atomicity means you finish it. This is likely the last slice before a `/session-end` close-out cycle; if a close-out instruction arrives mid-slice, ack it but finish 1.4's Step-10 commit first, then become interruptible. I'll relay any close-out the moment it arrives.