# /tdd brief — 4.6 baseline-immutability proof under reconciliation (REQ-E-005) — the Phase-4 acceptance closer

> **Final Phase-4 slice — the acceptance proof, not a new feature.** The locked-baseline-immutability *behavior* already exists (3.4b/3.5 gates + 4.4/4.5 confine writes); 4.6 is the **deterministic integration proof** that a FULL reconciliation pass (lock → start → add-unplanned → record-outcomes → carry-forward → close) leaves every locked PLANNED baseline field **byte-identical** (REQ-E-005). Acceptance-test-first: if it passes on first write, REQ-E-005 is proven; if it goes RED, there's a baseline-mutation bug to fix. **No new production code, no security-reviewer** (a test over existing invariants). For the fresh backend impl.

## Feature
`BaselineImmutabilityIntegrationTest` (Testcontainers PG16, deterministic) drives the full reconciliation chain through the real production HTTP/service path and asserts the locked planned baseline is unchanged end-to-end (REQ-E-005), the carry-forward successor lives in a distinct next-week plan, and the chain reaches `RECONCILED`.

## Use case + traceability
- **Task ID:** 4.6 (Phase-4 acceptance)
- **Architecture sections:** `ARCHITECTURE.md §3` (locked baseline immutable; the lifecycle), §5 (E8/E9/E6/E11/E12/E10), **Appendix E Part 2** (the R5 Grace two-week carry-forward chain fixture — the canonical scenario), REQ-E-005 / REQ-D-006 / REQ-F-028. **Requirements: REQ-E-005, REQ-D-006, REQ-F-028.**
- **Related:** session doc [012](../sessions/012-2026-06-03-backend-phase4-functional-surface.md) (the 4.4/4.4b/4.5 decisions this proves); LESSONS §28/§30 (lock + carry-forward patterns); §27-ext (the (state×kind) allow-list whose freezes this proves end-to-end).

## SCOPE RECONCILIATION — backend integration test ONLY (read first)
The MVP_TASKS 4.6 line lists a Cypress feature (`ic-reconcile-carry-forward.feature`) + `reconcile.steps.ts`. **Those already exist** — authored by the **test track at 11.7** (`apps/wc-e2e/cypress/features/ic-reconcile-carry-forward.feature`, authored-not-green, gated on a deployable app). They are **test-track territory + the test track is PAUSED**. So 4.6's Cypress part is **satisfied-by-11.7** — **do NOT re-author or edit any `apps/wc-e2e/*` file** (cross-track). If you judge the existing feature inadequate for the REQ-E-005 baseline-unchanged assertion, **flag it at Step 9** (I route it to the test track) — don't fix it here. **4.6 backend = the integration test only.**

## Acceptance criteria (what "done" means)
- [ ] `BaselineImmutabilityIntegrationTest` (Testcontainers PG16) drives, through the real service/endpoint path: seed an IC plan with ≥2 PLANNED commitments (≥1 linked SO) → **lock** (E8) → **capture every PLANNED commitment's baseline fields** (`title, description, supporting_outcome_id, priority, work_type, confidence, commitment_kind`) immediately post-lock → **start-reconciliation** (E9) → **add an UNPLANNED commitment** (E11) → **record outcomes** on planned + unplanned (E6) → **link the unplanned's SO** (E6, RECONCILING+UNPLANNED) → **carry-forward** one PLANNED commitment (E12) → **close** (E10).
- [ ] **REQ-E-005:** after the full pass, every PLANNED commitment's captured baseline fields are **byte-identical** to the post-lock snapshot (no rewrite by unplanned-add, outcome-record, carry-forward, or close).
- [ ] **Carry-forward isolation (REQ-D-006):** the carried-forward source row has ONLY `reconciliation_outcome=CARRIED_FORWARD` changed (no baseline field); the successor is in a **distinct next-week plan** with `carry_forward_source_commitment_id=source.id`; the prior-week locked baseline is untouched (the R5 two-week shape).
- [ ] **Chain completes:** the plan reaches `RECONCILED` (`reconciled_at` set); a mid-pass attempt to mutate a PLANNED baseline field → `409 LOCKED_BASELINE_EDIT` (the gate firing on the real locked state, end-to-end).
- [ ] `./gradlew check` green; the test is deterministic (injected `Clock`, seeded UUIDs, no flakiness).

## Files expected to touch
**New:** `apps/wc-api/api/src/test/java/com/st6/wc/reconciliation/BaselineImmutabilityIntegrationTest.java` (or co-locate under `commitment/`/`plan/` — your call; a `reconciliation/` package matches the architecture's framing).
**Reuse (no production change):** the existing `@SpringBootTest` base (`support/AbstractAppBootTest`, the Hikari cap §29), the demo-auth chain (2.6), the seeding helpers the slice endpoint tests already use, and the real E8/E9/E6/E11/E12/E10 endpoints. **No production files** — if you find yourself editing production code, a baseline-immutability bug surfaced → STOP and flag it (that's a finding, not a 4.6 scope item). **No `apps/wc-e2e/*` edits** (scope reconciliation above).

## RED/proof test outline (Step 2)
This is acceptance-test-first — the production behavior exists (4.1–4.5), so expect GREEN on a correct implementation; a RED means a real REQ-E-005 violation to escalate.
1. **`fullReconciliationPass_leavesPlannedBaselineByteIdentical`** — the full chain above; capture post-lock baseline (a per-commitment snapshot/record), assert field-by-field equality after close (REQ-E-005).
2. **`carryForward_writesOnlySourceOutcomeAndSuccessorLink`** — the source row diff = `reconciliation_outcome` only; successor in the next-week plan; prior-week baseline untouched (REQ-D-006, the R5 two-week shape).
3. **`plannedBaselineEdit_midReconciliation_rejected`** — a PATCH to a PLANNED baseline field at RECONCILING → `409 LOCKED_BASELINE_EDIT` (the gate end-to-end on the real locked state).
4. **`chainReachesReconciled`** — after all outcomes + the unplanned SO-link, close → `RECONCILED` + `reconciled_at`.

## Cross-doc invariant impact
- **None** — 4.6 is an assertion over existing WeeklyPlan/WeeklyCommitment invariants. No new field, no DTO, no code, no cross-doc row. (The scope reconciliation — Cypress satisfied-by-11.7 — I note in the Log at `/orchestrate-end`.)

## Things to flag at Step 2.5
1. **Scope reconciliation — confirm backend-only.** The Cypress feature is satisfied-by-11.7 (test-track, paused, authored-not-green). Confirm you'll write ONLY the backend integration test + NOT touch `apps/wc-e2e/*`. (If you think the existing `.feature` misses the REQ-E-005 baseline assertion, flag at Step 9 → I route to the test track.)
2. **Baseline-capture mechanism.** Default: capture each PLANNED commitment's baseline fields into a per-id record/map immediately post-lock, assert field-by-field equality after close (the strongest, most-readable byte-identity proof). Alternative: a row-hash. My vote: field-by-field record (clearer failure messages). Confirm.
3. **One integration test class, full chain in one test vs split.** Default: one `BaselineImmutabilityIntegrationTest` with the 4 scenarios above (the full-pass + the carry-forward-isolation + the mid-pass-409 + the reaches-RECONCILED). Confirm — or split if a single test gets unwieldy.
4. **Seeding the chain.** Reuse the slice endpoint tests' seeding pattern (direct repo seed of the plan + commitments, then drive the real endpoints). Confirm there's no need for the V5/V6 demo seed (that's 3.1b, separate) — this test seeds its own deterministic fixture.

## Dependencies + sequencing
- **Depends on:** 4.1–4.5 (the full reconciliation chain) — ALL landed (`a35961a`/`5af8356`/`c48c6fc`/`f0f415a`/`e41aa6e`). 3.5 (lock). The §29 Hikari cap (a new `@SpringBootTest` context).
- **Blocks:** Phase-4 acceptance sign-off (the `### Acceptance criteria (4)` block). Closes Phase 4.

## Estimated commit count
**1 — standalone.** A pure acceptance/integration test; **no production code, no security-reviewer.** Closes Phase 4. (If it surfaces a REQ-E-005 violation, the FIX is a separate finding-driven slice — don't bundle a fix into the proof.)

## Lessons-logged candidates anticipated
- Likely **none new** — it exercises the §28/§30/§27-ext patterns end-to-end. A reusable "full-lifecycle-chain integration harness" shape *might* be worth a note if the seeding/driving boilerplate is substantial. Flag at Step 9 if so.

## How to invoke
1. Read this brief — esp. the SCOPE RECONCILIATION (backend-only; no `apps/wc-e2e/*`).
2. Run `/tdd 4.6-baseline-immutability-proof`.
3. Pause at Step 2.5; send the test design + answers to the 4 questions. I reply `APPROVED.`/`TWEAK:`/`ADD:`.
4. If any scenario goes RED → that's a REQ-E-005 finding; STOP + flag (don't silently fix in this slice).
