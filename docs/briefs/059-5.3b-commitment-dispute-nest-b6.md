# /tdd brief — 5.3b `WeeklyCommitmentDto.dispute?` nest (B.6 Option-A read-path edit) — the frontend 9.11a unblock

> **READ-PATH slice — NO security-reviewer.** This is the **B.6 Option-A contract realization** (user-approved, `docs/planning/023` §6): nest the commitment's **unresolved** dispute (`OPEN`/`IC_RESPONDED`) into `WeeklyCommitmentDto` so the existing plan+commitments read (E3/E4) hands the frontend the dispute `id` + `managerNote` + `status` + `flagType` + `icResponse` needed to wire respond (E18) / resolve (E19) / display. **It is the FRONTEND 9.11a UNBLOCK** — when this lands, the backend orch claims the ARCHITECTURE B.6 window + pings the frontend orch to mirror `dtos.ts`. The dispute **inherits the commitment's authz scoping** (rides the already-authorized E3/E4 read) — no new IDOR surface, no new endpoint, no extra round-trip → no security pass needed. Consumes the **existing** `AlignmentDisputeDto` + `DisputeMapper` + `AlignmentDisputeRepository` (all shipped at 5.3, `8e2c91f`). For the standing impl (`b7849b0e`). **Do NOT bundle anything else** — this is a clean cross-doc contract edit; traceability wants it atomic.

## Feature
Nest `dispute?: AlignmentDisputeDto` in `WeeklyCommitmentDto` (B.6): the commitment's current `OPEN`/`IC_RESPONDED` dispute mapped via the 5.3 `DisputeMapper`, else `null`. Realizes the user-approved **Option A** and **drops** the never-implemented `hasUnresolvedDispute` from the contract (the DTO already omits it — the transitional subset becomes the final shape).

## Use case + traceability
- **Task ID:** 5.3b
- **Architecture sections it implements:** `ARCHITECTURE.md` Appendix **B.6** (`WeeklyCommitmentDto` — drop `hasUnresolvedDispute`, add `dispute`), **B.8** (`AlignmentDisputeDto` — the nested shape, pre-exists + shipped at 5.3), §3 (dispute lifecycle `OPEN→IC_RESPONDED→RESOLVED`; the unresolved bucket = `{OPEN, IC_RESPONDED}`), §5 (E3/E4 plan read = the carrier path).
- **Decision of record:** `docs/planning/023-9.11-disputes-contract-gap.md` §6 — **Option A APPROVED** (user, via lead, 2026-06-03). Exact decision: *nest the `OPEN`/`IC_RESPONDED` dispute if present, else `null`; DROP the redundant `hasUnresolvedDispute` boolean (the nested object's presence + status is the single source of truth).* The dispute is an entity **inside the commitment aggregate** (≤1 unresolved per commitment, rule #6; no independent existence) → expose it through the aggregate root.
- **Related context:** brief `058` (5.3 — created `AlignmentDisputeDto` + `DisputeMapper` + the repo finder this slice consumes); LESSONS **§24** (the DTO may carry a documented transitional subset for a pending contract change — now being discharged), **§22** (nested read-DTO pattern + the no-fetch-join / N+1 posture — see Step-2.5 Q2). **This slice UNBLOCKS frontend 9.11a (disputes UI) + ST.4 disputed→failure left-accent + ST.6 DisputePanel.**

## REUSE — don't re-create (verified against HEAD `255c99b`)
- **`AlignmentDisputeDto` (`api/dispute/dto/AlignmentDisputeDto.java`)** — EXISTS (5.3). `record(id, commitmentId, managerEmployeeId, status, flagType, managerNote, icResponse?, resolvedAt?, allowedActions[], version)`. Mirrors B.8 verbatim; `allowedActions` empty until 5.4/5.5. **This is the nested type — import it into `WeeklyCommitmentDto`.**
- **`DisputeMapper.toDto(AlignmentDispute)` (`api/dispute/mapper/DisputeMapper.java`)** — EXISTS (5.3, `@Component`). Maps the entity → `AlignmentDisputeDto`. **Inject + reuse — do NOT re-map inline.**
- **`AlignmentDisputeRepository.findByCommitmentIdAndStatusIn(commitmentId, statuses) → Optional<AlignmentDispute>` (`shared/dispute/repo/`)** — EXISTS (Phase 1). Returns ≤1 row by the V2 partial unique (rule #6). Pass the unresolved bucket `{OPEN, IC_RESPONDED}` → the single current dispute, empty when none. **This is the resolution finder.**
- **`CommitmentMapper` (`api/commitment/mapper/CommitmentMapper.java`)** — the single commitment→DTO site. It already resolves the RC→DO→SO breadcrumb per-commitment via `RcdoReadService` and fills `allowedActions` via `AllowedActionResolver`. The **single private core `toDto(c, allowedActions)`** is the one place to add the dispute lookup so BOTH the context-free path (E5/E6/E7/E11/E12 write responses) and the context-aware plan-read path (E3/E4 via `PlanMapper`) carry the field.
- **The unresolved-bucket set `{OPEN, IC_RESPONDED}`** — already used by the 5.2 `ReviewStatusDeriver` + the 5.3 open-dispute pre-check. **Reuse a shared constant if one exists** (DRY — check `ReviewStatusDeriver` / the dispute service); else inline `List.of(DisputeStatus.OPEN, DisputeStatus.IC_RESPONDED)` consistent with them.

## Acceptance criteria (what "done" means)
- [ ] `WeeklyCommitmentDto` gains a `dispute` field of type `AlignmentDisputeDto` (nullable), positioned per B.6 (where `hasUnresolvedDispute` sat — after `carryForwardSourceCommitmentId`, before `allowedActions`). The DTO stays a `record`, never the entity.
- [ ] `CommitmentMapper` populates `dispute` = the commitment's `OPEN`/`IC_RESPONDED` dispute mapped via `DisputeMapper.toDto`, **`null` when none** — resolved in the single private core `toDto(c, allowedActions)` so every path (write responses + plan read) is consistent.
- [ ] A commitment with an **OPEN** dispute → `dispute` populated (id/status/flagType/managerNote/icResponse all present). A commitment with an **IC_RESPONDED** dispute → also populated (IC_RESPONDED is unresolved). A commitment with **only RESOLVED** history → `dispute == null`. A commitment with **no** dispute → `dispute == null`.
- [ ] **End-to-end pin (the 9.11a unblock proof):** the plan read (E3 `GET /api/plans/current` AND/OR E4 `GET /api/plans/{id}`) for a plan whose commitment has an OPEN dispute returns JSON nesting `dispute` with the live id + `managerNote` + `status` + `flagType`. This is the field the frontend `dtos.ts` mirror + 9.11a consume.
- [ ] **Leak test:** the nested `dispute` is the `AlignmentDisputeDto` record, never the `AlignmentDispute` entity — no audit-quartet fields (`createdAt`/`createdBy`/`updatedAt`/`updatedBy`) on the nested object (extend the existing `WeeklyCommitmentDto` / plan-read `.doesNotExist()` leak assertions to the nested path).
- [ ] No new authz: the dispute rides the existing E3/E4 plan-read authorizer (IC-self ∨ direct-manager). **No `DomainAuthorizationService` change**, no new endpoint, no new error code.
- [ ] `./gradlew check` green; 100% line on the changed mapper path; **no security-reviewer** (read-path, no new safety surface).

## Files expected to touch
**Modified:**
- `api/.../commitment/dto/WeeklyCommitmentDto.java` — add the `dispute` record component (import `AlignmentDisputeDto`); update the transitional-subset Javadoc (the omission is now discharged — Option-A realized).
- `api/.../commitment/mapper/CommitmentMapper.java` — inject `AlignmentDisputeRepository` (`:shared`) + `DisputeMapper` (`:api`); resolve + map the unresolved dispute in the private core `toDto`; update the Javadoc (no longer "omits the dispute field").
- `api/.../commitment/mapper/CommitmentMapperTest.java` (or the existing mapper test) — the OPEN / IC_RESPONDED / RESOLVED-only / none cases + the DTO-not-entity leak.
- The existing plan-read endpoint/integration test (E3/E4) — extend with the disputed-commitment nesting pin (the frontend-facing proof).

**NOT in this slice:** `DomainAuthorizationService` (no new authz); any dispute mutation (5.4/5.5); `ManagerReviewDto.unresolvedDisputeCount` (still 0 / Phase-6 projection); `ProjectionService` (Phase 6). `ARCHITECTURE.md` / `apps/wc-api/CLAUDE.md` — **orchestrator-owned** (I write the B.6 + cross-doc rows hot at the round commit; you flag at Step 9).

If implementation needs files beyond this list, **flag at Step 2.5** before going GREEN.

## RED test outline (Step 2)
Tests in the `CommitmentMapper` test + the plan-read endpoint/integration test:

1. **`toDto_commitmentWithOpenDispute_nestsDisputeDto`** — commitment + an `OPEN` dispute → `dto.dispute()` == the `DisputeMapper`-mapped DTO (id/status=OPEN/flagType/managerNote).
   - Why: B.6 Option-A; the carrier of the dispute id for E18/E19.
2. **`toDto_commitmentWithIcRespondedDispute_nestsDisputeDto`** — an `IC_RESPONDED` dispute is unresolved → still nested.
   - Why: the unresolved bucket is `{OPEN, IC_RESPONDED}` (§3) — IC_RESPONDED does NOT resolve.
3. **`toDto_commitmentWithOnlyResolvedDispute_disputeNull`** — historical `RESOLVED` dispute(s), none unresolved → `dto.dispute() == null`.
   - Why: the decision nests the **unresolved** dispute else null (doc 023 §6) — not "most recent".
4. **`toDto_commitmentWithNoDispute_disputeNull`** — no dispute rows → `null` (the finder's empty branch).
5. **`planRead_disputedCommitment_nestsDisputeInJson`** (endpoint/integration, E3 and/or E4) — `GET /api/plans/{id}` (or `/current`) on a plan whose commitment has an OPEN dispute → response JSON `commitments[].dispute` carries id + managerNote + status + flagType. **The 9.11a unblock proof.**
6. **`planRead_disputeIsDtoNotEntity_noAuditQuartet`** (leak) — the nested `dispute` exposes no entity audit fields.

(If the impl judges the mapper unit tests + ONE endpoint test sufficient, fine — keep #5 as the load-bearing end-to-end pin.)

## Cross-doc invariant impact (implementer flags at Step 9; orchestrator writes the docs)
- **Model field change (contract DTO):** `WeeklyCommitmentDto` **+`dispute: AlignmentDisputeDto`** / **−`hasUnresolvedDispute`** (the latter never existed in code — the DTO was the documented transitional subset; only the ARCHITECTURE row carries it today).
- **Orchestrator doc rows to write hot (Step 9 routing → my round commit):**
  - **`ARCHITECTURE.md` Appendix B.6** — DROP the `hasUnresolvedDispute | boolean | no | derived` row (line ~474); ADD `dispute | AlignmentDisputeDto | yes | the current OPEN/IC_RESPONDED dispute (Option-A, docs/planning/023 §6); null if none`.
  - **`apps/wc-api/CLAUDE.md` cross-doc table** — update the `WeeklyCommitmentDto`↔B.6 row: the transitional-subset omission is **discharged**; the DTO now nests `dispute?` (Option-A realized, brief 059); `hasUnresolvedDispute` dropped from the contract.
  - **(Possible) LESSONS** — only if Step 9 surfaces a genuinely new pattern (e.g. "aggregate-child nested via the existing mapper core"); my pre-bet is this is a §24/§22 application, not a new lesson. Your call at Step 9.
- **This is a CROSS-TRACK contract edit.** After your Step-10 commit, I claim the ARCHITECTURE B.6 window (serialize two-way with `st6-main-wc-web-orchestrator`), write the rows, then ping the frontend orch to mirror `dtos.ts`. **You do not touch `ARCHITECTURE.md` / `apps/wc-api/CLAUDE.md` / `MVP_TASKS.md` / `LESSONS.md`.**

## Things to flag at Step 2.5
1. **Single lookup site = the private core `toDto(c, allowedActions)` (populate ALL paths).** My default: resolve the dispute once in the private core so write responses (E5/E6/E7/E11/E12) AND the plan read (E3/E4) all carry `dispute` consistently. Rationale: the dispute is an intrinsic property of the commitment; one site is DRY + leak-test-once. Alternative (scope to read-only paths) would require splitting the lookup out of the core — more complex, no contract benefit. **Confirm populate-everywhere, or flag if you see a reason to scope it.**
2. **[EFFICIENCY] Per-commitment finder vs batch-load on the plan read (N+1).** The plan read maps N commitments; populating `dispute` per-commitment adds N `findByCommitmentIdAndStatusIn` queries. **My default: per-commitment is acceptable here** — it is *consistent with the shipped mapper*, which ALREADY does a per-commitment `resolveBreadcrumb` (so the plan read is already N-shaped), and MVP plans are small (≤~20 commitments). A batch list-finder (`List<AlignmentDispute> findByCommitmentIdInAndStatusIn(...)` grouped in `PlanMapper`, §22-style) is the cleaner-at-scale option but a bigger rewire (new finder + PlanMapper threading). **Default = per-commitment now; batch-optimization deferrable to the Phase-6 command-center projection slice.** Flag if you'd rather batch it in this slice (I'd accept either; per-commitment keeps the slice tight).
3. **Field ordering + nullability.** `dispute` is nullable, positioned after `carryForwardSourceCommitmentId` / before `allowedActions` (where `hasUnresolvedDispute` sat in B.6). Confirm the record component order matches the B.6 row order I'll write. No `List.copyOf` (single nullable object, not a collection).
4. **Unresolved-bucket constant DRY.** Reuse the existing `{OPEN, IC_RESPONDED}` bucket if 5.2/5.3 centralized it (e.g. a constant on `ReviewStatusDeriver` or the dispute service); else inline consistent with them. Flag where you found/placed it.

## Dependencies + sequencing
- **Depends on:** 5.3 (`8e2c91f` — `AlignmentDisputeDto` + `DisputeMapper` + the repo finder); 3.3a (`WeeklyCommitmentDto` + `CommitmentMapper`); 4.4b (the context-aware `toDto(c, plan, actor)` overload).
- **Blocks:** **FRONTEND 9.11a** (disputes UI) + ST.4 disputed→failure left-accent + ST.6 DisputePanel — all unblock when I mirror `dtos.ts` after this lands. Also a soft input to 5.4 (IC respond) / 5.5 (resolve), which read `icResponse`/`status` through this nest on the UI side.

## Estimated commit count
**1 — standalone cross-doc contract edit.** Read-path, no safety invariant. Do NOT bundle (cross-doc traceability wants the B.6 realization atomic + bisectable; it is also the cross-track unblock boundary).

## Lessons-logged candidates anticipated
- **Architecture-doc note** — B.6 Option-A realized: the dispute is nested through the commitment aggregate root (no separate `GET /api/disputes`), discharging the long-standing transitional-subset (LESSONS §24).
- **Convention candidate (low-confidence)** — "nest an aggregate-child DTO via the existing mapper's single private core so all response paths stay consistent" — only if Step 9 shows it's more than a §24/§22 application.
- **Cross-doc invariant change** — the `WeeklyCommitmentDto`↔B.6 row (+`dispute` / −`hasUnresolvedDispute`); I write it hot.

## How to invoke
1. Read this brief end-to-end — esp. **Q1 (single lookup site)** + **Q2 (per-commitment vs batch / N+1)**.
2. Run `/tdd 5.3b-commitment-dispute-nest-b6`.
3. **Step 0 (Restate)** — confirm "nest the unresolved dispute in `WeeklyCommitmentDto`, drop `hasUnresolvedDispute`, populate via the existing `DisputeMapper`/repo in `CommitmentMapper`'s core."
4. **Step 2.5** — send the test design + answers to the 4 questions. I reply `APPROVED.` / `TWEAK:` / `ADD:`.
5. **Step 9** — flag the `WeeklyCommitmentDto`↔B.6 cross-doc change (I write the B.6 + CLAUDE rows hot); ship/no-ship + draft commit message. **No security-reviewer this slice.**
