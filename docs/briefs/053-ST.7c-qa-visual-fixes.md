# /tdd brief — ST.7c: QA visual fixes (stepper/StatusBadge redundancy · ConfidenceMeter LOW · stepper width)

## Feature
Three render-only visual fixes from the ST.7b real-browser QA punch-list: (1) **drop the in-bar StatusBadge** in `PlanLifecycleBar` (the lifecycle "Draft ×3" redundancy — the stepper + the top-right plan badge already convey state); (2) **bump the ConfidenceMeter LOW legibility** (the single LOW segment is near-invisible, esp. light); (3) **constrain the stepper width** (the connecting bars read sparse/faint across the wide container). Tailwind/token-native, `/tdd`. Closes the QA's only a11y-legibility nit (#2).

## Use case + traceability
- **Task ID:** ST.7c (Phase ST — QA visual fixes; from the ST.7b punch-list)
- **Spec source:** the ST.7b QA findings (orchestrator, 2026-06-03; screenshots `/tmp/qa-01-ic-draft-dark.png` light + `/tmp/qa-02-ic-draft-dark.png` dark) + the Cadence `WeeklyPlanView.jsx`/`atoms.jsx` (stepper + ConfidenceMeter reference). **Architecture:** `ARCHITECTURE.md §7` (styling-SoT), REQ-S-005 (legibility — #2). Render-only.
- **Related context:** ST.5a (`e49e46a` — `PlanLifecycleBar` stepper + the in-bar StatusBadge that Flag-A deferred to "decide rendered"; **now decided: drop it**), ST.3 (`ConfidenceMeter` — `cf609a5`), `statusTaxonomy.ts` §7 (`CONFIDENCE_TAXONOMY` — LOW=neutral tone; **don't change the tone map**).

## Acceptance criteria
- [ ] **#1 — drop the in-bar StatusBadge:** remove the `<StatusBadge kind="plan" .../>` from `PlanLifecycleBar.tsx` (~line 138). The stepper's active node conveys state *within* the bar; the **top-right plan StatusBadge in `WeeklyPlanView`** remains the canonical text indicator. (Confirm at Step 2.5 that `WeeklyPlanView` renders a plan-state badge in its header — if it does NOT, keep ONE text indicator; see Q1.) Net: "Draft" renders **at most 2×** (stepper active node + the canonical header badge), not 3×.
- [ ] **#2 — ConfidenceMeter LOW legibility:** the 3-slot meter is clearly legible at LOW (1 filled of 3) without changing the §7 `CONFIDENCE_TAXONOMY` tone (LOW stays `neutral`). **Default approach (Q2):** give the **empty** segments a subtle outline (`ring-1 ring-border` or `border border-border`) so the 3-slot structure is always visible and the single filled LOW segment reads as "1 of 3"; keep the filled fill = the tone-solid. Verify legible in **both** themes.
- [ ] **#3 — constrain stepper width:** the stepper (`data-cy="lifecycle-stepper"`) is bounded to a readable max-width (e.g. `max-w-md`/`max-w-lg`, left-aligned or centered) instead of spanning the full `content-max`/`reading-col` width, so the connecting bars (`data-cy="stepper-bar"`) read as a compact stepper. (Optionally strengthen the bar from `h-0.5` if still faint — Q3.)
- [ ] **Render-only — NO enum/Appendix-A change; NO §7 taxonomy tone change** (LOW stays neutral). **No `.wc-*` CSS / no hex / no arbitrary `[…]` hatches.** Both `[data-theme]` render. `yarn` typecheck + lint + prettier + full Vitest pass.
- [ ] **Re-QA (orchestrator, after GREEN):** I re-run the headless `/browse` pass on the IC/DRAFT workspace (dark + light) to confirm the gestalt — the deterministic tests pin the class/DOM contract; the "looks right" confirmation is my re-QA.

## Files expected to touch
**Modified:**
- `apps/wc-web/src/features/plan/PlanLifecycleBar.tsx` (+ `.test.tsx`) — remove the in-bar StatusBadge (#1); constrain stepper max-width (#3)
- `apps/wc-web/src/features/commitment/ConfidenceMeter.tsx` (+ `.test.tsx`) — empty-segment outline (#2)
- (If #1 leaves `StatusBadge` unused in `PlanLifecycleBar`, drop the import.)

## RED test outline (Step 2)
1. **`lifecycle_bar_drops_inbar_status_badge`** (`PlanLifecycleBar.test.tsx`) — the bar renders the stepper but **no** `[data-cy="status-badge"]` inside `PlanLifecycleBar` (the in-bar badge is gone); the stepper's active node still present. (If a test previously asserted the in-bar badge — the ST.5a disambiguation scoped queries to `status-badge` — update it to assert absence-in-bar / presence-in-header.)
2. **`confidence_meter_low_shows_all_three_slots`** (`ConfidenceMeter.test.tsx`) — at LOW, all 3 `conf-seg` render with the empty ones carrying the outline class (`ring`/`border`), 1 filled; the meter is structurally legible (3 slots visible). Keep the existing per-level filled-count assertions.
3. **`lifecycle_stepper_is_width_constrained`** (`PlanLifecycleBar.test.tsx`) — the stepper container carries the max-width class (not full-width).
4. **`no_wc_star_css_or_hardcoded_token_introduced`** — the touched surfaces stay in the `surfaceSkin.test.ts` guard (both already in it); no `.wc-*`/hex/arbitrary-`[…]`.

## Cross-doc invariant impact
- **NONE.** Render-only; no DTO/enum/taxonomy-tone change (LOW stays neutral). No Appendix-A/B change.

> **Implementer never edits `apps/wc-web/CLAUDE.md`, `ARCHITECTURE.md`, `MVP_TASKS.md`, or `apps/wc-web/LESSONS.md`.**

## Things to flag at Step 2.5
1. **#1 — confirm the canonical state indicator.** Does `WeeklyPlanView` render a plan-state `StatusBadge` in its header (the top-right "Draft" I saw in the QA)? **My default vote:** yes it does → drop the in-bar one, keep the header one + the stepper. If `WeeklyPlanView` does NOT have its own badge (the top-right one WAS the bar's), then keep a single state text — don't leave the bar with only the stepper if that loses the text label. Report what you find.
2. **#2 — LOW legibility approach.** **My default vote:** outline the empty segments (`ring-1 ring-border`) so the 3-slot structure always reads + the filled tone-solid shows "N of 3" — preserves the §7 neutral tone, fixes all levels' clarity, not just LOW. Alt: strengthen the empty-segment bg contrast. **Do NOT** change `CONFIDENCE_TAXONOMY` LOW's tone.
3. **#3 — stepper width + bar weight.** **My default vote:** `max-w-md` (or `-lg`) on the stepper container so it's compact; keep `h-0.5` bars unless still faint after constraining (then `h-1`). Confirm the width value.
4. **Bundle:** 3 small render-only fixes, same ST surface area — one slice/commit. Split only if a test surprises you.

## Dependencies + sequencing
- **Depends on:** ST.5a (`e49e46a`), ST.3 (`cf609a5`) — landed. The ST.7b QA — done.
- **Blocks/feeds:** ST.7 close (with ST.7d data-layer fixes). My post-GREEN re-QA confirms the gestalt.
- **Parallel sibling:** **ST.7d** (data-layer fixes #4–6 + persona-switch-refetch correctness + manager-surface spot-check) — separate brief 054; this (053) is the visual stream.

## Estimated commit count
**1 commit — NOT safety-critical** (render-only visual). RED→2.5→GREEN per fix, one Step-10 commit.

## Lessons-logged candidates anticipated
- Likely none. Flag if the "lifecycle conveyed by stepper, not a redundant badge" or the "always-outline meter slots for count legibility" reads as a reusable §-note.

## How to invoke
1. **Read this brief** + `PlanLifecycleBar.tsx` (the in-bar StatusBadge ~line 138 + the stepper ~105-130) + `ConfidenceMeter.tsx` + `WeeklyPlanView.tsx` (confirm its header badge for Q1).
2. **Run `/tdd ST.7c QA visual fixes`** (session already oriented).
3. **Step 0/1** — confirm Files; the Q1 header-badge check.
4. **Step 2.5** — send the write-up + Q1–Q4. Wait for `APPROVED.`/`TWEAK:`/`ADD:`.
5. **Step 7.5** — reachability: the fixes render on `/weekly-commit`. Flag gaps.
6. **Step 9** — categorized summary + ship/no-ship + draft commit; cross-doc = NONE. (I re-QA after your commit.)
