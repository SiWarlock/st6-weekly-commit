# Handoff 010 — st6-main frontend orchestrator cycle (disputes done + QA passed; styling-fidelity round + 9.15 incoming)

- **Date:** 2026-06-04 · **Track:** st6-main · **Cycling:** `st6-main-wc-web-orchestrator` (frontend orch) at **78% ACTION** — **user-directed "cycle everything"** before the big frontend work. Clean boundary (no slice in flight; the disputes round is sealed). **The frontend impl was already retired** (scope-complete pause after disputes); a **fresh impl spins up AFTER you (the fresh orch) scope the styling round + 9.15 together** (the lead spins it on your signal).
- **HEAD:** the backend orch is mid-seal of its Phase-6 round in parallel (`ARCHITECTURE.md`/`MVP_TASKS.md`/`apps/wc-api/*` + briefs 069/070/071 uncommitted in the working tree — **DO NOT TOUCH; backend territory**). My frontend cycle commit lands `docs/planning/024` + `docs/briefs/072` + this handoff (frontend/planning only, explicit `git add`).

## Where the frontend is — disputes feature COMPLETE + real-browser-QA'd
- **Phase 9 (functional, 9.1–9.14) + Phase ST (Cadence styling ST.1→ST.7e) + the alignment-disputes feature are DONE end-to-end.** IC respond/display (**9.11a** `743bdf9`) + manager open/resolve (**9.14** `06910ba`). 233/233 Vitest green. The disputes round is **sealed** (`b59d526` = 9.14 DONE + Carry-forward + Log; `1742a69`/`9e825bc`/`1e267fe` the doc commits; impl session doc `014` `07c0543`). LESSONS **§19** (dormant-until-emitted) + **§20** (one-gated-component-all-roles) banked.
- **Cross-track:** unblocked by backend 5.3b (B.6 `dispute?` nest); the controls auto-activated when backend **5.5b** (`003394a`) emitted the dispute `allowedActions` on E3/E4 (the §19 design paid off, zero rework).

## The real-browser disputes QA (2026-06-04) — PASS on per-state, LIVE-LOOP deferred
Ran via **headed gstack `/connect-chrome`** on **localhost:5180** (the standalone MSW demo). **Both personas verified live, both themes:**
- **IC (Lena Ortiz, reconciling):** the disputed SOC-2 commitment renders with the **ST.4 red accent**, the **DisputePanel stepper** (Open─IC responded─Resolved), the dispute display (Open/Misaligned + the manager's flag note), and the **IC Respond form** — correctly gated (IC sees Respond, not open/resolve). `/tmp/qa-ic-lena-dispute.png` (dark) + `/tmp/qa-ic-lena-light.png`.
- **Manager (Morgan Lee):** the command center (Lena = **Overdue** + **dispute flag "1"**) + the **9.14 Review Drawer** rendering the report's commitments via `CommitmentList` — **Mark reviewed**, per-commitment **Open-dispute** forms (Flag type + note + button, *disabled until the required managerNote*), and the **DisputePanel + Resolve-dispute** on the disputed commitment. `/tmp/qa-mgr-drawer-open.png` (dark Drawer) + `/tmp/qa-mgr-drawer-light.png` (light cmd-center).
- **The §11/§20 dividend, LIVE:** the SAME `CommitmentList` renders role-correct for both actors purely via the server (MSW) `allowedActions`. Confirmed.
- **LOOP-LIMITED (expected):** the live *transition* loop (open→OPEN→respond→IC_RESPONDED→resolve→resolved) can't progress on the **static** MSW — a resolve POST 404s (no stateful mutation handler). → fixed by **9.15** (below).

### Two QA findings (fold into 9.15)
1. **MSW service-worker cold-install race** — in a *fresh* browser the standalone hangs on a loading skeleton (the app's first RTK Query requests fire before the SW controls the page → hang → don't recover); a **fresh tab (warm SW) clears it**. The logged "MSW 1s-loop at first-SW-install" Carry-forward, reproduced. Fix in 9.15 (await SW-control-before-first-query / reload-on-cold-install).
2. **Demo base-URL** — the demo needs `VITE_AUTH_MODE=demo` AND **must NOT** set `VITE_API_BASE_URL` (the `.env.example`'s `localhost:8080` makes requests cross-origin → bypasses the same-origin MSW SW → hangs). Relative base only. (Both folded into brief 072.)

## YOUR NEAR-TERM JOB (fresh orch) — scope TWO things together, then the lead spins the impl
**The frontend track REACTIVATES.** Two bodies of work, both render-only / standalone-demo / **no contract change** / REQ-I-008-preserved. Scope them, brief them, then **ping the lead "ready to spin frontend impl."**

1. **STYLING-FIDELITY ROUND — `docs/planning/024-styling-deviations-punchlist.md` (READ IT FIRST; it's the authoritative fix-list from the user's mockup-vs-as-built QA).** **⚠️ USER DIRECTIVE — fix against the actual MOCKUP COMPONENTS directly (doc 024's "AUTHORITATIVE MOCKUP REFERENCE" block):** the full React mockup app is `docs/design/cadence-design-system/ui_kits/weekly-commit/` — **`app.jsx`** = the app-shell (§A S1), **`CommandCenter.jsx`** (§B), **`WeeklyPlanView.jsx`** + **`CommitmentCard.jsx`** (§C), **`atoms.jsx`**/`overlays.jsx`/`kit.css`/`colors_and_type.css` (atoms/overlays/tokens), `index.html` + `preview/component-*.html` (rendered). For each fix, read the mockup component for its layout/structure/composition + map it onto the existing Cadence Tailwind/Flowbite tokens + `statusTaxonomy.ts` (still **no hex / no `.wc-*`** — `surfaceSkin.test.ts` guards it). **⚠️ THE MOCKUP IS CANON + doc 024's list is NON-EXHAUSTIVE (user directive):** the fix-pair MUST do a **direct side-by-side gstack compare** — load BOTH the canon mockup (`docs/design/cadence-design-system/ui_kits/weekly-commit/index.html`, served locally) AND the running standalone app, **per surface (command center · IC plan view · heatmap · drawers · both themes)**, and **fix EVERY deviation found, including ones not enumerated below.** Headlines:
   - **S1 — the whole app-shell/chrome is MISSING** (top app-bar logo/product/Demo/week/identity + the My-Weekly-Commit/My-Team + Command-Center/Heatmap nav + breadcrumbs). **Root cause = expected MFE behavior** (the host owns the chrome; standalone only renders PersonaSwitcher+toggle). **Fix = build a demo app-shell into `src/standalone/StandaloneShell.tsx`**, standalone-only/tree-shaken (the `surfaceSkin`/build-grep REQ-I-008 guards must stay green); fold the PersonaSwitcher into the app-bar identity slot; nav tabs drive the existing `AppRoutes`.
   - **Command-center fidelity (S2):** the "At a glance" summary strip (= the deferred SLA strip, **client-computable from the MSW rows now** — D-1), rich **labeled** risk chips + P/U count pills + tone colors + hide-zero-chips (vs generic icon+number), avatars, review timestamps, the **compact filter-chip row** + active-filter chip + result count (vs the heavy dropdown grid), a RECONCILE column, the week-range pager + "updated" affordance, Review/Open buttons, section header + sort, footer legend, the "Alignment Command Center" title.
   - **IC card (S2):** missing plain description line, chip-row-above-SO-box (currently reversed), the plan-header card framing, the **GOLD** Start-reconciliation button (currently purple), drop the mono font on the SO/objective text, breadcrumb + "My Weekly Commit" title.
   - **~2–4 slices** (app-shell · command-center fidelity · IC card · final QA). **Two USER decisions** (the lead is relaying to the user): **D-1** (build the client-computed summary strip now for demo fidelity; queue the backend §9 `summary` field as the production follow-up — recommended) + **D-2** (confirm the ~2–4-slice scope). Default both = go.
2. **9.15 — STATEFUL MSW mutable-`db` — `docs/briefs/072-9.15-stateful-msw-mutable-db.md` (AUTHORED, ready to dispatch).** A mutable in-memory `db` seeded from fixtures behind the existing MSW handlers, with the **dispute mutations (E17/E18/E19) writing through** (enables the live loop) + both QA findings fixed. Step-2.5 Q1 scopes disputes-mutations-only vs all-lifecycle-mutations. Realizes the Phase-13 mutable-db Carry-forward. Demo-only, no backend dep.

**Sequence them as ONE reactivation** (the styling shell + the stateful MSW are the demo-fidelity round). Decide the slice order (the app-shell S1 is the highest-impact + unblocks every surface's fidelity; 9.15 unblocks the live-loop QA — both can run in parallel-ish across slices). Then the lead spins ONE fresh impl that works the queue.

## Held / cross-track (IMPORTANT)
- **MVP_TASKS window is now FREE — the backend orch's Phase-6 round seal LANDED (`39061e9`).** Its untracked files (and your `docs/briefs/072` + `docs/planning/024`) were left untouched. Add the **9.15 row** + the **styling-round task rows** (number them — 9.16+ or a styling sub-block; note "demo-fidelity reactivation") at your first `/orchestrate-end`, **re-claiming the window from `st6-main-orchestrator` per the usual two-way handshake** (it's into its next Phase-6 round — `git`-verify clean + explicit `git add` your frontend regions only).
- **Brief lane:** frontend used through **072** (9.15). Backend through **071** + re-picks **073–076** (6.5a-2/6.5b/6.6/6.7). **Next frontend brief ≈ 077** (ls + next-free; coordinate with `st6-main-orchestrator`).
- **Disputes contract is FROZEN + verified** — your earlier E13 (B.11/B.20) cross-doc check found ZERO drift; the backend's Phase-6 (6.5a E13 etc.) makes your 9.9/9.10 manager UIs *real* (it'll ping you for E14/E15 at 6.5b for the same field-for-field mirror check). The **SLA strip's** backend `summary` field stays the production follow-up (D-1).

## Environment for the QA (leave it UP — the fresh pair reuses it)
- **Dev server UP on `localhost:5180`** — launched via `cd apps/wc-web && VITE_AUTH_MODE=demo <nvm-node> <repo>/node_modules/vite/bin/vite.js --port 5180` (yarn is now Corepack-enabled on PATH, so `VITE_AUTH_MODE=demo yarn dev --port 5180` also works). **Do NOT set `VITE_API_BASE_URL`** (Finding #2). MSW on by default in dev.
- **Headed gstack browser connected** (`$B = ~/.claude/skills/gstack/browse/dist/browse`; `Mode: headed`). For the persona-switch QA: **headed only** (§18); **fresh tab / warm SW** to dodge the cold-install race until 9.15 fixes it; client-side-navigate (`history.pushState`+`popstate`) to manager routes after selecting Morgan (a full `goto` resets the persona to default Ivy). The `5173` port is a *different* app (a translation tool) — ignore it.

## Read on resume
`docs/orchestrator-briefing.md`, `docs/tdd-brief-template.md`; **`docs/planning/024` (the styling punch-list — your biggest new input)** + its **AUTHORITATIVE MOCKUP REFERENCE → `docs/design/cadence-design-system/ui_kits/weekly-commit/` (read `app.jsx`/`CommandCenter.jsx`/`WeeklyPlanView.jsx`/`CommitmentCard.jsx`/`atoms.jsx` when scoping each fix)**; brief **072** (9.15); impl session doc **014** (ST.6c→7e + 9.11a + 9.14); the disputes Log entry (2026-06-04) + the disputes-chain Carry-forward in `MVP_TASKS.md`; wc-web `LESSONS.md` §1–§20 + `CLAUDE.md` (lookup + cross-doc + lessons index, incl. the new dispute rows + §19/§20); the Cadence design system + the **mockup UI kits** the punch-list compares against. **Register your identity + run `/orchestrate-start`** (NOT `/session-start`).

## Standing memories (already in `~/.claude/.../memory/`)
- `frontend-real-browser-qa.md` — gstack/Claude-in-Chrome authorized for wc-web QA (gstack `/connect-chrome` is the working path; the MCP/Arc extension was flaky this session).
- `per-slice-reviewers-disabled.md`, `cycle-cleanup-stale-registry.md` (the lead cleans my ghost on cycle), `parallel-track-handoff-numbering.md` (committed-brief-keeps-number; track-qualify handoff filenames — this one is `010-…-st6-main-frontend-…`), `lead-cycle-timing-no-thrash.md` — all apply.
