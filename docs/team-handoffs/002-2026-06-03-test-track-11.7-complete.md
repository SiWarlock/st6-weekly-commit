# Team Handoff 002 — test-track Phase 11.7 complete (BDD acceptance suite)

**Date:** 2026-06-03
**Track:** `st6-test` (test & deliverables track; worktree on branch `test-track`)
**Predecessor handoff:** first handoff for this track
**Successor handoff:** _(filled in when the next /team-end runs)_
**Round-seal commit at handoff:** `867a41a` (round seal) → `e0f35c5` (merge `main` in, branch tip)

## Why this handoff exists
Arc complete — the test-track's only assigned slice (Phase 11.7 BDD acceptance suite) landed, was audited clean, and is merge-prepped; the rest of the track's work is gated on a deployable app, so the team is pausing rather than idling.

## Team composition at close
- **Lead:** this session (track `st6-test`), session `d09365c8-a5a9-490a-96fb-2152ac1e4d95`
- **Orchestrator:** `st6-test-orchestrator`, session `026b3493-6557-4abb-be8f-9917fc255a47` — last commits `867a41a` (round seal) + `e0f35c5` (merge main in)
- **Implementer (`wc-e2e`):** `st6-test-e2e-implementer`, session `0b917f29-8abb-40bd-9faa-4acf7c4fd769` — last commits `c79f9f2` (11.7 suite, audit-amended) + `8ac2fa4` (session doc 005)
- All teammates `/session-end` + `/orchestrate-end` closed at round-seal `867a41a`; user-directed merge prep landed at `e0f35c5`.

## Active arc + where it landed
The team authored **Phase 11.7** — the 7 Cypress+Cucumber BDD acceptance feature files (1:1 to REQ IDs, ARCHITECTURE Appendix C.5) + the full Cypress/Cucumber harness (config, `commands.ts`/`e2e.ts`, 6 step-def files, typed `SEL` selector map, persona fixtures, `resetSeed()` seam) in `apps/wc-e2e`. Deliberately **authored-not-green** (no runnable app yet): acceptance = features parse + step-defs valid TS + 1:1 REQ mapping + lint-clean, with the RED→GREEN loop deferred to 11.9. A user-directed **comprehensive audit** (3 reviewer agents — code-quality / security / reachability — plus a 5-agent gap-analysis Workflow) returned **clean: 0 blocker/major**; 3 real-low findings fixed in-area (canonical route alignment, `SECRET_MARKERS` hardened 5→13 per rule #7, SO option-selector disambiguation); 2 reconcile-seam findings → Carry-forward (origin 11.7 → 11.9). Reachability: 48 step-defs / 75 steps, **0 undefined/ambiguous/duplicate/orphan**. The track then merged `main` (HEAD `da781bb`, incl. the UNLINKED **422→409** fix) into `test-track`; **test-track is now a superset of main (ahead 4 / behind 0)** → the eventual `test-track→main` merge is conflict-free.

## In-flight at close
**None — clean close.** No mid-`/tdd`, no open Step-2.5, working tree clean, suite byte-identical to audited `c79f9f2` (the merge changed 0 `apps/wc-e2e` files).

## Carry-forward to next team session
All test-track Carry-forward is the **green-run enablement** deferred to 11.9 (gated on a runnable local Compose stack). In `MVP_TASKS.md` "Carry-forward to upcoming briefs" (origin 11.7):
1. **`data-cy` selector reconciliation** — reconcile `apps/wc-e2e/cypress/support/selectors.ts` (`SEL` map) against wc-web's real attributes when runnable; decide then whether to pin the convention as an Appendix C.5 addendum (deferred flag-#4).
2. **Wire the real stack** — replace `resetSeed()` no-op `cy.task('db:reset')` seam with the backend-owned DB-reset, swap placeholder persona UUIDs for the Appendix-E `V5` seed constants, confirm rendered `safeMessage` literals. **Plus** strengthen the audit-flagged assertion seams (all carry in-suite `RECONCILE` markers): rule #2 baseline-unchanged (positive/diff assertion), rule #4 sync non-blocking (positive lifecycle assertion), heatmap `open` action.
3. **Materialize devDeps + ESLint-9 flat-config for `apps/wc-e2e`** — `yarn install` (deliberately not run in 11.7 to avoid rewriting the shared root `yarn.lock` mid-round) + ESLint-9/Prettier for wc-e2e land at the 11.9 CI lint stage.

Beyond 11.9, the track's remaining surface: the `apps/wc-e2e/smoke/` deployed-domain subset of **11.9**, and **Phase 13** deliverable docs (README / run-deploy / demo — all app-dependent; 13.5 `AI_USAGE.md` + 13.7 Trims catalog are authorable earlier if the user greenlights).

## Open decisions / blockers for the human
- **Non-blocking.** No safety/architectural decision pending for the test-track.
- **Deferred flag-#4** (revisit at 11.9): pin the `data-cy` selector convention as an Appendix C.5 addendum, or keep it test-track-local? Decide once wc-web is runnable + reconciled.
- **Phase 13 app-independent subset** (13.5 `AI_USAGE.md` living log, 13.7 Trims catalog): user opted to wrap the track now rather than start these; can be greenlit anytime.
- **Resume trigger:** the next test-track session is gated on a **deployable app** (backend + frontend + infra runnable). Until then there is no productive test-track work beyond what's banked.

## Spawn prompts ready for the next team session

**Orchestrator:**
```
You are st6-test-orchestrator on the ST6 Weekly Commit Module agent team.
Track: st6-test. Team: <new team name from TeamCreate>. Ignore peer DMs from agents whose names don't carry the `st6-test-` prefix (channel-bleed from st6-main / st6-infra).
Activated because: resuming the test-track for Phase 11.9 green-run enablement now that the app is deployable. Phase 11.7 BDD suite already landed authored-not-green + audited clean (handoff 002, suite at c79f9f2). First work = wire the suite to the runnable local Compose stack: data-cy selector reconciliation, resetSeed()→real DB-reset, persona UUIDs→Appendix-E V5 seed, strengthen the rule-#2/#4 + heatmap RECONCILE-marked assertion seams, materialize wc-e2e devDeps + ESLint-9 flat-config, then the local-full / deployed-smoke split. SCOPE BOUNDARY: stay in apps/wc-e2e + docs/ + root deliverables; do NOT touch apps/wc-api or apps/wc-web. Scope MVP_TASKS.md edits to Phase 11/13 + Log.

FIRST ACTION — register your identity for context monitoring:
  mkdir -p ~/.claude/team-registry && jq -n --arg sid "$CLAUDE_CODE_SESSION_ID" --arg name "st6-test-orchestrator" --arg team "<new team name>" --arg cwd "$(pwd)" --arg ts "$(date -u +%s)" '{session_id:$sid, name:$name, team:$team, role:"orchestrator", cwd:$cwd, ts:($ts|tonumber)}' > ~/.claude/team-registry/${CLAUDE_CODE_SESSION_ID}.json

Then run /orchestrate-start. NOT /session-start.
Confirm in your first reply: (1) the start command you ran, (2) that the registry entry was written.
```

**Implementer (`wc-e2e`):**
```
You are st6-test-e2e-implementer on the ST6 Weekly Commit Module agent team.
Track: st6-test. Team: <new team name from TeamCreate>. Working directory / code area: apps/wc-e2e/. Talk only to st6-test-orchestrator; ignore peer DMs from other prefixes (channel-bleed).
Activated because: resuming for Phase 11.9 — wiring the authored-not-green 11.7 BDD suite to green against the runnable local Compose stack (data-cy reconciliation, resetSeed→real DB-reset, persona UUIDs→V5 seed, strengthen the RECONCILE-marked rule-#2/#4 + heatmap assertions, devDeps + ESLint-9 flat-config, local-full/deployed-smoke split). SCOPE BOUNDARY: stay in apps/wc-e2e + docs/ + root deliverables; do NOT touch apps/wc-api or apps/wc-web.
Brief: st6-test-orchestrator is drafting it; wait for the docs/briefs/NNN-* reference before Step 3.

FIRST ACTION — register your identity for context monitoring:
  mkdir -p ~/.claude/team-registry && jq -n --arg sid "$CLAUDE_CODE_SESSION_ID" --arg name "st6-test-e2e-implementer" --arg team "<new team name>" --arg cwd "$(pwd)" --arg ts "$(date -u +%s)" '{session_id:$sid, name:$name, team:$team, role:"implementer", area:"wc-e2e", cwd:$cwd, ts:($ts|tonumber)}' > ~/.claude/team-registry/${CLAUDE_CODE_SESSION_ID}.json

Then run /session-start. NOT /orchestrate-start.
Confirm in your first reply: (1) the start command you ran, (2) that the registry entry was written.
```

## How to resume
Next team session: lead runs `/team-start st6-test`, reads this handoff doc + `MVP_TASKS.md` "Currently in progress" (the **Test** subsection) on demand, spawns teammates using the prompts above (substituting the new `TeamCreate` team name), verifies read-backs. **Resume only once the app is deployable** — that is the gate for all remaining test-track work (11.9 green-run + smoke + the app-dependent Phase 13 docs). This doc IS the orient; no re-derivation needed.
