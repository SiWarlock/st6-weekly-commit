# Handoff 014 — st6-main frontend orchestrator cycle (deploy-demo round: 083 build:standalone landed; OAuth-login scoped, held)

- **Date:** 2026-06-04 · **Track:** st6-main · **Cycling:** `st6-main-wc-web-orchestrator` (frontend orch) at **77% ACTION** (user-directed dual-orch cycle; backend orch cycling in parallel). Clean boundary — **9.16 just committed `1c04e3c`**; the next frontend slice (OAuth-login) is **HELD** (no slice in flight). **TIGHT handoff (area-specific only)** — the lead carries the cross-cutting context (the persona model = real Auth0 test users; the Outlook/SQS-SNS pipeline; the M365 path) in your spawn prompt.
- **Prior round (sealed `62ce663`):** the demo-fidelity round — 9.15 stateful MSW + ST.8a–e mockup canon fidelity, 290 Vitest, every doc-024 surface mockup-faithful both themes, the live disputes loop proven. **The demo is ship-quality in MSW/demo mode.** This NEW round wires the **deployed real-backend + real-OAuth** path (user-directed START 2026-06-04).

## What landed this session (frontend)
- **9.16 — standalone static-SPA build target** (`1c04e3c`, brief 083). `vite build` used to emit ONLY the MF remote (`remoteEntry.js`); now a **`build:standalone`** mode (`VITE_BUILD_TARGET=standalone`) flips the federation plugin OFF → a deployable static SPA (`dist/index.html` + hashed assets, NO `remoteEntry`). A pure env-injected resolver `vite.buildTarget.ts` (`resolveBuildTarget`/`shouldEnableFederation`) gates it, **fail-safe-to-remote** (only the exact `'standalone'` opts out). `build` (=`build:remote`) is the **unchanged remote default**; REQ-I-008 `boundary.test.ts` green (remote stays demo-free). `.env.example` documents `VITE_USE_MOCKS` + the standalone/real-backend build env block (`VITE_AUTH_MODE`/`VITE_USE_MOCKS=false`/`VITE_API_BASE_URL`; one-build-per-URL, baked at build). **294 Vitest green.** wc-web **LESSONS §28** banked (this commit). Build-verified both targets.

## THE NEXT FRONTEND SLICE — OAuth-login (HELD; you author the brief = next-free **085**)
**Held pending the user's persona-model pick** — the lead's call (per its spawn prompt) is **real Auth0 test users, one per seeded persona** (log in as Dana → manager; log out → log in as Lena → IC; the cross-role disputes flow across logins; + two browser windows as the demo-day technique). The instant in-memory **PersonaSwitcher retires to the dev/MSW path.**

**The scoped PLAN (you author + drive the brief):**
- **The Bearer-SEND is ALREADY wired** — `baseApi.ts` `prepareHeaders` has the `auth0` branch (`getAccessToken()` → `Authorization: Bearer`) consuming the `accessTokenProvider` seam (`authAccessor.ts` `setAccessTokenProvider`). The **backend Auth0 resource-server is BUILT** (Phase 2: 2.1 JWT decoder + 2.2 claim→employee mapper). So **only the frontend login PRODUCER side is missing.**
- **The gap (greenfield, ~1.5–2 slices):** install **`@auth0/auth0-react`** (PKCE SPA SDK; NOT installed) → an `Auth0Provider` (standalone-only, `src/standalone/` — the remote still gets its token from the host per §7) → `loginWithRedirect` + a **`/callback`** route → `getAccessTokenSilently` → feed the **existing `setAccessTokenProvider` seam** (then the Bearer-send just works) → **login/logout + "logged in as X" UI in the app-shell** (replace the PersonaSwitcher identity slot) → the unauthenticated login-screen state → the **§16 `resetApiState`** on login/logout (the `DemoIdentityProvider` reset pattern is the model) → `VITE_AUTH0_DOMAIN/CLIENT_ID/AUDIENCE` env.
- **CROSS-AREA (coordinate w/ the backend orch):** the Auth0 test-users' **claims must resolve to the V5-seeded employee IDs via the F.1 claim-mapper** (the OAuth-world persona↔seed alignment). The backend is actively building the V5 seed — see the untracked `apps/wc-api/shared/.../V5SeedPersonasMigrationTest.java` + backend handoff `013` (deploy-demo-v5-start). Sync the claim→employee mapping ↔ the seeded personas before the OAuth demo can resolve real users.

## Deferred to your first `/orchestrate-end` (NOT done — I cycled before sealing)
- **MVP_TASKS reconciliation** (with the two-way window handshake — the backend orch is ALSO cycling, so re-establish the window): tick the **9.16 row** (`1c04e3c`); add the **9.16 follow-ups** (the `docs/runbooks/` build-and-serve note for the infra hand-off; the **§7/Appendix-D ARCH note** = the standalone SPA vs the remote are two targets from one config — via the shared-doc window handshake). The prior round's **Carry-forward still queued**: 9.15b · the 3 backend B.11 follow-ups (`resolvedDisputeCount`/`unlinkedCount`/`reviewedAt`) · the 2 ST.8e BOUND-IT (drilldown SO-breadcrumb/count; dynamic self-plan row) · F2 cold-install residual (→§22) · the `FilterDropdown` rule-of-three · the clean dark-drawer screenshot.
- **`docs/runbooks/` build-and-serve note + the §7/Appendix-D ARCH note** — I deferred both (orch territory); author at your `/orchestrate-end`.

## Held / cross-track
- **Brief lane:** **083 used** (this commit); **next frontend brief = 085** (084 is the backend's per the lead — coordinate). The OAuth-login brief = 085.
- **Backend orch cycling in parallel** (deploy-demo + V5 seed; handoff 013). MVP_TASKS/ARCHITECTURE serialize two-way — re-establish the window at your seal. Backend's untracked `V5SeedPersonasMigrationTest.java` is theirs — never stage it; explicit `git add` frontend/planning regions only.
- This cycle commit stages: this handoff + `apps/wc-web/LESSONS.md` (§28) + `apps/wc-web/CLAUDE.md` (§28 index) + `docs/briefs/083`.

## Read on resume
Run **`/orchestrate-start`** (NOT `/session-start`). Then: brief **083** (committed — the build target); this handoff (the OAuth-login PLAN); `docs/planning/024` (the prior round); wc-web **LESSONS §1–§28** + `CLAUDE.md`; for the OAuth slice — `src/app/baseApi.ts` (the `auth0` branch + `prepareHeaders`), `src/app/authAccessor.ts` (the `setAccessTokenProvider` seam), `src/standalone/DemoIdentityProvider.tsx` (the §16 reset + the persona-applier pattern = the model for the Auth0 token provider), `src/standalone/shell/*` (the app-shell identity slot the login UI replaces). The lead's spawn prompt carries the cross-cutting context (persona model, Outlook/SQS-SNS, M365).
