# Handoff — st6-main backend ORCHESTRATOR cycle, Phase-6 mid-phase (full-team reset) (2026-06-04)

> **User directive: cycle EVERYTHING** (full team reset for the next push) — backend orch (`98bd8384`, ~57%, healthy) + backend impl (`a72d2a78`) + the frontend pair all cycle. This is a clean **mid-Phase-6 boundary**: the §9 projection spine is COMPLETE + the first manager read (E13) is live + sealed. Orch-authored continuity for the impl's slices is in **session doc 017**. HEAD = `39061e9`.

## YOUR IMMEDIATE NEXT ACTIONS (fresh backend orch)
1. Register (the team-registry `jq` one-liner) → run **`/orchestrate-start`** to orient (this handoff + session docs **016**+**017** + the tracker Phase-6 section + LESSONS §22–§39 + ARCHITECTURE §9 / Appendix B.11/B.12/B.20 / F.5).
2. **Phases 1–5 + the Phase-6 projection spine + E13 are COMPLETE — nothing to finish there.** Your first work is **dispatching the PRE-AUTHORED brief 073** (6.5a-2) to the fresh impl (see NEXT). Confirm direction with the lead before dispatch (the user reset the team; the lead logs).
3. **Shared-doc CAUTION:** the **frontend pair also cycled** (fresh web-orch). It owns `docs/planning/024-styling-deviations-punchlist.md` + brief 072 (9.15) — **NOT backend territory.** Git-verify the shared tree (MVP_TASKS/ARCHITECTURE) is clean + serialize the MVP_TASKS window with the fresh `st6-main-wc-web-orchestrator` before any shared-doc commit (we serialize two-way; explicit `git add` of backend regions only, never `-A`).

## STATE
- **HEAD:** `39061e9` (the Phase-6 3-slice round seal) on `5268784` (6.5a E13). **Backend Phases 1–5 COMPLETE; Phase 6 projection spine + E13 COMPLETE.**
- **Phase-6 landed (this arc):** **6.1** satisfied-by-prior (Phase 1). **6.2** (`7e86239`, brief 067) §9 `ProjectionService` derivation completion — misaligned-union + real `unresolvedDisputeCount` + the **`blocked_count` latent-bug correction** (`work_type=BLOCKER → reconciliation_outcome=BLOCKED`, lead-ruled per R5/`rebuild==seed`; §9 pinned). **6.3a** (`0e5d725`, brief 068) `ProjectionRefresher.recomputeForPlan` extraction (the source-loading entrypoint; the 6.7 rebuild reuses it). **6.3b** (`efdf0f5`, brief 069) the 4 remaining §9 triggers wired + **stale-cell deletion** (recompute-then-prune) + **RISK-003** rollback + §17 integration + the REQ-F-013 past-due pin → **all 9 synchronous projection triggers complete & drift-free**. **6.4** (`3a49985`, brief 070) `RiskBadgeDeriver` extraction (RISK-014 structural) + Appendix-E R2/R3/R5 + edge badge coverage. **6.5a** (`5268784`, brief 071, **security-reviewer CLEAN PASS**) `GET /api/manager/command-center` (E13) — direct-report-scoped (IDOR-safe query-scoping), the B.20 paginated envelope of B.11 rows, the 3 summary-level filters, N+1-free api-layer Criteria query → the frontend 9.9 command-center is real (web-orch verified zero-drift). All `./gradlew check` green; 6.5a security-reviewed CLEAN.
- **Impl:** `a72d2a78` (the fresh post-6.3a impl) — did 6.3b/6.4/6.5a. **Continuity in session doc 017** (orch-authored; the impl's `/session-end` was skipped at 72% per the lead). Now cycling (full reset).
- **Session docs:** **016** (6.2/6.3a, orch-authored) + **017** (6.3b/6.4/6.5a, orch-authored).
- **LESSONS banked this arc:** **§37** (sibling-table projection counts: load-once-derive-in-memory + pin the source against the binding seed; + a 6.3b addendum = recompute-then-prune stale-cell deletion, + a 6.4 addendum = self-FK `deleteAllInBatch`), **§38** (a new synchronous FK-writing side-effect on an existing service method ripples into every driver test's teardown — trace all drivers, FK-safe order), **§39** (a paginated response whose contract carries `sort` needs a custom `PageEnvelope` record — stock `Page`/`VIA_DTO` omit it; sort-by-joined-non-association-column ⇒ a Criteria query, co-located with its tests). Read §35 (the `viewerIsDirectManager` threading) — the **MARK_REVIEWED plan-read** carry-forward reuses it.

## NEXT — Phase 6 continues (manager reads). DISPATCH brief 073 first.
**Brief 073 (6.5a-2) is PRE-AUTHORED + COMMITTED** (`docs/briefs/073-6.5a-2-command-center-cross-table-filters.md`) — dispatch it to the fresh impl as your first slice. It directs the fresh impl through `/session-start` first (NEW session).
- **6.5a-2 (brief 073)** — the 4 E13 **cross-table EXISTS filters** (`definingObjectiveId` via the `manager_heatmap_cell` grain; `priority`/`workType`/`alignmentStatus` via `weekly_commitment` over the §4 indexes), extending the existing `ManagerCommandCenterQuery` (the impl left a hook: *"the 6.5a-2 cross-table EXISTS filters slot into `summaryPredicates` without touching this structure"*). The IDOR scope is unchanged (filters only narrow). Light ad-hoc security-reviewer (confirm no IDOR widening). _(Split from 6.5a — recorded in Carry-forward; the 4 params are currently undeclared in the E13 controller, so the API honestly reflects only the applied filters until 073 lands.)_
- **6.5b (brief 074)** — **E14 heatmap** (`GET /api/manager/heatmap` → `HeatmapResponseDto {weekStart, cells: HeatmapCellDto[]}`, NOT paginated, bounded by reports×DOs; coarse `authorizeTeamHeatmapAccess` + query-scoping) **+ E15 drilldown** (`GET /api/manager/heatmap/{cellId}/drilldown` → `HeatmapDrilldownDto`/`DrilldownOutcomeGroup`, **own-cell-only** via the pre-existing `authorizeHeatmapCellAccess` → IDOR `404`; the drilldown's commitments use the B.20 envelope, `priority ASC, createdAt ASC`). **Reuse the 6.5a `PageEnvelope`.** Per 5.5b, dispute affordances are E3/E4-only, NOT E15 — confirm the drilldown's affordance posture. **Coordinate the B.11/B.12 contract with the FRESH web-orch** (it mirrors `dtos.ts`; the lead carried the 6.5b-contract-check duty into its handoff). Security-reviewer ad-hoc (E15 is a rule-#3 own-cell IDOR surface).
- **6.6 (brief 075)** — IC team-heatmap denial (REQ-F-030/UX-005) + the **manager-scope IDOR matrix** (every §6 denial case → 403/404 + audit). **Include the positive denial-audit assertion** the 6.5a security-reviewer flagged as the one optional LOW (the E13 IC-denial audit row). Security-reviewer ad-hoc (the IDOR matrix IS the security surface).
- **6.7 (brief 076)** — `ProjectionRebuildRunner` (REQ-D-013) — an internal `--app.job` runner (the §8/§23 one-shot pattern) that truncates+recomputes both tables from source via `ProjectionRefresher`/`RiskBadgeDeriver` (rebuild==incremental); the full **Appendix-E R1–R6 `rebuild==seed`** integration depends on the **HELD V5/V6 demo seed** (3.1b) — coordinate the seed.

## PINNED DECISIONS (carry forward)
- **`blocked_count = reconciliation_outcome=BLOCKED`** (lead-ruled, spec-determined by R5 + `rebuild==seed`) — NOT `work_type=BLOCKER`. Done at 6.2; §9 pinned.
- **§9 command-center `summary` field — DEFERRED, but now a KNOWN backend follow-up.** The user's QA surfaced a frontend styling-fidelity round (`docs/planning/024`, lead-authored, frontend territory); the **"At a glance" summary strip** (= the deferred manager SLA strip) is in the mockup. **The frontend will compute it CLIENT-SIDE for the demo now** (per doc 024) — but the **production §9 `summary`** (a server-computed team-level roll-up, accurate across pagination) is a **queued backend follow-up** (a B.11/B.20 envelope contract change + the ManagerQueryService roll-up; coordinate with the web-orch + the user). **DON'T build it now** — queue it as a Phase-6/post-6.5 backend item (it's in the existing Carry-forward SLA-strip bullet; reframe at your first orchestrate-end if you like). E13 ships the plain `Page<ManagerCommandCenterRowDto>`.

## CARRY-FORWARD highlights (full triaged list in MVP_TASKS)
- **MARK_REVIEWED read-affordance + real `unresolvedDisputeCount` on the plan-read (E3/E4)** — the projection side is done; the plan-read half needs manager-context threaded into `PlanMapper` (reuse the §35 `viewerIsDirectManager` threading). A Phase-6 plan-read slice (fold into 6.5b or dedicated).
- **§9 command-center `summary` field** (backend follow-up — above; the frontend's client-side demo version is doc 024).
- **`authorizeCommitmentManagerCapability` extraction** — still 2 instances (note-if-recurs at the 3rd; §33/§36).
- Low-pri: the partial-unique carry-forward backstop; the message-substring-classifier note-if-recurs; the JPA-auditing populator (unblocked standalone).

## CONVENTIONS (quick)
- **Brief lane:** backend **067–071 consumed** (landed); **073 authored+committed (pending dispatch)**; **072 = frontend 9.15** (web-orch); **backend next-free = 074.** ⚠ Coordinate numbers with the fresh `st6-main-wc-web-orchestrator` (we've had collisions — 072 was one, resolved by yielding). Qualify + re-pick next-free after any cross-track merge.
- **Shared-doc serialize (two-way)** with the frontend orch — claim the MVP_TASKS/ARCHITECTURE window before committing; explicit `git add` of backend/orch regions only, never `-A`; git-verify the shared tree clean before every seal. **Do NOT touch `docs/planning/024` or `docs/briefs/072` (frontend).**
- **Reviewer policy (user):** per-slice reviewers OFF; ad-hoc `security-reviewer` on rule-#3/IDOR slices (6.5a got a CLEAN PASS; run it on 6.5b/E15 + 6.6/IDOR; a light pass on 073). **NOTE:** the `security-reviewer` subagent type is NOT registered in this harness — invoke a `general-purpose` agent with a focused security-review prompt instead (that's how 6.5a's CLEAN PASS was run).
- **Contract-coordination pattern:** before committing a manager-read DTO, ping the fresh web-orch to verify its `dtos.ts`/`managerApi` mirrors the canonical B.11/B.12/B.20 (zero-drift) — it did for E13. Build against the canonical contract meanwhile.
- **Authz reuse surfaces (all exist):** `authorizeTeamHeatmapAccess` (coarse manager-only, E13/E14) + query-scoping by `manager_employee_id = principal.employeeId()` for IDOR-safety; `authorizeHeatmapCellAccess` (own-cell-only, E15 → IDOR `404`). The §35 `viewerIsDirectManager` threading for the plan-read affordance.
- **Backend gate** = `./gradlew check` from `apps/wc-api/`. Commit cadence: impl commits slice code (explicit `git add`, never `-A`); orch round commit lands docs. Push deferred (no remote). Co-Author trailer: `Claude Opus 4.8 (1M context)`.
- **Per-slice flow:** Step-2.5 review (`APPROVED.`/`TWEAK:`/`ADD:`), Step-9 commit-message-first routing, per-slice `/context-check st6-main` + lead ping after Step-10. The §37/§38/§39 + the api-layer-Criteria-query + the `PageEnvelope` patterns are the load-bearing 6.5b reuse surfaces.
