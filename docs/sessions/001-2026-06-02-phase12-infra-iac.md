# Session 001 — Phase 12 infrastructure IaC (Terraform + first 8 slices)

- **Date:** 2026-06-02
- **Phase:** 12 (AWS infrastructure, deployment & CI/CD) — `st6-infra` track, branch `infra-track`
- **Role:** implementer (`st6-infra-implementer`)
- **Predecessor session:** none (first infra session)
- **Successor session:** _(to be linked — fresh successor implementer picks up at 12.8)_
- **HEAD at close:** `dfa1448`
- **Close reason:** context cycle at the clean 12.7c boundary (team aggregate at WARN edge; 12.8 would breach HARD-STOP mid-slice). Nothing in flight.

## Why this session existed
Stand up the complete production-shaped AWS topology for the WC module as Terraform IaC under `infra/terraform/`, slice by slice via the `/tdd` discipline (infra variant: verify-not-test). Scope posture throughout: **author + VALIDATE only** — `terraform fmt`/`init -backend=false`/`validate`/`tflint` (+ throwaway-spike `plan` for negatives). No `terraform apply`/`plan` against AWS, no `kubectl apply` — the real apply/deploy is HITL (needs the user's AWS creds).

## What was built (10 commits, 8 task-slices)
| Commit | Slice | Summary |
|---|---|---|
| `15b743b` | 12.1 | Terraform root — S3 backend (partial) + dual-region providers (aws ~>6.0, k8s ~>3.0, helm ~>3.0) + root vars + `.gitignore` + multi-platform lockfile |
| `f81638c` | 12.2 | VPC + EKS (module v21) + 1 thin managed node group + IRSA OIDC + ALB controller (helm_release, chart 3.3.0) |
| `dd70573` | 12.3 | RDS PostgreSQL 16 — private, node-SG-only 5432, encrypted, `random_password` (Pattern 2) **(safety)** |
| `43a0cc9` | 12.4+12.5 | ECR (2 repos, IMMUTABLE+scan+lifecycle) + SNS/SQS + DLQ redrive (bundled) |
| `05fc6ab` | 12.6 | Private S3 + CloudFront (OAC, SPA 403/404 fallback) + 4 Secrets Manager secrets **(safety)** |
| `39a520e` | 12.7a | Route53 zone + ACM certs (CloudFront us-east-1, ALB regional) + CloudFront cert/alias attach + **re-enabled tflint `terraform_unused_declarations`** (closed the 12.1 carry-forward) |
| `79dcfac` | 12.7b | Per-workload IRSA least-privilege (api/worker/cronjob/migration) **(safety core)** |
| `da5d216` | 12.10 | CloudWatch log groups (for_each per workload, finite retention) — dispatched out of order (ruling-independent) |
| `dfa1448` | 12.7c | GitHub OIDC + hardened CI deploy role (guardrail permissions boundary, env-scoped) + EKS access entry; boundary threaded onto all 8 TF-created roles **(safety core)** |

### Files created (`infra/terraform/`)
- `versions.tf`, `backend.tf`, `main.tf`, `variables.tf`, `outputs.tf`, `.tflint.hcl`, `.gitignore`, `.terraform.lock.hcl` (12.1)
- `vpc.tf`, `eks.tf` (12.2) · `rds.tf` (12.3) · `ecr.tf`, `sns_sqs.tf` (12.4/12.5) · `s3_cloudfront.tf`, `secrets.tf` (12.6) · `route53_acm.tf` (12.7a) · `iam_irsa.tf` (12.7b) · `cloudwatch.tf` (12.10) · `iam_ci.tf` (12.7c)

### Files modified across slices
`outputs.tf` (append-only per slice), `variables.tf`, `versions.tf` (provider adds: random@12.3), `.terraform.lock.hcl` (re-locks), `.tflint.hcl` (mute removed @12.7a), `s3_cloudfront.tf` (cert attach @12.7a), `iam_irsa.tf` + `eks.tf` (boundary threading @12.7c).

## Decisions made
- **Provider/module majors** adopted from the LIVE registry (not memory): aws 6.x, kubernetes 3.x, helm 3.x; vpc 6.6.1, eks 21.23.0, iam 6.6.1; ALB chart 3.3.0; random 3.9.0. (LESSONS §4/§6.)
- **DB password (D-implicit):** Pattern 2 (`random_password` → RDS + the `db` secret), keeps Appendix D.2 intact; never an output (rule #7).
- **D1 (human):** full-apply CI, hardened defense-in-depth (permissions boundary + GitHub Environment + env-scoped OIDC). → 12.7c.
- **D2 (human):** external-dns owns the `api.wc` record (supersedes the 12.7a deferral). → lands in 12.8.
- **D3 (human) + refinement:** thread `ci_boundary` through EVERY TF-created role; realized as a **guardrail boundary** (`Allow *` + escalation Denies, NOT a service-allowlist) — the only correct way to thread without capping the ALB-controller (needs wafv2/shield/cognito-idp) or the unauditable AWS-managed eks roles. Recorded in `docs/decisions/001`.
- Thin posture throughout (single NAT, single-AZ RDS, no autoscaler, MVP teardown defaults) per RISK-009.

## Decisions explicitly NOT made (deferred)
- Real `terraform init`/`plan`/`apply` against AWS — **HITL** (the user's creds + the GitHub `production` Environment).
- The `api.wc` alias mechanism settled as external-dns (12.8), not a pure-TF record.
- Hardening items (Multi-AZ HA, DB migrate/app role separation, RDS deletion-protection/final-snapshot, AWS-ruleset tflint plugin, log-forwarding agent) — Phase 13 trims.

## TDD compliance
**Infra is verify-not-test** (root `CLAUDE.md` TDD posture) — no red-green loop. Every slice followed the substitute discipline: Step-2.5 design review (orch-approved) → author `.tf` → `fmt`/`init -backend=false`/`validate`/`tflint` rc=0 → Step-9 categorized routing → Step-10 commit. Negative guardrails (validation blocks, IAM least-privilege/escalation, no-cycle, OAC acyclicity) were **pre-verified in throwaway `/tmp` spikes** before each GREEN. **No violations.** Two safety slices (12.3, 12.6) + two safety cores (12.7b, 12.7c) had explicit rule-#5/#7 grep audits.

## Reachability
Production entry point for IaC = `terraform -chdir=infra/terraform apply` (HITL + the 12.11 CI pipeline). Every slice's resources are on that path; outputs are consumed downstream (12.8 SA annotations/ingress cert, 12.9 env injection, 12.11 image push/role-to-assume). No tested-but-unwired gaps. (The 12.10 log-forwarding-agent gap is a *documented* Phase-13 carry-forward, not an unwired feature.)

## Open follow-ups (Step-9 categorized — for `/orchestrate-end` verification; orchestrator writes, not me)
- **Convention candidates → infra/LESSONS.md §1–§13** (all routed hot): outputs.tf append-only; verify recipe; module-pin discipline; configure-provider-in-first-using-slice; author-against-current-major-schema; random_password→secret-slice + must-declare-random; ECR repo posture; secret-value split; CloudFront-default-cert-then-attach; ACM us-east-1-aliased-cert; per-workload IRSA least-privilege; hardened-CI guardrail-boundary (+ self-ref-ARN-constructed + node-group-per-node-group-boundary gotchas).
- **Carry-forwards:** tflint `unused_declarations` re-enable → **CLOSED @12.7a**. AWS-ruleset tflint plugin → Phase 13. Log-forwarding agent → Phase 13. Hardening set (Multi-AZ / role-separation / deletion-protection) → Phase 13.
- **Cross-slice contracts (for 12.8):** 12.7b↔12.8 SA-identity = namespace **`wc`**, SA names **`wc-api` / `wc-worker` / `wc-cronjob` / `wc-migration`** (the IRSA trust `sub` pins these). 12.7c↔12.8 = the **external-dns role MUST carry `permissions_boundary = aws_iam_policy.ci_boundary.arn`** (else CI's apply denies creating it). 12.6↔12.8 = SecretProviderClass jmesPath must match the secret JSON keys (db: spring.datasource.{url,username,password}; auth0: jwt.issuer-uri + auth0.audience; graph: GRAPH_TENANT_ID/CLIENT_ID/CLIENT_SECRET).
- **HITL runbook prereqs:** GitHub `production` Environment + required reviewers; the constructed `local.ci_boundary_arn` must equal the real ci_boundary policy ARN (apply verifies); state bucket/lock-table pre-created; hosted zone pre-registered; RDS minor + EKS version availability per region.
- **Architecture-doc edit (orchestrator):** Appendix C.7 external-dns component (per D2).

## RESUME STATE FOR THE SUCCESSOR IMPLEMENTER (read this first)
- **Next slice: 12.8** — k8s ServiceAccounts + SecretProviderClass + service + ingress + **external-dns** (per D2). **Brief 010 is authored + ready:** `docs/briefs/010-12.8-k8s-sa-spc-svc-ingress-externaldns.md`.
- **Install `kubeconform` at first use** (k8s manifest validation — not yet installed; `actionlint` also pending for 12.11).
- **Hard contracts to honor in 12.8:** the SA names/namespace above (must match the IRSA trust subs); the external-dns role carries `ci_boundary`; the ingress hostname annotation = `api.wc.${ROOT_DOMAIN}`; the SecretProviderClass per-SA exposes ONLY that SA's permitted secrets (worker→graph-only, cron/migration→db-only, api→db/auth0/graph).
- Then **12.9** (Deployments/CronJob/migration Job — flyway-enabled ONLY on the migration Job) → **12.11** (full §13 GitHub Actions pipeline + deployed smoke; needs `actionlint`).
- **Verify recipe (every infra slice):** spike-first in `/tmp` → `fmt -check -recursive` + `init -backend=false` + `validate` + `tflint` rc=0 (`unused_declarations` is ACTIVE — keep it green) + `kubeconform`/`actionlint` for k8s/CI. No `apply`/`plan`/`kubectl apply` agent-side (HITL). Strict explicit `git add` (never `-A`).

## How to use what was built
The full Terraform root validates at HEAD `dfa1448`. A HITL operator runs `terraform -chdir=infra/terraform init -backend-config=... && plan && apply` with real AWS creds (state bucket + lock table + hosted zone pre-created; `ROOT_DOMAIN` + `github_repo` + GitHub `production` Environment supplied). 12.8/12.9/12.11 + the deploy runbook complete the deployable path.
