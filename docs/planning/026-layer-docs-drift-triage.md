# Layer-Docs Drift Triage — ST6 Weekly Commit

> **Generated:** 2026-06-04, against `HEAD 3b919a9`.  
**Source:** the `/layer-docs` deep read in [`docs/layers/`](../layers/), then each finding **re-verified against the current code** and given a concrete fix recommendation.

A triage list you can hand to the team. Every item is a place where `ARCHITECTURE.md` (the binding contract) and the code disagree. Each was re-opened at the current HEAD to confirm it still holds, classified, and paired with a recommended fix + effort + how to verify it.

## How to read this

- **Category** — `CODE_FIX` (change code) · `DOC_FIX` (the code is right; edit `ARCHITECTURE.md`) · `NEEDS_DECISION` (a real contract mismatch — a human picks which side is authoritative) · `NO_ACTION` (correct by design, an already-accepted scope trim, or the finding was itself an error).
- **Priority** — `P0` breaks a shipped feature / safety; `P1` real gap with user-visible or operational impact; `P2` minor code gap or a load-bearing doc fix; `P3` trivial tidy-up / no-action.
- **Effort** — `S` < 0.5d · `M` 0.5–2d · `L` > 2d.    **Re-verified** — ✓ still present at HEAD · ⚠ already resolved or not reproducible.    **Safety** — which of the 7 Key safety rules the item touches (`—` = none; an adjacent-but-not-violated rule is noted in the item's *Safety note*).
- **Important nuance:** the two largest gaps are not bugs but **known, already-decomposed work that was never built** (`MVP_TASKS.md` Phase 7 for comments, task 8.9 for the Outlook retry surface). They are triaged as unbuilt features, not regressions. There are **no P0s** — the core weekly lifecycle and its safety rules are intact.

## Summary

**20 items** · by category: `CODE_FIX` 7 · `DOC_FIX` 4 · `NEEDS_DECISION` 1 · `NO_ACTION` 8  ·  by priority: `P0` 0 · `P1` 5 · `P2` 2 · `P3` 13

| Pri | Category | Item | Layer | Safety | Effort | At HEAD |
|-----|----------|------|-------|:------:|:------:|:------:|
| P1 | CODE_FIX | `comments-backend` — No backend HTTP surface for comments — CommentController/CommentService unbuilt; feature dead end-to-end | 08 Comments | #3 | M | ✓ |
| P1 | CODE_FIX | `e2e-selectors` — wc-e2e selectors.ts data-cy vocabulary does not match realized wc-web data-cy attributes (suite authored-not-green, would fail on green run) | 09 Frontend | — | M | ✓ |
| P1 | CODE_FIX | `problemdetail-fielderrors` — ProblemDetail fieldErrors wire shape: backend emits a JSON object (Map), B.21 + frontend expect an array of {field,code,message} | 02 API & Web | — | M | ✓ |
| P1 | CODE_FIX | `sync-retry` — Outlook sync read/retry endpoints (E22/E23) + FAILED→RETRY_REQUESTED state machine unimplemented | 06 Calendar Sync | #4 | M | ✓ |
| P1 | NEEDS_DECISION | `worker-redelivery` — Worker redelivery guard keys on graphEventId, not status; Graph adapter is CREATE-only (no update / no graph_event_id reuse) — §10 mismatch | 06 Calendar Sync | #4, #7 | M | ✓ |
| P2 | CODE_FIX | `calendar-deeplink` — Calendar event deep-link / body (WC_FRONTEND_BASE_URL) not wired into the Graph event | 06 Calendar Sync | #7, #4 | S | ✓ |
| P2 | CODE_FIX | `perf-seed-runner` — job-perf-seed.yaml passes --app.job=generate-perf-seed but no matching runner exists, and the manifest omits web-application-type=none so the Job hangs instead of completing | 07 Scheduled Jobs | — | M | ✓ |
| P3 | CODE_FIX | `migration-job-deadline` — Migration Job lacks activeDeadlineSeconds backstop unlike sibling generation/rebuild Jobs | 07 Scheduled Jobs | — | S | ✓ |
| P3 | DOC_FIX | `authz-403-404-doc` — §6 "403/404" shorthand is ambiguous for the not-own heatmap drill-down denial — code is unambiguously IDOR-safe 404 | 04 Authorization | #3 | S | ✓ |
| P3 | DOC_FIX | `authz-codes-doc` — ARCHITECTURE §6 prose names only 2 of the 7 authorizer-local 403 codes (B.21 already has all 7) | 04 Authorization | #3 | S | ✓ |
| P3 | DOC_FIX | `demo-failure-port` — §10 lists a "demo-failure" Graph adapter mode that has no code/value implementation | 06 Calendar Sync | #4 | S | ✓ |
| P3 | DOC_FIX | `five-routes-doc` — ARCHITECTURE §7 "five routes" count is loose vs realized AppRoutes tree | 09 Frontend | — | S | ✓ |
| P3 | NO_ACTION | `add-unplanned-doc` — ADD_UNPLANNED affordance precondition (ARCHITECTURE F.4 vs code/tests) | 03 Application & Lifecycle | — | - | ⚠ |
| P3 | NO_ACTION | `allowedaction-placeholders` — AllowedAction enum carries values not yet emitted by any resolver (RESPOND_DISPUTE, RESOLVE_DISPUTE, COMMENT, RETRY_SYNC) | 03 Application & Lifecycle | — | - | ⚠ |
| P3 | NO_ACTION | `aws-profile` — Deploy Spring profile is "aws", not "prod" (application-prod.yml is dead config) | 10 Infrastructure | — | - | ⚠ |
| P3 | NO_ACTION | `cloudwatch-forwarder` — CloudWatch log groups provisioned but no pod→CloudWatch log-forwarding agent | 10 Infrastructure | — | - | ✓ |
| P3 | NO_ACTION | `enum-count` — Layer-doc brief said "18 enums"; code + ARCHITECTURE.md both say 16 | 01 Domain | — | - | ⚠ |
| P3 | NO_ACTION | `mark-reviewed-doc` — MARK_REVIEWED affordance narrows documented granted-when (intentional §31 UX narrowing) | 02 API & Web | — | - | ✓ |
| P3 | NO_ACTION | `rule1-not-notnull` — Rule #1 (required SO at lock) is service-enforced, not a DB NOT NULL — by design | 01 Domain | #1 | - | ⚠ |
| P3 | NO_ACTION | `worker-irsa` — Worker IRSA scope reconciled to db+graph (doc and code agree) | 10 Infrastructure | #7 | - | ⚠ |

## Recommended fix queue (the important ones)

In priority order — the items that change code or need a decision. The `DOC_FIX` and `NO_ACTION` items are listed in full below but need no engineering work beyond a doc edit.

1. **`comments-backend`** (P1, effort M, safety #3) — No backend HTTP surface for comments — CommentController/CommentService unbuilt; feature dead end-to-end
2. **`e2e-selectors`** (P1, effort M) — wc-e2e selectors.ts data-cy vocabulary does not match realized wc-web data-cy attributes (suite authored-not-green, would fail on green run)
3. **`problemdetail-fielderrors`** (P1, effort M) — ProblemDetail fieldErrors wire shape: backend emits a JSON object (Map), B.21 + frontend expect an array of {field,code,message}
4. **`sync-retry`** (P1, effort M, safety #4) — Outlook sync read/retry endpoints (E22/E23) + FAILED→RETRY_REQUESTED state machine unimplemented
5. **`worker-redelivery`** (P1, needs decision, safety #4, #7) — Worker redelivery guard keys on graphEventId, not status; Graph adapter is CREATE-only (no update / no graph_event_id reuse) — §10 mismatch
6. **`calendar-deeplink`** (P2, effort S, safety #7, #4) — Calendar event deep-link / body (WC_FRONTEND_BASE_URL) not wired into the Graph event
7. **`perf-seed-runner`** (P2, effort M) — job-perf-seed.yaml passes --app.job=generate-perf-seed but no matching runner exists, and the manifest omits web-application-type=none so the Job hangs instead of completing
8. **`migration-job-deadline`** (P3, effort S) — Migration Job lacks activeDeadlineSeconds backstop unlike sibling generation/rebuild Jobs

## All items (detail)

### P1

#### `comments-backend` · CODE_FIX — No backend HTTP surface for comments — CommentController/CommentService unbuilt; feature dead end-to-end
**Layer:** 08 Comments · **Severity:** high · **Effort:** M · **Safety:** #3 · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE §11 + B.2 E20 (GET /api/comments) / E21 (POST /api/comments) + B.9 CommentDto specify a flat comment list/create surface gated IC(own)/Manager(direct-report, LOCKED+), with the authorizer resolving targetId→owning plan and returning IDOR-safe 404. MVP_TASKS Phase 7 (tasks 7.1-7.5) decomposes this; 7.4 is the controller/service.
- **Code does:** No CommentController/CommentService exists (find for *Comment*Controller*/*Service* → 0; api has 7 controllers, none for comments). The comment authorizer (DomainAuthorizationService.java:125,:135) and COMMENT allowedAction (AllowedAction.java:25) are dead — referenced only by tests/doc comments; AllowedActionResolver never emits COMMENT. Frontend commentsApi.ts hits /api/comments but only against MSW mocks; the real endpoint 404s.
- **Recommended fix:** Build Phase-7 tasks 7.2/7.4 backend: add CreateCommentRequest + CommentDto + CommentMapper (api/comment/dto, mapper), then CommentService + CommentController exposing GET /api/comments (B.20 envelope, createdAt ASC, page/size defaults 25/100) and POST /api/comments (forces parentCommentId=null/depth=0). Service calls the existing authorizeCommentTargetAccess chokepoint FIRST (rule #3 IDOR-safe 404), validates body per Appendix E (≤4000 code points, never logged §15), and emits COMMENT in AllowedActionResolver for owning-IC always + direct-manager when LOCKED+. Reject MANAGER_REVIEW/ALIGNMENT_DISPUTE targets (CHECK already forbids).
- **Verify:** Write failing MVC tests first (7.4 scenarios): IC POST→GET own plan comment returns B.20 envelope, depth=0/parentCommentId=null, createdAt ASC; manager POST to DRAFT direct-report plan denied; cross-IC/unseeable target → 404 + one AUTHORIZATION_DENIED audit (no body in metadata); unknown targetType → 400. Then ./gradlew check from apps/wc-api/ (JaCoCo ≥80%, IDOR matrix green). Confirm frontend commentsApi.ts works against the live endpoint, not just MSW.
- **Files:** `apps/wc-api/api/src/main/java/com/st6/wc/comment/CommentController.java`, `apps/wc-api/api/src/main/java/com/st6/wc/comment/CommentService.java`, `apps/wc-api/api/src/main/java/com/st6/wc/comment/dto/CreateCommentRequest.java`, `apps/wc-api/api/src/main/java/com/st6/wc/comment/dto/CommentDto.java`, `apps/wc-api/api/src/main/java/com/st6/wc/comment/mapper/CommentMapper.java`, `apps/wc-api/api/src/main/java/com/st6/wc/auth/DomainAuthorizationService.java`, `apps/wc-api/api/src/main/java/com/st6/wc/plan/AllowedActionResolver.java`
- **Notes:** The shared Comment entity, CommentRepository, comment table, and the IDOR-safe authorizer methods already exist (built in Phases 1-2) — only the HTTP/service/DTO layer (7.2/7.4) is missing, so the chokepoint plumbing is ready. Frontend (9.11b) is fully built and tested, making this purely a backend gap.

#### `e2e-selectors` · CODE_FIX — wc-e2e selectors.ts data-cy vocabulary does not match realized wc-web data-cy attributes (suite authored-not-green, would fail on green run)
**Layer:** 09 Frontend · **Severity:** medium · **Effort:** M · **Safety:** — · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md Appendix C.5 (line 883) defines apps/wc-e2e as "Cypress + Cucumber, 1:1 to REQ IDs" — 7 .feature files each tagged to REQ IDs (lines 890-896), asserting via UI. The harness intent (wc-e2e/CLAUDE.md "Lookup table" + selectors.ts header lines 1-12) makes selectors.ts the SINGLE source of selector truth: every targeted UI element exposes data-cy="<kebab-case>" and step defs import SEL/cySel and never inline raw selectors. C.5 itself is SILENT on the concrete data-cy vocabulary (selectors.ts:4-5 notes this and flags a candidate one-line C.5 addendum); the binding is owned by the harness, to be reconciled 1:1 with wc-web's real attrs once the frontend is runnable (selectors.ts:9-11, Carry-forward origin 11.7).
- **Code does:** Gap reproduced at HEAD. selectors.ts:13-59 defines ~30 keys; cross-checked against the 105 production data-cy values realized in apps/wc-web/src (test files excluded). Only ~6 of the ~30 keys actually consumed by step defs match a realized attribute: commitmentRow→commitment-row, commandCenter→command-center, disputePanel→dispute-panel, disputeResolve→dispute-resolve, heatmapCell→heatmap-cell, riskBadge→risk-badge. The rest MISMATCH: planView='plan-view' (realized weekly-plan-view, WeeklyPlanView.tsx:51); lockButton='lock-button' (LockButton.tsx:39 button has NO data-cy at all); errorBanner/errorCode/safeMessage='error-banner'/'error-code'/'safe-message' (realized per-form: lock-error+lock-error-message LockButton.tsx:49,53, form-error, etc — no global error-banner/error-code); directReportRow='direct-report-row' (realized cc-row, CommandCenter); heatmap='heatmap' (realized heatmap-grid/heatmap-table, HeatmapGrid.tsx); drilldownSoBreakdown='drilldown-so-breakdown' (realized drilldown-outcome-group, HeatmapCellDrilldown.tsx:79); supportingOutcomePicker='supporting-outcome-picker' (realized so-picker, SupportingOutcomePicker.tsx:59); disputeState='dispute-state' (realized dispute-status, DisputePanel.tsx:140-test); syncWarning='sync-warning' (realized sync-failed-warning, SyncStatusBadge.tsx:27); syncRetryButton/lifecycleStatus/reconcileOutcome/reviewState/baselineEdit/unplanned*/carryForward*/supportingOutcomeLink/reviewDueDate/disputeRespondSo also unrealized. personaSwitcher='persona-switcher' and accessDenied='access-denied' (authz.steps.ts:21,29,39) have ZERO realized data-cy — PersonaSwitcher.tsx exists but emits no data-cy; no access-denied attribute exists anywhere in wc-web. selectors.ts:9-11 self-flags authored-not-green; cypress.config.ts:13-15 confirms no running app so green run is deferred to 11.9 CI / Phase-12 smoke. Timing corroborates drift: selectors.ts last touched 2026-06-02; wc-web/src evolved through 2026-06-04 (8+ later commits including data-cy-bearing fidelity reworks).
- **Recommended fix:** Execute the already-scheduled 11.7 Carry-forward reconciliation: align selectors.ts keys 1:1 with the realized wc-web data-cy attributes. Concretely, in apps/wc-e2e/cypress/support/selectors.ts remap the ~24 mismatched values (planView→weekly-plan-view, lockButton→add+target a real button data-cy, errorBanner/errorCode/safeMessage→the per-surface *-error/*-error-message family or a normalized scheme, directReportRow→cc-row, heatmap→heatmap-grid, drilldownSoBreakdown→drilldown-outcome-group, supportingOutcomePicker→so-picker, disputeState→dispute-status, syncWarning→sync-failed-warning, lifecycleStatus→plan-lifecycle-bar, reconcileOutcome→reconciliation-outcome-form, supportingOutcomeLink→so-breadcrumb/rcdo-breadcrumb, etc.). Two values have NO realized counterpart and need a wc-web change (cross-area, route via orchestrator since wc-e2e CLAUDE.md forbids editing apps/wc-web/**): add data-cy=\"persona-switcher\" to standalone/PersonaSwitcher.tsx and a data-cy=\"access-denied\" element on the 404/denial surface (or repoint the steps to the realized empty/error-state). Add the one-line C.5 addendum selectors.ts:5 already proposes so the data-cy convention is contract-anchored. Effort M (mechanical remap is S; the 2 wc-web additions + a green run on the Compose stack push it to M). Touch: apps/wc-e2e/cypress/support/selectors.ts (primary), apps/wc-e2e/cypress/support/step_definitions/authz.steps.ts (if repointing), apps/wc-web/src/standalone/PersonaSwitcher.tsx + the denial surface (orchestrator-routed), ARCHITECTURE.md Appendix C.5 (one-line addendum).
- **Verify:** Green run on the local Compose stack (11.9) / deployed smoke subset (Phase 12): `yarn cypress run` from apps/wc-e2e with the app + API up — all 7 features pass (currently they would fail on ~24 of ~30 selectors). Cheaper interim check now: a static cross-check that every cySel(...) value in selectors.ts matches a data-cy literal present in apps/wc-web/src (grep both sets, assert zero orphans); plus `npx tsc --noEmit` stays clean (it already does — the mismatch is runtime, not compile-time, which is why the authored-not-green gate masks it).
- **Files:** `apps/wc-e2e/cypress/support/selectors.ts`, `apps/wc-e2e/cypress/support/step_definitions/authz.steps.ts`, `apps/wc-web/src/standalone/PersonaSwitcher.tsx`, `apps/wc-web/src/features/plan/WeeklyPlanView.tsx`, `apps/wc-web/src/features/plan/LockButton.tsx`, `ARCHITECTURE.md`
- **Safety note:** none (the suite asserts safety rules #1/#3/#4/#6 behaviors, but this is a test-binding drift, not a safety-invariant violation itself)
- **Notes:** This is a real, currently-latent code gap, not a false finding: tsc passes because SEL is internally consistent, but a green run would fail on the majority of features (~24 of ~30 consumed selectors mismatch, including 2 with no realized counterpart). Severity stays medium / P1 (not P0) because the suite is intentionally authored-not-green by design (cypress.config.ts:13-15, wc-e2e/CLAUDE.md TDD-protocol), green runs are explicitly deferred to 11.9/Phase-12, and the reconciliation is an already-tracked Carry-forward item (selectors.ts:9, origin 11.7) — so this is scheduled debt, not a regression. Categorized CODE_FIX (not DOC_FIX) because the test binding is wrong vs the realized UI and Appendix C.5 is deliberately silent on the vocabulary; the harness owns reconciliation. Note the scope boundary: the selectors.ts/authz.steps.ts edits are in-area for wc-e2e, but the PersonaSwitcher/access-denied data-cy additions are wc-web cross-area edits that the wc-e2e CLAUDE.md forbids the e2e implementer from making — must be orchestrator-routed to the wc-web area.

#### `problemdetail-fielderrors` · CODE_FIX — ProblemDetail fieldErrors wire shape: backend emits a JSON object (Map), B.21 + frontend expect an array of {field,code,message}
**Layer:** 02 API & Web · **Severity:** medium · **Effort:** M · **Safety:** — · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md B.21 (ARCHITECTURE.md:681 prose says `fieldErrors?[]`; the concrete example at ARCHITECTURE.md:694-697) pins fieldErrors as a JSON ARRAY of `{field, code, message}` objects, e.g. `[{ "field": "commitments[3].supportingOutcomeId", "code": "REQUIRED", "message": "Supporting Outcome is required." }]`. Each entry carries a per-error `code` distinct from the human `message`.
- **Code does:** Backend emits fieldErrors as a JSON OBJECT (field→message), NOT an array, and with no per-error `code`. ProblemDetailsExceptionHandler.java:202-206 (`renderValidation`) and :151-163 (`handleUnlinked`) call `body.setProperty("fieldErrors", <Map<String,String>>)`; the maps are built at :70-73, :86-92, :100, :107, and ValidationException.java:16 / UnlinkedPlannedCommitmentException.java:16 / UnplannedMissingLinkAtCloseException.java:20 — all `Map<String,String>`. The backend's own test confirms the object shape: PlanLockEndpointTest.java:211 asserts `$.fieldErrors['<commitment-uuid>']` (object-key access). Frontend problemDetails.ts:27-43 (`parseFieldErrors`) requires `Array.isArray(value)` and returns `[]` otherwise, mapping each item to `{field, code, message}` (problemDetails.ts:8-12). So against a REAL backend response the array check fails → fieldErrors silently parses to `[]` → per-field messages never render (LockButton.tsx:54-56, CommitmentForm.tsx:189-191, ReconciliationOutcomeForm.tsx:109-111, etc.). safeMessage + code still render (set separately in ProblemDetailFactory.java:33,36). Frontend tests pass only because their mock fixtures (LockButton.test.tsx:59) hand-author the array/contract shape the backend never actually produces.
- **Recommended fix:** Change the backend to emit the B.21 array shape (frontend + contract already agree on it; the backend is the divergent side). Replace the `Map<String,String>` carriers with an ordered `List<FieldErrorDto>` record `{field, code, message}`: (1) add a `FieldErrorDto` record in api/web; (2) change `ValidationException`, `UnlinkedPlannedCommitmentException`, `UnplannedMissingLinkAtCloseException` to hold `List<FieldErrorDto>`; (3) in ProblemDetailsExceptionHandler, build entries with a real per-error `code` — for the @Valid path use the Bean-Validation constraint name (`fe.getCode()` from FieldError) as `code` and `fe.getDefaultMessage()` as `message`; for the unlinked/close paths set `code=REQUIRED` (or the existing per-violation constraint string) and keep the commitment-id inside `field` (e.g. `commitments[<id>].supportingOutcomeId`) so no information is lost when flattening the id-keyed map to an array; (4) `setProperty("fieldErrors", List.of(...))`. Update PlanLockEndpointTest / CloseReconciliationEndpointTest / *ServiceTest assertions from object-key (`$.fieldErrors['<id>']`) to array-element (`$.fieldErrors[?(@.field=='...')]` or index). Frontend needs NO change. Alternatively (smaller, but a doc concession that loses per-error `code` and the array ordering) re-spec B.21 + change the frontend parser to read an object — NOT recommended, since the array shape is the load-bearing one already wired through ~8 frontend surfaces.
- **Verify:** Add a backend endpoint test that asserts the array shape end-to-end: `$.fieldErrors` is a JSON array, `$.fieldErrors[0].field`, `$.fieldErrors[0].code`, and `$.fieldErrors[0].message` all exist (replacing PlanLockEndpointTest.java:211's `$.fieldErrors['<id>']` object-key assertion). Run `./gradlew test` from apps/wc-api/. On the frontend, the existing problemDetails.test.ts already pins the array contract — confirm it still passes with `yarn nx test wc-web`. A contract round-trip check (a frontend fixture generated from a captured real backend body) would prevent regression of the mock-vs-real divergence.
- **Files:** `apps/wc-api/api/src/main/java/com/st6/wc/web/ProblemDetailsExceptionHandler.java`, `apps/wc-api/api/src/main/java/com/st6/wc/web/ValidationException.java`, `apps/wc-api/api/src/main/java/com/st6/wc/web/UnlinkedPlannedCommitmentException.java`, `apps/wc-api/api/src/main/java/com/st6/wc/web/UnplannedMissingLinkAtCloseException.java`, `apps/wc-api/api/src/test/java/com/st6/wc/plan/PlanLockEndpointTest.java`, `apps/wc-api/api/src/test/java/com/st6/wc/plan/CloseReconciliationEndpointTest.java`, `apps/wc-web/src/shared/lib/problemDetails.ts`
- **Safety note:** none (rule #1 required-SO-at-lock is the feature affected — its rejection still works via safeMessage; only the per-field detail of the UNLINKED_PLANNED_COMMITMENT response degrades. No safety invariant is broken.)
- **Notes:** Genuine, still-present mismatch at HEAD. Classified CODE_FIX rather than NEEDS_DECISION because BOTH the spec (B.21 array) AND the frontend (problemDetails.ts) already agree on the array shape — only the backend diverges, and its own test encodes the wrong (object) shape. Severity medium not high: the affected feature (rule-#1 lock rejection) still functions — safeMessage + code render correctly, so the user is told WHY the lock failed; only the per-field commitment-level detail is silently dropped (degrades to an empty list, no crash). User-visible impact = missing per-field hints across ~8 form surfaces (Lock, Commitment create/patch, ReconciliationOutcome, CarryForward, Delete, MarkReviewed, SyncRetry). Watch the safety boundary when reshaping: keep the commitment id inside `field` (the backend currently keys by commitment id, not array index — ARCHITECTURE.md uses array index in its example; preserving the id is more useful and is rule-#7-safe since ids are non-PII) and never echo untrusted user text into `message` (the existing handlers already use safe constants / field names only — preserve that).

#### `sync-retry` · CODE_FIX — Outlook sync read/retry endpoints (E22/E23) + FAILED→RETRY_REQUESTED state machine unimplemented
**Layer:** 06 Calendar Sync · **Severity:** high · **Effort:** M · **Safety:** #4 · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE §10 (:193) + B.2 E22 `GET /api/outlook-sync?planId=` / E23 `POST /api/outlook-sync/{id}/retry` + retry diagram (:1357): a read-list plus a manual retry flipping `FAILED→RETRY_REQUESTED` and re-publishing the same pointer via the single SnsLifecyclePublisher path → `QUEUED`. MVP_TASKS 8.9 names NEW SyncController/OutlookSyncRecordDto/SyncRecordMapper + a SyncRecordService retry method.
- **Code does:** Publish-side exists (SnsLifecyclePublisher.publish PENDING_PUBLISH→QUEUED, create-only SyncRecordService, worker listener/adapter), but no SyncController/DTO/mapper and no retry method exist; grep for any sync/retry/outlook request `Mapping(` returns nothing. `RETRY_REQUESTED` lives only in the enum, V1 CHECK constraint, and vocab test — no production code writes it, so FAILED is terminal at runtime; `AllowedAction.RETRY_SYNC` is unwired.
- **Recommended fix:** Implement MVP_TASKS 8.9: add `SyncController` (E22 list bounded-per-plan via DomainAuthorizationService ownership 404-IDOR; E23 retry requiring `status=FAILED`, owner-IC-or-managing-manager, else 404+audit), `OutlookSyncRecordDto` (B.10 verbatim + `safeMessage`/`weekStartDate`/`retryCount`/`allowedActions[RETRY_SYNC iff FAILED+authorized]`), `SyncRecordMapper`, and a `SyncRecordService.requestRetry(id)` that sets FAILED→RETRY_REQUESTED then calls the existing `SnsLifecyclePublisher.publish` (single path, @Version-guarded, no duplicate row). Files: NEW `apps/wc-api/api/src/main/java/com/st6/wc/sync/SyncController.java`, `.../sync/dto/OutlookSyncRecordDto.java`, `.../sync/mapper/SyncRecordMapper.java`; extend `SyncRecordService.java` + `DomainAuthorizationService.java`.
- **Verify:** After implementing, `grep -rn "outlook-sync" apps/wc-api/api/src/main` shows the two mappings; new tests cover happy (owner lists; manager retries FAILED direct-report record FAILED→RETRY_REQUESTED→QUEUED reusing the id), edge (allowedActions omits RETRY_SYNC for non-FAILED), error (unowned retry→404+audit, non-FAILED retry→409); `./gradlew check` green; concurrent double-retry guarded by @Version (one republish).
- **Files:** `apps/wc-api/api/src/main/java/com/st6/wc/sync/SyncRecordService.java`, `apps/wc-api/api/src/main/java/com/st6/wc/sns/SnsLifecyclePublisher.java`, `apps/wc-api/api/src/main/java/com/st6/wc/auth/DomainAuthorizationService.java`, `apps/wc-api/api/src/main/java/com/st6/wc/action/AllowedAction.java`, `apps/wc-api/shared/src/main/java/com/st6/wc/enums/SyncStatus.java`, `ARCHITECTURE.md`, `MVP_TASKS.md`
- **Notes:** Genuine unbuilt feature, not stale doc: MVP_TASKS 8.9 + line-901 retry task are unchecked `[ ]` and the named files are absent. P1 (not P0) — core lifecycle and the publish path work and are non-blocking per rule #4; this is the missing user-visible recovery affordance for FAILED records, not a safety break.

#### `worker-redelivery` · NEEDS_DECISION — Worker redelivery guard keys on graphEventId, not status; Graph adapter is CREATE-only (no update / no graph_event_id reuse) — §10 mismatch
**Layer:** 06 Calendar Sync · **Severity:** medium · **Effort:** M · **Safety:** #4, #7 · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §10 (:193): "Worker redelivery guard: ... the worker loads the row and treats SYNCED/active-SYNCING as a no-op; it only (re)attempts Graph when status ∈ {QUEUED, RETRY_REQUESTED}, reusing graph_event_id to UPDATE rather than create." Reinforced by the §10 sequence diagram (:1268 "guard: SYNCED/active-SYNCING => no-op"; :1270 "create/update event (reuse graph_event_id if present)") and the OutlookSyncRecordDto contract (:601 graphEventId "present on SYNCED; reused on retry to update not create"). The §22/file-map even names the intended seam createOrUpdateEvent(SyncContext) (:836).
- **Code does:** At HEAD 3b919a9: SyncMessageListener.onMessage guards ONLY on graphEventId (SyncMessageListener.java:64 `if (record.getGraphEventId() != null) return;`) — there is no status check and no SYNCING-no-op branch; it transitions QUEUED-or-anything→SYNCING (:69-71) then unconditionally calls graphPort.createEvent(record) (:74). The port exposes only createEvent (GraphCalendarPort.java:16); GraphCalendarAdapter.createEvent always calls gateway.createEvent(...) (GraphCalendarAdapter.java:55) and MsGraphEventGateway always does graphClient...events().post(event) to CREATE (MsGraphEventGateway.java:34) — no update branch, graph_event_id is never threaded into the gateway. Because graphEventId is set only AFTER a successful create (SyncMessageListener.java:76), a record left in SYNCING (worker crash mid-flight, or genuine SQS redelivery of an in-flight message) has graphEventId==null → the guard passes → a SECOND post() fires → a duplicate calendar event. The unit test pins exactly this CREATE-only/graphEventId-guard behavior (SyncMessageListenerTest.java:62-71, :75-85), so the code is internally consistent and green — it simply enforces a weaker guard than §10.
- **Recommended fix:** Pick the authoritative side (human decision): (A) CODE_FIX to meet §10 (recommended) — change the guard to status-based: no-op on SYNCED and active-SYNCING (e.g. SYNCING && lastAttemptAt within a freshness window, else treat a stale SYNCING as re-attemptable), and (re)attempt Graph only when status ∈ {QUEUED, RETRY_REQUESTED}; add an update path: widen the port to createOrUpdateEvent(record) (or add updateEvent), have the adapter/gateway PATCH the existing event via graph_event_id when present (events().byEventId(id).patch(event)) and POST only when null. Files: SyncMessageListener.java, GraphCalendarPort.java, GraphCalendarAdapter.java, MsGraphEventGateway.java, DemoSuccessGraphCalendarPort.java + SyncMessageListenerTest/GraphCalendarAdapterTest. Effort M. OR (B) DOC_FIX — if the team affirms the shipped graphEventId-only guard is the intended MVP posture (LESSONS §44 calls it "skip on the strongest already-done signal"), narrow §10:193 + the diagram (:1268-1270) + DTO note (:601) + file-map (:836) to describe the graphEventId-guarded create-only behavior and drop the {QUEUED,RETRY_REQUESTED}/update language. Effort S.
- **Verify:** For CODE_FIX path: add SyncMessageListenerTest cases — (1) a record in SYNCING with graphEventId==null and a fresh lastAttemptAt → verifyNoInteractions(graphPort) (active-SYNCING no-op, the duplicate-create regression); (2) a record in QUEUED with a non-null graphEventId → the gateway UPDATE (patch) branch is invoked, not post; (3) a RETRY_REQUESTED record → re-attempts. Then `./gradlew :worker:test` from apps/wc-api/ green, plus full `./gradlew check`. For DOC_FIX path: no test; the existing green SyncMessageListenerTest IS the proof the documented behavior matches code.
- **Files:** `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/SyncMessageListener.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/GraphCalendarPort.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/GraphCalendarAdapter.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/MsGraphEventGateway.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/DemoSuccessGraphCalendarPort.java`, `apps/wc-api/worker/src/test/java/com/st6/wc/worker/sync/SyncMessageListenerTest.java`, `ARCHITECTURE.md`
- **Safety note:** #4 (Outlook sync failure/behavior never blocks the core lifecycle — adjacent; the duplicate-create path does not threaten the core txn) and #7 (pointer-only payload, observed intact). The redelivery-idempotence contract itself is NOT one of the 7 safety rules.
- **Notes:** Real-world blast radius is narrow: a duplicate event needs an SQS redelivery (or worker crash) to land specifically while a record is in the brief SYNCING window before the Graph create returns (graphEventId still null). The happy path and SYNCED-redelivery are already idempotent. graph_event_id-reuse/update only matters once that guard exists (today nothing ever re-attempts a record that has a graphEventId). The CREATE-only gateway is otherwise correct for the first attempt. Rule #7 (pointer-only payload, no-PII failure fields) and rule #4 (no rollback into the core txn) are both intact and unaffected by either resolution. The finding's cited file/line drifted (code now uses a port/adapter/gateway seam from Wave-2 s8/s9) but the substance reproduces exactly.

### P2

#### `calendar-deeplink` · CODE_FIX — Calendar event deep-link / body (WC_FRONTEND_BASE_URL) not wired into the Graph event
**Layer:** 06 Calendar Sync · **Severity:** low · **Effort:** S · **Safety:** #7, #4 · **Re-verified:** ✓ present

- **Spec says:** §10 (ARCHITECTURE.md:195): "Deep links: the worker gets WC_FRONTEND_BASE_URL (= https://wc.${ROOT_DOMAIN}) via config; IC events → {base}/weekly-commit[/history/{planId}], review-block → {base}/manager/command-center." Appendix C.3 (ARCHITECTURE.md:841) names a dedicated worker class: "graph/DeepLinkBuilder.java  # WC_FRONTEND_BASE_URL → /weekly-commit[/history/{planId}] | /manager/command-center". Appendix D.3 (ARCHITECTURE.md:1012) lists app.outlook.frontend-base-url (WC_FRONTEND_BASE_URL) as an R (required) worker env: "deep links written to Graph events ... IC events → {base}/weekly-commit[/history/{planId}]; review-block → {base}/manager/command-center (§10)", with the matching api-side parity row at D.2 (ARCHITECTURE.md:991).
- **Code does:** Reproduced at HEAD 3b919a9. GraphCalendarAdapter.buildSpec (apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/GraphCalendarAdapter.java:64-72) builds a CalendarEventSpec from subject+start+end+recordId only — no body, no deep link, never reads WC_FRONTEND_BASE_URL. CalendarEventSpec (CalendarEventSpec.java:14) is a 4-field record (String subject, Instant start, Instant end, UUID recordId) with no body field. MsGraphEventGateway.createEvent (MsGraphEventGateway.java:29-36) sets only setSubject/setStart/setEnd on the Graph Event — never setBody/ItemBody. No DeepLinkBuilder class exists in the worker tree (ls of .../worker/sync/ shows no such file). A repo-wide grep for DeepLinkBuilder|setBody(on the event)|frontend-base-url|WC_FRONTEND_BASE_URL across shared/api/worker source + worker ymls returns zero relevant hits (only unrelated JWT-test setBody, a Comment-entity setBody, and the /api/manager/command-center route). GraphRealModeConfig (GraphRealModeConfig.java:42-73) injects only the 3 GRAPH_* creds — never frontend-base-url. The s9 brief (docs/briefs/095) never mentions deep links/body — the surface was untouched, not removed; a faithful rule-#7 simplification but a real §10 feature gap.
- **Recommended fix:** Wire the deep link as the Graph event body (1) Add a String body (or deepLink) field to CalendarEventSpec. (2) Add worker class graph/DeepLinkBuilder reading app.outlook.frontend-base-url (WC_FRONTEND_BASE_URL); from the sync record's eventKind/relatedType build {base}/weekly-commit[/history/{planId}] for IC events (IC_PLANNING/IC_RECONCILIATION) and {base}/manager/command-center for the MANAGER_REVIEW_BLOCK. (3) In GraphCalendarAdapter.buildSpec, populate the body from DeepLinkBuilder (keep it non-PII: URL only, no OKR/commitment text — rule #7). (4) In MsGraphEventGateway.createEvent, set event.setBody(new ItemBody) with BodyType.Html/Text containing the link. (5) Add app.outlook.frontend-base-url to worker application.yml (default https://wc.${ROOT_DOMAIN}) + the aws-profile configtree binding; keep a non-blocking fallback (blank base ⇒ skip body, never throw — rule #4). Files: apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/{CalendarEventSpec.java,GraphCalendarAdapter.java,MsGraphEventGateway.java}, new DeepLinkBuilder.java, apps/wc-api/worker/src/main/resources/application.yml (+ application-aws.yml).
- **Verify:** Add a DeepLinkBuilderTest: IC_PLANNING/IC_RECONCILIATION record → body URL = {base}/weekly-commit (and /history/{planId} when a planId is available); MANAGER_REVIEW_BLOCK record → {base}/manager/command-center; blank base ⇒ no body, no throw (rule #4). Extend the GraphCalendarAdapter unit test to assert the built CalendarEventSpec carries the expected deep-link body, and the MsGraphEventGateway test (mocked GraphServiceClient) to assert event.getBody() content equals the deep link. Add a rule-#7 assertion that the body contains only the URL (no commitment/OKR text). Run ./gradlew check from apps/wc-api/.
- **Files:** `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/CalendarEventSpec.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/GraphCalendarAdapter.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/MsGraphEventGateway.java`, `apps/wc-api/worker/src/main/resources/application.yml`, `ARCHITECTURE.md`
- **Safety note:** #7 (no-secret/PII-leak + pointer-only) — adjacent: the deep link is a derived non-PII URL, so it is rule-#7-safe by construction; the fix must keep it body-text-only with no commitment/OKR/PII. Also #4-adjacent: deep-link build must stay inside the non-blocking sync path (a build/config miss must never throw into the core lifecycle).
- **Notes:** Genuine reproducible CODE_FIX, not doc-stale: the spec is load-bearing and self-consistent across §10 + Appendix C.3 (names DeepLinkBuilder.java) + D.2/D.3 (WC_FRONTEND_BASE_URL marked R=required in BOTH api and worker). Severity LOW / priority P2: calendar sync is the visible-but-non-blocking surface (safety rule #4 — never blocks the IC lifecycle); the event already has a non-PII subject, so the only loss is a clickable back-link from the calendar event into the app — convenience, not correctness/safety. If the team would rather defer permanently (the calendar deep-link is demo-cosmetic), the alternative is a DOC_FIX trimming the §10 deep-link sentence + the C.3 DeepLinkBuilder line + relaxing the two D.2/D.3 R→O markers — but given the spec's prescriptiveness and the small S effort, implementing is the cleaner reconciliation. WC_FRONTEND_BASE_URL is already specified as required worker env, so leaving it unread is a latent config-contract gap regardless.

#### `perf-seed-runner` · CODE_FIX — job-perf-seed.yaml passes --app.job=generate-perf-seed but no matching runner exists, and the manifest omits web-application-type=none so the Job hangs instead of completing
**Layer:** 07 Scheduled Jobs · **Severity:** medium · **Effort:** M · **Safety:** — · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §F.6 (line 1212) pins the contract: one opt-in synthetic perf seed, k8s Job `job-perf-seed.yaml` runs the `wc-api` image with arg `--app.job=generate-perf-seed` (Gradle task `generatePerfSeed` for local runs), isolated `perf+<n>@st6demo.com` namespace, scoped-DELETE teardown. The perf-seed shape section (lines 1137-1146, REQ-D-008) requires it to write ~2,000 `manager_plan_summary`/`manager_heatmap_cell` projection rows (≈40 reports × 10 weeks × 5 commitments) to back the §14 p95 < 200ms measurement of `GET /plans/current` and `GET /api/manager/command-center`; deterministic fixed RNG seed; fixed-namespace UUIDs idempotent via `ON CONFLICT DO NOTHING`; reuses the seeded read-only RCDO. The arch tree (line 937) also lists the manifest as "opt-in 2,000-record synthetic seed (REQ-D-008)." For the `--app.job=generate-perf-seed` arg to do anything, a runner bean gated on that property must exist, and (per the §12 / LESSONS §22 termination contract) the Job must pass `--spring.main.web-application-type=none` so it terminates.
- **Code does:** RE-VERIFIED at HEAD. apps/wc-api/api/src/main/java/com/st6/wc/job/ contains exactly three classes — PlanShellGenerationRunner.java (`@ConditionalOnProperty(name="app.job", havingValue="generate-plan-shells")`, line 30), ProjectionRebuildRunner.java (`havingValue="rebuild-projections"`, line 23), and the PlanShellGenerator helper. A grep for `app.job` across the package returns ONLY those two havingValue strings; there is NO `@ConditionalOnProperty(app.job=generate-perf-seed)` runner and NO PerfSeed generator anywhere under apps/wc-api/ (the only "synthetic" hits are unrelated worker Graph-port test/main files). So `infra/k8s/job-perf-seed.yaml:34-35` (`args: - "--app.job=generate-perf-seed"`) wires no bean — the container would boot a normal `wc-api` web app and write zero synthetic projection rows. Separately, job-perf-seed.yaml:34-35 passes ONLY `--app.job=...` (env profiles `aws,generation`, line 38) and, unlike job-rebuild-projections.yaml:40-42 which passes both `--app.job=rebuild-projections` AND `--spring.main.web-application-type=none`, omits the web-application-type=none arg and also lacks `activeDeadlineSeconds` (rebuild Job has 600 at line 28). Per the already-banked LESSONS §22, such a Job starts a web server and never reaches Complete (hangs to backoffLimit). The Job is annotated `wc.st6/excluded-from-deploy: "true"` (lines 19-20) and .github/workflows/deploy.yml:21 confirms it is deliberately NOT applied, so normal deploys are unaffected.
- **Recommended fix:** Add the missing runner so the contracted arg is functional, and fix the manifest's termination args. (1) Create apps/wc-api/api/src/main/java/com/st6/wc/job/PerfSeedRunner.java — an ApplicationRunner `@ConditionalOnProperty(name="app.job", havingValue="generate-perf-seed")` delegating to a new PerfSeedGenerator that writes ~2,000 manager_plan_summary/manager_heatmap_cell rows for one synthetic manager (~40 reports × 10 weeks × 5 commitments across the 3 DOs), reusing the seeded RCDO, deterministic via a fixed RNG seed, fixed-namespace UUIDs, idempotent `ON CONFLICT DO NOTHING`, into the isolated `perf+<n>@st6demo.com` namespace. (2) Add the `generatePerfSeed` Gradle task in the api module per §F.6 (parity with the existing generate-plan-shells / rebuild-projections local tasks). (3) Edit infra/k8s/job-perf-seed.yaml: add `- "--spring.main.web-application-type=none"` after the existing arg and add `activeDeadlineSeconds` to the Job spec (mirror job-rebuild-projections.yaml). Reconfirm the `aws,generation` profile + flyway-disabled env are correct for a data-seeding (non-migrating) Job. Files: NEW apps/wc-api/api/src/main/java/com/st6/wc/job/PerfSeedRunner.java + PerfSeedGenerator.java; extended apps/wc-api/api/build.gradle (Gradle task); extended infra/k8s/job-perf-seed.yaml.
- **Verify:** Mirror the existing runner gating tests: add a PerfSeedRunnerGatingTest asserting the bean is present ONLY under `app.job=generate-perf-seed` (and absent on a normal web boot, like PlanShellGenerationRunnerTest), plus a generator test asserting ~2,000 projection rows in the `perf+<n>@st6demo.com` namespace and idempotency on rerun (Testcontainers). For the manifest, run `kubeconform` on job-perf-seed.yaml and grep-audit that it carries both `--app.job=generate-perf-seed` and `--spring.main.web-application-type=none` (the LESSONS §22 audit). End-to-end: applying the Job manually reaches Complete (not hang) and the §14 p95 perf run sees ~2,000 rows.
- **Files:** `infra/k8s/job-perf-seed.yaml`, `apps/wc-api/api/src/main/java/com/st6/wc/job/PlanShellGenerationRunner.java`, `apps/wc-api/api/src/main/java/com/st6/wc/job/ProjectionRebuildRunner.java`, `ARCHITECTURE.md`
- **Notes:** Two distinct defects in one item. (a) Missing runner: confirmed only generate-plan-shells + rebuild-projections runners exist; generate-perf-seed has no backing bean, so the contracted Job is a no-op. (b) Missing web-application-type=none + activeDeadlineSeconds: the manifest would hang to backoffLimit; this exact failure mode is already banked as infra LESSONS §22, whose audit explicitly says "audit every --app.job Job for the arg" — so the manifest half is a known-pattern miss that escaped that audit (likely because the Job is excluded-from-deploy). Severity medium / P2, not P0: the whole feature is opt-in and excluded-from-deploy (job-perf-seed.yaml:19-20, deploy.yml:21), so no normal deploy or user-facing path is broken — impact is that the §14 perf-measurement tooling is non-functional if invoked. The manifest-only fix (arg + deadline) is S and could ship independently; the runner+generator is the bulk of the M effort. No safety rule implicated.

### P3

#### `migration-job-deadline` · CODE_FIX — Migration Job lacks activeDeadlineSeconds backstop unlike sibling generation/rebuild Jobs
**Layer:** 07 Scheduled Jobs · **Severity:** low · **Effort:** S · **Safety:** — · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §12/§13/Appendix D.5 (lines 205, 211, 1036-1047) do not mandate activeDeadlineSeconds on any Job. The project's own infra LESSON §22 says "Always pass it + activeDeadlineSeconds; audit every --app.job Job," and both sibling one-shot Jobs set it.
- **Code does:** job-migration.yaml sets only backoffLimit: 2 (line 19) with no activeDeadlineSeconds, while cronjob-generation.yaml (25-26) and job-rebuild-projections.yaml (27-28) both set activeDeadlineSeconds: 600. Low impact: application-flyway-migrate.yml sets web-application-type: none (line 11), so the container self-exits.
- **Recommended fix:** Add activeDeadlineSeconds: 600 under spec: in job-migration.yaml (next to backoffLimit: 2, line 19) to match the two sibling Jobs and satisfy infra LESSON §22. Pure consistency/defensive backstop (guards a wedged Flyway advisory-lock against a busy RDS); no architecture-doc edit needed since the contract is silent on it.
- **Verify:** terraform fmt -check -recursive && tflint; kubeconform job-migration.yaml; grep activeDeadlineSeconds across infra/k8s confirms all three one-shot Jobs now carry it.
- **Files:** `infra/k8s/job-migration.yaml`
- **Notes:** Not a contract mismatch — ARCHITECTURE.md never specifies the deadline; it is an internal-consistency gap vs sibling Jobs + the project's own LESSON §22. Note migration is a one-shot Job (not a CronJob), so the layer label "07 Scheduled Jobs" applies loosely.

#### `authz-403-404-doc` · DOC_FIX — §6 "403/404" shorthand is ambiguous for the not-own heatmap drill-down denial — code is unambiguously IDOR-safe 404
**Layer:** 04 Authorization · **Severity:** low · **Effort:** S · **Safety:** #3 · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §6 enumerates the required-denial set with the parenthetical "(each `403`/`404` + an authorization-denial `audit_event`)" at line 163, and the §6 sequence diagram at line 1302 reads "cell.manager_employee_id == caller? (else 403/404 + audit)". Both use the ambiguous "403/404" shorthand for the not-own heatmap drill-down cell. The same §6 already disambiguates correctly in its "Realized (Phase 2)" prose (line 165): "cross-owner/cross-team/missing → 404 (existence-hiding); capability/role → 403 + a named code."
- **Code does:** DomainAuthorizationService.authorizeHeatmapCellAccess (apps/wc-api/api/src/main/java/com/st6/wc/auth/DomainAuthorizationService.java:146-153) loads the cell then, for a UserPrincipal that is not the owning manager, throws deny404(principal, HEATMAP_CELL, cellId) at line 150 — an unambiguous IDOR-safe existence-hiding 404. There is no 403 branch. Tests pin 404 only: ManagerHeatmapDrilldownEndpointTest.java:180-192 (drilldown_notOwnCell_404AndAudit asserts status().isNotFound() + audit), :201-212 (IC denied → 404 + audit), :218-226 (missing cell → 404 no-audit). The class javadoc (DomainAuthorizationService.java:38-43) restates the same 404/403 split. This is the correct, safe choice (rule #3); only the §6 "403/404" phrasing is stale/ambiguous.
- **Recommended fix:** Edit ARCHITECTURE.md §6 in two spots to remove the ambiguous "403/404" shorthand for cross-owner/cross-team denials. (1) Line ~163 denial-set sentence: change the leading parenthetical "(each `403`/`404` + an authorization-denial `audit_event`)" to state the rule explicitly, e.g. "(cross-owner/cross-team/missing → `404` existence-hiding; capability/role → `403`+named code; each + an authorization-denial `audit_event`)" — matching the already-correct Realized prose two paragraphs below at line 165. (2) Line 1302 sequence-diagram step: change "(else 403/404 + audit)" to "(else 404 IDOR-safe + audit)" since a not-own heatmap cell is a cross-owner case that always renders 404. No code change. Optionally also tighten the §17 testing-matrix shorthand "each §6 case → `403/404` + audit" at line 229 (that one is acceptable as-is since the matrix legitimately spans both kinds of denial, but mirroring the explicit mapping would help).
- **Verify:** No test needed (doc-only). Confirm the edit matches the code by re-reading DomainAuthorizationService.java:150 (deny404 for HEATMAP_CELL) and that ManagerHeatmapDrilldownEndpointTest.drilldown_notOwnCell_404AndAudit (:180) still asserts status().isNotFound(); both already green at HEAD. The existing AuthorizationIdorMatrixTest + the 3 drill-down 404 tests lock the behavior so no regression risk.
- **Files:** `ARCHITECTURE.md`
- **Safety note:** #3 IDOR-safe central authorization (cross-owner/cross-team/missing → 404 existence-hiding)
- **Notes:** Re-verified at HEAD 3b919a9 — gap still present (the "403/404" shorthand survives at ARCHITECTURE.md:163 and :1302). This is purely a documentation-precision nit, NOT a code gap: the code, three test files, and the §6 Realized prose all agree on IDOR-safe 404 for the not-own drill-down. The not-own heatmap cell is unambiguously a cross-owner case (a manager probing another manager's cell), which is why 404 is the single correct answer — 403 would leak existence and violate rule #3. The "403/404" set notation is only legitimate where a denial bucket genuinely spans both kinds (e.g. the §17 test matrix at :229), not for a single cross-owner case. P3 because it is load-bearing on a safety contract (§6/rule #3) but the as-shipped code is already correct and tested; this is doc hygiene, no user-visible or operational impact.

#### `authz-codes-doc` · DOC_FIX — ARCHITECTURE §6 prose names only 2 of the 7 authorizer-local 403 codes (B.21 already has all 7)
**Layer:** 04 Authorization · **Severity:** low · **Effort:** S · **Safety:** #3 · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §6 at line 165 cites the capability-denial named-code set with only two codes: "...capability/role -> 403 + a named code (`IC_CANNOT_RESOLVE_DISPUTE`, `MANAGER_ROLE_REQUIRED`)". The parenthetical reads as the named-code set. However, the AUTHORITATIVE named-code contract in the SAME doc, Appendix B.21 (ARCHITECTURE.md:681), already enumerates ALL SEVEN authorizer-local codes — IC_CANNOT_OPEN_DISPUTE, IC_CANNOT_RESOLVE_DISPUTE, MANAGER_CANNOT_RESPOND_DISPUTE, IC_CANNOT_WRITE_MANAGER_NOTE, MANAGER_ROLE_REQUIRED, COMMITMENT_OWNER_REQUIRED, PLAN_OWNER_REQUIRED — each with a description and an "authorizer-local constant" annotation. apps/wc-api/CLAUDE.md's ErrorCodes cross-doc row also tracks each of these against B.21.
- **Code does:** DomainAuthorizationService.java:68-74 (apps/wc-api/api/src/main/java/com/st6/wc/auth/) declares exactly seven code constants: CODE_IC_CANNOT_RESOLVE="IC_CANNOT_RESOLVE_DISPUTE" (:68), CODE_IC_CANNOT_OPEN_DISPUTE (:69), CODE_MANAGER_CANNOT_RESPOND (:70), CODE_IC_CANNOT_WRITE_MANAGER_NOTE (:71), CODE_MANAGER_ROLE_REQUIRED (:72), CODE_COMMITMENT_OWNER_REQUIRED (:73), CODE_PLAN_OWNER_REQUIRED (:74). Each is thrown via deny403(...) at :160/:175/:195/:205/:254/:265/:291. The seven exactly match Appendix B.21's enumeration (ARCHITECTURE.md:681) — code and the authoritative contract agree. DomainAuthorizationServiceTest asserts IC_CANNOT_RESOLVE_DISPUTE, MANAGER_ROLE_REQUIRED, COMMITMENT_OWNER_REQUIRED against the live throws. The discrepancy is ONLY between the §6 narrative example list (2 codes) and the §6/B.21 full contract (7 codes); it is not a code-vs-spec gap.
- **Recommended fix:** Edit the §6 parenthetical at ARCHITECTURE.md:165 so it no longer reads as the complete code set. Change "...403 + a named code (`IC_CANNOT_RESOLVE_DISPUTE`, `MANAGER_ROLE_REQUIRED`)" to "...403 + a named code (e.g. `IC_CANNOT_RESOLVE_DISPUTE`, `MANAGER_ROLE_REQUIRED`; the full authorizer-local code set is enumerated in Appendix B.21)". This points readers to the single authoritative list (B.21) without duplicating the 7-code enumeration in two places (which would create a new drift surface). No code change; no test change. Anchor to edit: ARCHITECTURE.md §6, line 165 (the "Realized (Phase 2, 2.4-2.7)" paragraph). Do NOT add the codes to web/ErrorCodes.java — they are deliberately authorizer-local constants per the B.21 annotation and apps/wc-api/CLAUDE.md ErrorCodes row.
- **Verify:** After the edit, grep ARCHITECTURE.md §6 (line ~165) shows the cross-reference to Appendix B.21; confirm B.21 (line 681) still lists all 7 codes (IC_CANNOT_OPEN_DISPUTE, IC_CANNOT_RESOLVE_DISPUTE, MANAGER_CANNOT_RESPOND_DISPUTE, IC_CANNOT_WRITE_MANAGER_NOTE, MANAGER_ROLE_REQUIRED, COMMITMENT_OWNER_REQUIRED, PLAN_OWNER_REQUIRED) matching DomainAuthorizationService.java:68-74 one-for-one. No build/test run needed (doc-only).
- **Files:** `ARCHITECTURE.md`
- **Safety note:** #3 (central IDOR-safe authorization) — informational link only; no behavioral gap.
- **Notes:** Re-verified at current HEAD (commit becc22a). The finding's premise is accurate (§6 prose lists 2 codes; the authorizer has 7), but the "fix" surface is smaller than it might appear: the full 7-code contract is ALREADY present and correct in Appendix B.21 — the two cited §6 codes are clearly illustrative examples, not a closed set. So this is a tidy-up of one narrative line, not a missing-contract gap, hence P3/low. The code is correct and complete; no CODE_FIX warranted. Caveat: if the orchestrator prefers B.21 as the sole code registry, the §6 reference approach (recommended) avoids creating a second enumeration that could itself drift.

#### `demo-failure-port` · DOC_FIX — §10 lists a "demo-failure" Graph adapter mode that has no code/value implementation
**Layer:** 06 Calendar Sync · **Severity:** low · **Effort:** S · **Safety:** #4 · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §10 prose (line 195) lists three Graph adapter modes: "real (...), demo-success, demo-failure." Appendix D.2 (line 994) and D.3 (line 1013) both list `app.graph.mode`/`GRAPH_MODE` enum values as `demo-success | demo-failure | real`. The C.3 worker file tree (line 839) names a concrete file `graph/DemoFailureGraphAdapter.java # deterministic FAILED + safe_message (REQ-E-004)`. So the spec treats `demo-failure` as a first-class, selectable adapter mode with its own port class.
- **Code does:** No `demo-failure` mode or DemoFailure port exists at HEAD. The real package is `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/` (not `graph/`). Only three resolved modes exist: GraphRealModeConfig.java:36 `@ConditionalOnProperty(name="app.graph.mode", havingValue="real")` wires the real GraphCalendarAdapter.java (or, on blank creds, DegradedGraphCalendarPort.java:18 which throws GraphCalendarException on every createEvent → safe FAILED); DemoSuccessGraphCalendarPort.java:18 `@ConditionalOnProperty(havingValue="demo-success", matchIfMissing=true)` is the default. GraphRealModeSelectionTest.java asserts exactly these three resolutions (real+creds, real+blank→Degraded, demo-success/absent→DemoSuccess) — no `demo-failure` case. Setting `GRAPH_MODE=demo-failure` would match no bean condition and leave GraphCalendarPort unwired. Critically, LESSONS.md:770 (§42) records the team's deliberate reconciliation: the failure OUTCOME is delivered by DegradedGraphCalendarPort (real mode, blank creds) and by the seeded FAILED sync record (ARCHITECTURE.md:1133, REQ-E-004) that drives outlook-failed-retry.feature (line 895) — a dedicated demo-failure adapter is redundant.
- **Recommended fix:** Edit ARCHITECTURE.md to drop `demo-failure` as a first-class adapter mode and describe the actual two-mode + degrade design. (1) §10 line 195: change "real (...), demo-success, demo-failure" to "real (...), demo-success; plus a startup fail-safe that degrades `real`+missing-creds to a safe-failure port." (2) Appendix D.2 line 994 and D.3 line 1013: change the enum `demo-success | demo-failure | real` to `demo-success | real` and note the degrade-to-safe-failure fallback (already partly described in the Notes column + D.6 line 1058). (3) C.3 file tree line 839: replace `graph/DemoFailureGraphAdapter.java` with the real `sync/DegradedGraphCalendarPort.java # safe-failure fallback (REQ-E-004/REQ-I-004)` and align the package path `graph/` → `sync/` and class names (DemoSuccessGraphAdapter → DemoSuccessGraphCalendarPort, RealGraphCalendarAdapter → GraphCalendarAdapter, GraphAdapterSelector → GraphRealModeConfig). Cross-reference LESSONS.md §42 which already adjudicated this.
- **Verify:** After the doc edit: `grep -n "demo-failure\|DemoFailureGraphAdapter" ARCHITECTURE.md` returns no matches; the C.3 tree package/class names match `ls apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/`. No code/test change needed — GraphRealModeSelectionTest already pins the three real resolutions (real+creds / real+blank→Degraded / demo-success/absent→DemoSuccess); run `./gradlew :worker:test --tests '*GraphRealModeSelectionTest'` to confirm green is unaffected.
- **Files:** `ARCHITECTURE.md`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/GraphRealModeConfig.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/DegradedGraphCalendarPort.java`, `apps/wc-api/worker/src/main/java/com/st6/wc/worker/sync/DemoSuccessGraphCalendarPort.java`, `apps/wc-api/LESSONS.md`
- **Safety note:** #4 (Outlook sync failure never blocks the core lifecycle) — the failure path the demo-failure mode was meant to exercise is what rule #4 protects; the path is already covered by other means.
- **Notes:** Low impact / cosmetic-contract drift: the user-facing failure demo (FAILED warning + manual-retry via outlook-failed-retry.feature) is fully delivered by the seeded FAILED record (REQ-E-004, ARCHITECTURE.md:1133) and the runtime DegradedGraphCalendarPort, so no specified feature is non-functional. Could be folded into the same round as any other §10/Appendix-D adapter-naming drift (the C.3 tree has multiple stale `graph/` package + class names beyond just DemoFailure). Note one residual ambiguity for the doc author: D.6 (line 1058) describes the degrade as "degrades to the demo adapter," which LESSONS §42 explicitly corrects to a safe-FAILURE port (not demo-success) — keep the D.6 wording consistent with the chosen §10/Appendix-D phrasing so the doc no longer implies a fake-SYNCED degrade.

#### `five-routes-doc` · DOC_FIX — ARCHITECTURE §7 "five routes" count is loose vs realized AppRoutes tree
**Layer:** 09 Frontend · **Severity:** low · **Effort:** S · **Safety:** — · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §7 (:173) "Realized (9.4)" sentence: AppRoutes.tsx "registers the five routes under one Suspense". The surrounding prose already correctly describes the conditional manager gating and the catch-all.
- **Code does:** AppRoutes.tsx (:58-76) registers 6 Route elements: `/`→RootRedirect, `/weekly-commit`, `/weekly-commit/history/:planId`, two `isManager`-gated manager routes, and a `*`→Navigate catch-all. Substance matches §7 exactly; only the literal word "five" undercounts (4 always-on + 2 conditional + catch-all).
- **Recommended fix:** Edit the §7 "Realized (9.4)" sentence (ARCHITECTURE.md:173): replace "registers the five routes" with "registers the route tree" (or "the four feature routes plus the persona-aware `/` default and a catch-all `*`"). Pure doc tidy; the §7 prose already names the catch-all and conditional manager routes correctly, so no other anchor changes. Code is correct as-is — do not touch AppRoutes.tsx.
- **Verify:** grep -n "five routes" ARCHITECTURE.md returns nothing after the edit; visually confirm §7 prose still matches the 6-Route tree in AppRoutes.tsx.
- **Files:** `ARCHITECTURE.md`, `apps/wc-web/src/routes/AppRoutes.tsx`
- **Notes:** Cosmetic count drift only; the detailed §7 prose (one Suspense, RouteErrorBoundary, conditional manager gating, catch-all) is accurate. No code or contract behavior affected.

#### `add-unplanned-doc` · NO_ACTION — ADD_UNPLANNED affordance precondition (ARCHITECTURE F.4 vs code/tests)
**Layer:** 03 Application & Lifecycle · **Severity:** low · **Effort:** - · **Safety:** — · **Re-verified:** ⚠ resolved / not reproducible

- **Spec says:** ARCHITECTURE.md F.4 affordance table, row `ADD_UNPLANNED` (ARCHITECTURE.md:368): "actor = owning IC, plan `LOCKED` or `RECONCILING`". The doc row at current HEAD already lists BOTH states (the finding asserted it listed LOCKED only — that premise is stale).
- **Code does:** AllowedActionResolver.canAddUnplanned (apps/wc-api/api/src/main/java/com/st6/wc/plan/AllowedActionResolver.java:74-77) grants ADD_UNPLANNED when `plan.getState() == PlanState.LOCKED || plan.getState() == PlanState.RECONCILING` (and owner match). Enforcement agrees: CommitmentService.createUnplanned (apps/wc-api/api/src/main/java/com/st6/wc/commitment/CommitmentService.java:128) rejects any state that is `!= LOCKED && != RECONCILING` with IllegalStateTransitionException. Tests agree: AllowedActionResolverTest.java:131 parameterizes presence over `List.of(PlanState.LOCKED, PlanState.RECONCILING)` (lines 128-143) and asserts absence in DRAFT/LOCKED-for-non-owner/RECONCILED. Code + enforcement + tests + the F.4 doc row all agree on both states.
- **Why no action:** 
- **Files:** `ARCHITECTURE.md`, `apps/wc-api/api/src/main/java/com/st6/wc/plan/AllowedActionResolver.java`, `apps/wc-api/api/src/main/java/com/st6/wc/commitment/CommitmentService.java`, `apps/wc-api/api/src/test/java/com/st6/wc/plan/AllowedActionResolverTest.java`
- **Notes:** Re-verified at current HEAD: the gap is NOT reproducible. ARCHITECTURE.md:368 already reads "plan `LOCKED` or `RECONCILING`", which matches AllowedActionResolver (:76), CommitmentService.createUnplanned (:128), and AllowedActionResolverTest (:131). The layer-doc finding described an earlier doc state; the F.4 row has since been widened (or was already correct) to both states. Nothing for the team to change. The finding was either already reconciled or based on a narrower reading of the row at the time it was written.

#### `allowedaction-placeholders` · NO_ACTION — AllowedAction enum carries values not yet emitted by any resolver (RESPOND_DISPUTE, RESOLVE_DISPUTE, COMMENT, RETRY_SYNC)
**Layer:** 03 Application & Lifecycle · **Severity:** low · **Effort:** - · **Safety:** — · **Re-verified:** ⚠ resolved / not reproducible

- **Spec says:** Appendix B.1 (ARCHITECTURE.md:357) + F.4 (:1180-1196) define the full AllowedAction affordance vocabulary incl. RESPOND_DISPUTE/RESOLVE_DISPUTE/COMMENT/RETRY_SYNC, each mapped 1:1 to its endpoint. The project follows "no affordance without enforcement" (§15) — an enum value need not be emitted until its enforcing slice ships.
- **Code does:** Finding is HALF-STALE at HEAD 3b919a9: RESPOND_DISPUTE + RESOLVE_DISPUTE ARE now emitted (DisputeMapper.java:71,76, production-reachable via the E3/E4 plan read → CommitmentMapper nested-dispute context-aware path). Only COMMENT + RETRY_SYNC remain unemitted — and correctly so: no CommentController/CommentDto and no OutlookSyncRecordDto/retry endpoint exist in api/src/main yet.
- **Why no action:** No change to AllowedAction. The two still-unemitted values (COMMENT, RETRY_SYNC) are intentional under §15 "no affordance without enforcement" and already match Appendix B.1/F.4 verbatim — the enum should keep them as the canonical vocabulary so the emitting slices add no enum churn. The real gap is the absent comments-backend and sync-retry-endpoint slices, tracked as separate drift items; they will emit COMMENT/RETRY_SYNC when built. Editing the enum now (removing the two values) would diverge it from the binding B.1 contract — do not.
- **Files:** `apps/wc-api/api/src/main/java/com/st6/wc/action/AllowedAction.java`, `apps/wc-api/api/src/main/java/com/st6/wc/dispute/mapper/DisputeMapper.java`, `apps/wc-api/api/src/main/java/com/st6/wc/plan/AllowedActionResolver.java`, `ARCHITECTURE.md`
- **Notes:** Half-stale: 2 of 4 cited values now wired since the layer doc was written. Remaining COMMENT/RETRY_SYNC are correct-by-design placeholders gated on the absent comments + sync-retry backend slices, not a defect in the enum.

#### `aws-profile` · NO_ACTION — Deploy Spring profile is "aws", not "prod" (application-prod.yml is dead config)
**Layer:** 10 Infrastructure · **Severity:** low · **Effort:** - · **Safety:** — · **Re-verified:** ⚠ resolved / not reproducible

- **Spec says:** ARCHITECTURE.md:207 (§12): deploy profile is `aws`, config in `application-aws.yml`; an `application-prod.yml` would be dead under an `aws` profile.
- **Code does:** deployment-api.yaml:40 and deployment-worker.yaml:38 both set `SPRING_PROFILES_ACTIVE: "aws"`. `application-aws.yml` exists in api+worker source; no `application-prod.yml` exists anywhere in source.
- **Why no action:** None. Doc and manifests already agree; this was a reader-expectation note, not a gap. No code or doc change needed.
- **Files:** `infra/k8s/deployment-api.yaml:40`, `infra/k8s/deployment-worker.yaml:38`, `ARCHITECTURE.md:207`, `apps/wc-api/api/src/main/resources/application-aws.yml`
- **Notes:** Consistent at HEAD: profile=aws across both Deployments plus migration/seed/cron/rebuild jobs; no prod profile to remove. Correct by design.

#### `cloudwatch-forwarder` · NO_ACTION — CloudWatch log groups provisioned but no pod→CloudWatch log-forwarding agent
**Layer:** 10 Infrastructure · **Severity:** low · **Effort:** - · **Safety:** — · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md §15 (L219-221) + REQ-O-009 (E24, L404) name CloudWatch logs/metrics as the observability destination for api/worker/cron/migration, but specify no forwarding agent. Appendix C.6 (L919) and C.7 (L923+) list cloudwatch.tf log groups and the k8s manifests — neither names a Fluent Bit / Container Insights forwarder.
- **Code does:** cloudwatch.tf:16-23 provisions exactly 4 aws_cloudwatch_log_group resources (api/worker/cronjob/migration); addons.tf installs only CSI/ASCP/external-dns; no forwarder anywhere in infra/k8s. So container logs do not reach the groups — exactly as the cloudwatch.tf:5-7 comment states.
- **Why no action:** No code change. The gap is an explicitly accepted, documented Phase-13 trim, not an unspecified contract gap: cloudwatch.tf:5-7 flags it, MVP_TASKS.md:1550 (task 12.10) flagged it, and MVP_TASKS.md:1582 carries it to the §20 trims catalog. The architecture contractually requires only the CloudWatch destination (provisioned), not the agent. If pulling the forwarder into MVP is desired, that is a scope decision (add an aws-for-fluent-bit helm_release + Container Insights IRSA, effort M) — otherwise leave deferred and ensure it lands in the §20 Trims catalog.
- **Files:** `infra/terraform/cloudwatch.tf`, `infra/terraform/addons.tf`, `MVP_TASKS.md`, `ARCHITECTURE.md`
- **Notes:** Finding is technically accurate (logs do not flow without a forwarder) but it is a known, deliberately-deferred Phase-13 trim with a tracked carry-forward — not a drift the team needs to fix in MVP. Only actionable if scope is expanded to require log content end-to-end.

#### `enum-count` · NO_ACTION — Layer-doc brief said "18 enums"; code + ARCHITECTURE.md both say 16
**Layer:** 01 Domain · **Severity:** low · **Effort:** - · **Safety:** — · **Re-verified:** ⚠ resolved / not reproducible

- **Spec says:** ARCHITECTURE.md:359 and apps/wc-api/CLAUDE.md:123 both state exactly "16 enums" in shared/enums/, pinned by EnumVocabularyTest. AllowedAction (ARCHITECTURE.md:357) is documented as a computed DTO-layer vocabulary, excluded by design.
- **Code does:** shared/src/main/java/com/st6/wc/enums/ has exactly 16 enum files; EnumVocabularyTest.java:25-47 parameterizes over 16 enum classes. Code and both spec anchors agree on 16.
- **Why no action:** No code or doc change needed. The "18 enums" figure exists only in a non-binding layer-docs generation brief, not in ARCHITECTURE.md or CLAUDE.md. If desired, correct that brief to 16 (and note AllowedAction/ErrorCodes/ReviewStateFilter are DTO-layer vocabularies, not persisted enums), but this is optional tidy outside the contract surface.
- **Files:** `apps/wc-api/shared/src/main/java/com/st6/wc/enums/`, `apps/wc-api/shared/src/test/java/com/st6/wc/enums/EnumVocabularyTest.java`, `ARCHITECTURE.md`, `apps/wc-api/CLAUDE.md`
- **Notes:** Finding was a brief error, not a drift. The 17th file in a naive glob is EnumVocabularyTest in src/test, not a production enum.

#### `mark-reviewed-doc` · NO_ACTION — MARK_REVIEWED affordance narrows documented granted-when (intentional §31 UX narrowing)
**Layer:** 02 API & Web · **Severity:** low · **Effort:** - · **Safety:** — · **Re-verified:** ✓ present

- **Spec says:** ARCHITECTURE.md B.1:370 grants MARK_REVIEWED when actor = direct manager and plan LOCKED+, stating no reviewStatus condition.
- **Code does:** AllowedActionResolver.canMarkReviewed (lines 128-133) additionally requires reviewStatus == NOT_REVIEWED, hiding the button once reviewed — its Javadoc (117-127) explicitly documents this as a §31 UX-narrowed subset of E16's broader enforcement.
- **Why no action:** No code or doc change required. The narrowing is a deliberate affordance ⊆ enforcement subset, globally sanctioned by the project's §31 affordance-subset pattern and already recorded in apps/wc-api/CLAUDE.md (ManagerReviewDto row, origin 6.8). If a doc nicety is ever wanted, add a one-line footnote to B.1:370 noting the affordance hides once reviewed (effort S) — but this is optional, not needed.
- **Files:** `apps/wc-api/api/src/main/java/com/st6/wc/plan/AllowedActionResolver.java`, `apps/wc-api/api/src/main/java/com/st6/wc/plan/mapper/PlanMapper.java`, `ARCHITECTURE.md`
- **Notes:** Confirmed at HEAD: resolver line 132 (reviewStatus == NOT_REVIEWED), emit site PlanMapper.java:95-96, spec B.1 line 370. Matches sibling subsets canCarryForward/canOpenDispute; finding is correct-by-design, not a real gap.

#### `rule1-not-notnull` · NO_ACTION — Rule #1 (required SO at lock) is service-enforced, not a DB NOT NULL — by design
**Layer:** 01 Domain · **Severity:** low · **Effort:** - · **Safety:** #1 · **Re-verified:** ⚠ resolved / not reproducible

- **Spec says:** Safety rule #1: every planned commitment links a Supporting Outcome at lock, enforced server-side in PlanLifecycleService (409 UNLINKED_PLANNED_COMMITMENT / EMPTY_PLAN_LOCK). ARCHITECTURE.md:464 explicitly marks supportingOutcomeId as "nullable-until-lock (planned)".
- **Code does:** supporting_outcome_id is a nullable FK (V1__core_schema.sql:111; WeeklyCommitment.java:44, no nullable=false). The lock gate in PlanLifecycleService.java:123-132 rejects empty plans (EmptyPlanLockException) and any planned commitment with null SO (UnlinkedPlannedCommitmentException). Matches spec exactly.
- **Why no action:** No change. Code, schema, and ARCHITECTURE.md already agree: the column is intentionally nullable (links accrue pre-lock) and the invariant is the service-layer lock gate, not a DB constraint. A DB NOT NULL would be wrong — it would block legitimate DRAFT/unplanned states where the link is absent.
- **Files:** `apps/wc-api/shared/src/main/resources/db/migration/V1__core_schema.sql`, `apps/wc-api/shared/src/main/java/com/st6/wc/commitment/WeeklyCommitment.java`, `apps/wc-api/api/src/main/java/com/st6/wc/plan/PlanLifecycleService.java`
- **Notes:** Correct by design and already documented at ARCHITECTURE.md:464 ("nullable-until-lock"). The drift note itself flagged it as low-sev documentation-only; nothing to enforce, no doc edit needed.

#### `worker-irsa` · NO_ACTION — Worker IRSA scope reconciled to db+graph (doc and code agree)
**Layer:** 10 Infrastructure · **Severity:** low · **Effort:** - · **Safety:** #7 · **Re-verified:** ⚠ resolved / not reproducible

- **Spec says:** ARCHITECTURE.md §12 (line 207) states worker SA gets GetSecretValue on db + graph (db added Wave-2 s8 adfa639 for SyncRecord reload), explicitly NOT auth0/demo/*. Mirrored at line 929 (serviceaccount-worker.yaml comment).
- **Code does:** iam_irsa.tf:98-102 grants the worker GetSecretValue on db.arn + graph.arn only (comment: "db + graph ONLY, NOT auth0/demo, NOT *"). secretproviderclass.yaml:58-90 mounts db + graph keys for the worker SA, mirroring the grant.
- **Why no action:** No action. Doc and code are fully reconciled at HEAD (commit adfa639 landed both code files and the §12 anchor reflects the db+graph scope). Least-privilege is preserved — worker still has no auth0/demo grant and no wildcard. Finding was already resolved by the reconcile commit.
- **Files:** `ARCHITECTURE.md`, `infra/terraform/iam_irsa.tf`, `infra/k8s/secretproviderclass.yaml`
- **Notes:** Clean tree at HEAD 3b919a9; both code files last touched by adfa639, the reconcile commit. Least-privilege intact: no auth0/demo, no wildcard.

---
_Generated from the `/layer-docs` drift findings by a re-verification pass at `HEAD 3b919a9` — each card was produced by an agent that re-opened the cited code. To refresh: re-run `/layer-docs --update`, then regenerate this triage. Priority/severity are advisory; the team owns the final call._
