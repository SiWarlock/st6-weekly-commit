# Lombok config for all wc-api modules (task 0.2).
# @Data is FORBIDDEN (breaks Hibernate entity identity) — enforced mechanically by the
# `forbidLombokData` Gradle gate; use @Getter/@Setter/@Builder instead.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
