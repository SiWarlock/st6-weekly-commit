# wc-api Gradle build properties (task 0.2).
# Spring Boot version pinned for 0.4/0.5 to consume (plugin not applied in 0.2).
springBootVersion=3.3.5

org.gradle.caching=true
org.gradle.parallel=true

# NOTE: the JDK-21 toolchain resolves from JAVA_HOME (+ foojay auto-provision). Machine-specific
# `org.gradle.java.installations.paths` pins, if needed, belong in an UNCOMMITTED ~/.gradle/gradle.properties
# — never here (would break CI / other devs). CI (0.8) pins JDK 21 via setup-java.
