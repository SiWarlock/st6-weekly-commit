# ST6 Weekly Commit Module `apps/wc-api/` — Build Guide

> **You're in `apps/wc-api/`.** This file plus root `CLAUDE.md` both load. The root file covers global project conventions + shared comm rules (track-prefix, escalation taxonomy, messaging budget); this file owns code-area conventions for backend.

## Launch protocol

| Working on... | cwd | Loads |
|---|---|---|
| Planning / docs / commits | repo root (`ST6/`) | root `CLAUDE.md` only |
| backend code | `apps/wc-api/` | this `CLAUDE.md` + root |

<!-- For a multi-area project, add a row per additional code area. -->

If you find yourself fighting the wrong conventions, check your cwd.

## Session start/end protocol

**At session start:**
1. Read `MVP_TASKS.md` (repo root) → "Currently in progress" section.
2. Confirm with the user what feature this session is targeting.
3. Read the relevant section of `ARCHITECTURE.md` from the lookup table below.

**At session end** (only when the user explicitly says we're done):

1. **Implementer runs `/session-end`.** Implementer writes ONLY:
   - `apps/wc-api/` code files (the slice's implementation)
   - test files (the slice's tests)
   - dependency manifest / lockfile (deps the slice adds)
   - `docs/sessions/<NNN>-<date>-<topic>.md` (session doc, created at `/session-end` Step 5)

   **Implementer must NOT touch (all orchestrator territory):**
   - `MVP_TASKS.md`
   - `apps/wc-api/LESSONS.md`
   - `apps/wc-api/CLAUDE.md` (entire file — both the Cross-doc invariants table AND the Lessons logged index)
   - `ARCHITECTURE.md`
   - `docs/orchestrator-briefing.md` / `docs/tdd-brief-template.md` / `docs/briefs/` / `docs/runbooks/`
   - other top-level deliverable / design docs
   - `.gitignore` and root-level dotfiles (unless adding a new artifact to ignore, flagged at Step 9)

   At the slice's Step 10 commit, **explicit `git add <path>` for each slice file**; **never `git add -A`** or `git add .`; **never stage an orchestrator-territory file**. If the slice surfaces a change to any orchestrator-territory file (new model needing a cross-doc table row, a lesson candidate, an architecture note), the implementer **flags it at Step 9** per the routing matrix in `docs/orchestrator-briefing.md`. The orchestrator writes the change hot during the same session — working-tree state stays aligned within the round even though commits stagger.

2. **Orchestrator runs `/orchestrate-end`** for round close-out + Carry-forward triage + round terminal commit + push.

## Lookup table — where to find canonical info

Don't paste these sections into the prompt. Grep the file:section, read only what you need. `/check-arch <topic>` dispatches off this table.

| Topic | File (relative to repo root) | Section |
|---|---|---|
| Plan lifecycle (`DRAFT→LOCKED→RECONCILING→RECONCILED`) | `ARCHITECTURE.md` | §3 |
| Lessons logged (full prose) | `apps/wc-api/LESSONS.md` | by lesson # |

<!-- Starts near-empty. Add a row whenever a topic is looked up twice. -->

**Code intelligence & docs (when available):** prefer a code-intelligence MCP (e.g. CodeGraph) for code navigation / callers / traces over `grep`+read loops, and a docs MCP (e.g. Context7) for up-to-date library/API docs — see root `CLAUDE.md` "Code intelligence & docs." No-op if not installed.

## Stack

<!-- ▼ EXAMPLE BLOCK [id=area-stack]: stack quick-reference for implementer sessions. Canonical stack lives in root CLAUDE.md + ARCHITECTURE.md; this is the cheat sheet. ▼ -->

- **Runtime / build:** Java 21 · Gradle multi-module (shared / api / worker)
- **Framework:** Spring Boot 3.3 (Spring MVC · Spring Data JPA + Hibernate · Spring Security OAuth2 resource server)
- **Migrations / DB:** Flyway · PostgreSQL 16 (RDS, latest 16.x) — Flyway enabled ONLY on the migration Job
- **Validation:** Jakarta Bean Validation (Hibernate Validator) on every request DTO
- **Lint / types / tests:** Spotless + SpotBugs / javac (statically typed) + SpotBugs / JUnit 5 + Testcontainers · JaCoCo ≥80%/module

<!-- ▲ END EXAMPLE BLOCK [id=area-stack] ▲ -->

## Standard commands

> **Run backend Gradle commands from `apps/wc-api/`** (or via the repo-root composite). The repo-root build only exposes the **aggregate** `check`/`build`; per-task targets (`compileJava`/`spotbugsMain`/`spotlessCheck`/`test`) live in the `apps/wc-api` build. See LESSONS §6.

```bash
# (from apps/wc-api/)
# Run the dev server (if applicable)
./gradlew :api:bootRun

# Tests
./gradlew test

# Quality (per-task)
./gradlew spotbugsMain
./gradlew spotlessCheck
./gradlew compileJava

# Preflight / the real gate (use before saying "done" — this is also the §13 CI gate):
# runs Spotless + SpotBugs + JaCoCo ≥80%/module verification + forbidLombokData
# + checkModuleBoundaries + all tests, per module. NOT `build -x test` (jacoco
# verification is bound to check and needs tests to run).
./gradlew check
```

## TDD protocol

**Write the failing test first.** Applies to deterministic code — see the TDD posture in root `CLAUDE.md` for what is test-first vs. exempt.

**Commit per slice when practical.** Never bundle a safety-critical slice with anything else.

## Forbidden patterns

<!-- ▼ EXAMPLE BLOCK [id=forbidden-patterns]: forbidden patterns — 3-5 narrow, enforceable, domain-specific rules. Shape: "Don't <pattern X> because <reason / past incident>; use <alternative Y>." Test-pin them where possible. Starts small; accretes as lessons surface. ▼ -->

Do not:

1. **Write code without a failing test first** (deterministic backend logic). Even one-line methods.
2. **Use Lombok `@Data`** — use `@Getter`/`@Setter`/`@Builder`; `@Data`'s generated `equals`/`hashCode` on JPA entities breaks Hibernate identity.
3. **Return JPA entities across the API boundary** — map to DTOs (per Appendix B); entities leaking causes lazy-init + over-exposure bugs.
4. **Put lifecycle transitions in controllers** — transitions live in service methods (`PlanLifecycleService`, etc.) inside one transaction with validation + projection updates.
5. **Store `OVERDUE` as a review status** — it is derived at read time via an injectable `Clock`.
6. **Let an Outlook/SNS failure roll back a core mutation** — the sync record is a downstream outbox; failures are caught, never rethrown into the core txn.
7. **Authorize a create/mutation with `authorize…Access`** — the read-authorizer admits a manager-direct-report (correct for *reads* like E4/E14, a §6 authorship hole for *writes*). Authorship + mutations are IC-owner-only → use `authorize…Mutation` (`authorizePlanMutation` / `authorizeCommitmentMutation`) → `403 …_OWNER_REQUIRED` + denial audit for a manager-of-owner. (Real shipped bug: 3.4a E5 create used `authorizePlanAccess` → a manager could author on a report's DRAFT plan; fixed at the Phase-4→5 boundary. LESSONS §32.)

<!-- ▲ END EXAMPLE BLOCK [id=forbidden-patterns] ▲ -->

## Cross-doc invariants — schema/docs mirroring

Several typed models in this codebase are **contracts** mirrored in `ARCHITECTURE.md` and indexed in the table below. The architecture doc is the canonical contract; the model is the executable enforcement. Drift produces silent disagreement.

**Authoring discipline (orchestrator owns this table).** When the implementer adds, removes, or renames a field on one of these models, the implementer **flags it at Step 9 categorized as `Cross-doc invariant change`** per the routing matrix in `docs/orchestrator-briefing.md`. The implementer does NOT edit `apps/wc-api/CLAUDE.md` or `ARCHITECTURE.md` directly — the orchestrator writes the table row + the architecture edit hot during the same session. Working-tree state aligns within the round; commits stagger (implementer's slice commit lands code+tests; orchestrator's round commit lands the doc rows).

| Model | `ARCHITECTURE.md` section | Notes |
|---|---|---|
| Enum vocabulary (16 enums in `shared/enums/`) | Appendix A / Appendix B.1 | Constant sets mirror Appendix B.1 **exactly** (REQ-D-010 — the executable mirror of the `VARCHAR`+`CHECK` columns). Pinned by `EnumVocabularyTest` (parameterized value-set + the 3 drift-trap negatives: no `ReviewStatus.OVERDUE`, `CommentTargetType={PLAN,COMMITMENT}`, `SyncRelatedType=MANAGER_REVIEW_WEEK`). `RoleType` is in `enums/`; `AllowedAction` is computed DTO vocab (Phase 3), not an `enums/` member. Adding/removing/renaming a constant requires a paired B.1 edit. (origin: 0.3) |
| Core schema (`V1__core_schema.sql`, 12 tables) | §4 / Appendix A | Physical encoding of the 12 Appendix-A core models. `VARCHAR`+`CHECK` status columns mirror the enum vocabulary exactly — pinned by the `enum↔CHECK` test (`V1CoreSchemaMigrationTest`, parses `pg_get_constraintdef` over all 15 status columns). Carries the 4 deltas (manager_alignment_note present, progress_status absent, comment flat `{PLAN,COMMITMENT}`, sync week_start_date + MANAGER_REVIEW_WEEK), `version bigint` `@Version` on the 5 mutable tables, `unique(employee_id,week_start_date)`. **Binding source = §4/Appendix A; `docs/planning/DATA_MODEL.md` is superseded (stale).** A column/constraint change pairs a §4 + Appendix A edit. (origin: 1.2) |
| Partial unique indexes (`V2__partial_unique_indexes.sql`) | §4 / §6 / §3 / §10 / Appendix A | The 3 partial uniques backing the single-row invariants (safety-relevant): `uq_active_manager_per_report` (single active manager, §6), `uq_one_unresolved_dispute_per_commitment` (one unresolved dispute, safety rule #6), `uq_one_review_block_per_manager_week` (one review-block/manager/week, §10). Each tested firing (23505) + non-firing/partial-scope on PG16. Distinct from the V1 full sync unique. **Repo-layer proof (1.6):** each fires through `saveAndFlush` as a Spring `DataIntegrityViolationException`; the unresolved-dispute index is proven across **OPEN + IC_RESPONDED** (the rule-#6 cross-status uniqueness bucket), not a same-status duplicate. A constraint-clause change pairs a §4 + Appendix A edit. (origin: 1.3) |
| Manager projections (`V3__projection_tables.sql`) | §9 / Appendix A | The 2 synchronous read-model tables — `manager_plan_summary` (unique(manager,employee,week)) + `manager_heatmap_cell` (unique(manager,employee,week,DO)), each with the 7 count columns + `updated_at` (read models: **no audit quartet, no `@Version`** — recomputed wholesale by the ProjectionService). `manager_heatmap_cell.risk_badges text[]` is constrained to the 6-badge `RiskBadge` vocabulary via a `<@` containment CHECK, pinned to `RiskBadge.values()`. `is_review_overdue` is the §9 projection column (NOT safety rule #6). **6.2 pins the count DERIVATION from source** (no schema change): `misaligned_count` = `alignment_status=MISALIGNED` ∪ open-`flag_type=MISALIGNED`-dispute (deduped per commitment; the MISALIGNED badge consumes the same union), `unresolved_dispute_count` now real (was hardcoded 0), `blocked_count` = `reconciliation_outcome=BLOCKED` (**corrected from the latent `work_type=BLOCKER`** — a planning category; the reconciliation-outcome source is pinned by the R5 Grace seed + `rebuild==seed`, §9/§17 — so `blocked_count=0` at lock); disputes loaded once internally so the 5 `recompute(...)` callers stay unchanged (LESSONS §37). `carry_forward_count` unchanged (successor-link reading, §30). **6.3a centralizes the 5-site in-txn refresh** (lock, start/close-reconciliation, carry-forward, commitment-outcome) in `ProjectionRefresher.recomputeForPlan(plan)` — the §9 source-loading entrypoint reused by the 6.7 rebuild job (resolve manager from plan owner → §28 no-manager/no-review skip → load source → `ProjectionService.recompute`). **6.4** extracts the badge derivation into `RiskBadgeDeriver` (the single badge source for the incremental path + the 6.7 rebuild — RISK-014 made *structural*: each badge derives from the cell's own stored count, so it can't disagree). A field change pairs a §9 + Appendix A edit. **6.7** adds the from-source **rebuild job** (`ProjectionRebuilder` + the `--app.job=rebuild-projections` `ProjectionRebuildRunner`, §8/§23) — truncate both tables + recompute every plan through the SAME `ProjectionRefresher.recomputeForPlan`, so **`rebuild==incremental`** (drift/orphan cells corrected); one `@Transactional` run, the §28 unmanaged/unreviewed skip, one SYSTEM `PROJECTIONS_REBUILT` null-actor audit/run. **The §9 manager-projection story is now COMPLETE** (9 synchronous triggers + the from-source rebuild, one derivation). `rebuild==seed` (Appendix-E R1–R6) pends the HELD V5/V6 demo seed (Phase 10). (origin: 1.4; derivation 6.2; refresh-centralized 6.3a; badges 6.4; rebuild 6.7) |
| WeeklyPlan | §3 / Appendix A | Lifecycle state + locked baseline; mirror field changes into Appendix A. |
| JPA entities (14, `shared/<domain>/*.java`) | Appendix A (all 14 models) / §3 / §4 / §9 | The Spring Data JPA entities are the **executable mirror of all 14 Appendix-A models** — field names, nullability, enum bindings (`@Enumerated(STRING)`), the 4 deltas (`managerAlignmentNote` present, no `progressStatus`, `weekStartDate` present, comment `{PLAN,COMMITMENT}`), `@Version` on the 5 mutable lifecycle entities — mapped onto the V1–V3 columns. Three base-class shapes + flat-`UUID` FKs (LESSONS §7); `risk_badges text[]`/`metadata_json jsonb` via Hibernate-6 `@JdbcTypeCode` (§8). A field add/rename/remove pairs an Appendix A + `§`-section edit. **No drift at 1.5** (entities == V1–V3 DDL == Appendix A; both reviewers confirmed). `@Version` optimistic locking proven behaviorally at the repo layer (increment 0→1 + `ObjectOptimisticLockingFailureException` on stale update) in 1.6. (origin: 1.5) |
| `MeDto` (E1 response DTO, `api/me/dto/`) | Appendix B.3 / §5 (E1) / §6 | The **first DTO across the API boundary** — the executable mirror of B.3 (`employeeId/email/displayName/role/persona/isManager/timezone?`); a `record`, **never an entity** (forbidden-pattern #3, pinned by `MeEndpointTest`'s entity-field `.doesNotExist()` leak test). Fields bind to `Employee`/`ManagerRelationship` (Appendix A): `role`=authoritative `Employee.role`, `isManager`=relationship-driven (2.4), `persona`=email (both modes). B.3 already exists in `ARCHITECTURE.md` + the frontend mirrors it — **no Appendix edit needed**; a future field add/rename pairs a B.3 edit. Sets the DTO-mirrors-Appendix-B pattern for every Phase-3+ response. (origin: 2.7) |
| `RcdoTreeDto` + `RallyCryNode`/`DefiningObjectiveNode`/`SupportingOutcomeNode` (E2 response, `api/rcdo/dto/`) | Appendix B.4 / §5 (E2) / §6 | The **2nd DTO across the boundary** — nested object-wrapper `{rallyCries:[RallyCryNode{id,title,description?,active,definingObjectives[]}]}`, DO adds `rallyCryId`+`supportingOutcomes[]`, SO adds `definingObjectiveId`; all **records**, never entities (forbidden-pattern #3, pinned by the endpoint's audit-quartet `.doesNotExist()` leak test). Mirrors B.4 **verbatim** — B.4 pre-exists in `ARCHITECTURE.md` + the frontend `dtos.ts` already mirrors it, **no Appendix edit needed**; a future field add/rename pairs a B.4 edit. `active` serialized verbatim (inactive not filtered); org-wide read = authenticated-only, no authorizer call (LESSONS §22). (origin: 3.1) |
| `WeeklyPlanDto` (E3/E4/E8/E9/E10/E11 response, `api/plan/dto/`) | Appendix B.5 / §5 / §15 | **3rd DTO across the boundary** — `record` (B.5: `id`,`employeeId`,`employeeDisplayName`,`weekStartDate`/`weekEndDate`,`state`,4 lifecycle ts,`plannedCount`/`unplannedCount`,`commitments[]`,`managerReview?`,`allowedActions[]`,`version`), never an entity (leak-tested). `managerReview` null while DRAFT. `allowedActions[]` is server-authoritative affordance (§15) — see `AllowedAction`. B.5 pre-exists — no Appendix edit; a field change pairs a B.5 edit. (origin: 3.3a) |
| `WeeklyCommitmentDto` (E5/E6/E7/E11/E12 response; nested in B.5, `api/commitment/dto/`) | Appendix B.6 / §5 | **4th DTO** — `record` mirroring B.6 verbatim, now including **`dispute?: AlignmentDisputeDto`** (the user-approved **Option-A** nest, `docs/planning/023` §6 — **realized at 5.3b**, brief 059): `CommitmentMapper`'s single private core resolves the commitment's current `OPEN`/`IC_RESPONDED` dispute via the 5.3 `DisputeMapper` + `AlignmentDisputeRepository.findByCommitmentIdAndStatusIn`, else `null`. The transitional-subset omission is **DISCHARGED** and the never-implemented `hasUnresolvedDispute` boolean **DROPPED** from B.6 (the nested object's presence + status is the single source of truth). Read-path slice — **no new authz** (rides the existing E3/E4 authorizer; no `DomainAuthorizationService` change, no endpoint, no error code). Frontend mirrors `dispute?` into `dtos.ts` (drops `hasUnresolvedDispute`) → unblocks 9.11a. `dispute` is a nullable single object (**no `List.copyOf`**); `version` as `long`; `allowedActions` via `List.copyOf` (§22). (origin: 3.3a; dispute nest 5.3b) |
| `RcdoBreadcrumbDto` (nested in B.6, `api/commitment/dto/`) | Appendix B.6 | **5th DTO** — `record` (`rallyCryId`/`rallyCryTitle`, `definingObjectiveId`/`definingObjectiveTitle`, `supportingOutcomeId`/`supportingOutcomeTitle`); resolved by `RcdoReadService.resolveBreadcrumb(soId)` (RCDO knowledge stays in the RCDO service), null when the commitment is unlinked. B.6 pre-exists — no Appendix edit. (origin: 3.3a) |
| `ManagerReviewDto` (B.7, nested in B.5; E16 response, `api/review/dto/`) | Appendix B.7 / §3 / §9 | **6th DTO** — `record` mirroring B.7; **type created at 3.3a** to compile `WeeklyPlanDto.managerReview` (mapped **null-while-DRAFT**). **`ReviewMapper` + derived `isOverdue` realized at 3.5** (`now > reviewDueAt ∧ NOT_REVIEWED`, never stored — rule #6, injectable `Clock`; `REVIEWED`/`REVIEWED_WITH_DISPUTES` never overdue); `PlanMapper` now maps the review for **LOCKED+** plans (null while DRAFT — the 3.3a/3.3b read tests stay green); **`allowedActions` + `unresolvedDisputeCount` wired at 6.8** (`fea91b2`, brief 081): `PlanMapper` emits **`MARK_REVIEWED`** on the review iff the viewer is the active direct manager (`viewerIsDirectManager`, §35) AND `status == NOT_REVIEWED` — a **§31 UX-narrowed subset** of E16's enforcement, via `AllowedActionResolver.canMarkReviewed` (mirrors — never calls — `authorizeReviewMutation`); `unresolvedDisputeCount` is now the **real** count, derived from the loaded `commitmentDtos`' nested disputes (OPEN/IC_RESPONDED, 5.3b — no extra query / no N+1; §9-projection parity). The IC owner (and any non-direct-manager) sees empty `allowedActions` (§35 leak guard; the 4 `PlanMapper` callers traced). B.7 pre-exists — no Appendix edit. (origin: 3.3a; realized 3.5; affordance + count wired 6.8) |
| `AllowedAction` (computed DTO vocab, `api/action/`) | Appendix B.1 / F.4 / §15 | **NOT an `enums/` member** (computed affordance vocab, never persisted — keeps `EnumVocabularyTest`'s 16-enum pin intact). Materialized at 3.3a in neutral pkg `com.st6.wc.action` (cross-cutting — used by `WeeklyPlanDto`/`WeeklyCommitmentDto`/`ManagerReviewDto`/dispute DTOs; not `plan/`). Computed by `AllowedActionResolver` from the SAME predicates the lifecycle services enforce (affordance↔enforcement single-source, §15) — **"no affordance without enforcement"** (3.3a emits only `LOCK`; other actions emitted by their enforcing slices). **`LOCK` enforcement realized at 3.5** — `PlanLifecycleService` reuses `canLock` as the lock precondition, closing the affordance↔enforcement loop for `LOCK`. Vocab mirrors B.1; a value add pairs a B.1 + F.4 edit. (origin: 3.3a; LOCK enforced 3.5) |
| `CreateCommitmentRequest` (E5 request DTO, `api/commitment/dto/`) | Appendix B.6 (E5 request) / Appendix E Part 1 / §16 | The first **request** DTO across the boundary — a validated `record`: `title` (required, ≤255 **code points** via `@CodePointSize`, `@NotBlank`, `@NoControlChars`), `description?` (≤4000 cp), `supportingOutcomeId?`, `priority`/`workType`/`confidence`/`alignmentStatus?`. **Normalized ONCE in the compact constructor** (`TextNormalizer`: strip+collapse+NFC; title rejects C0/C1, description strips C0 except `\n`/`\t`); server forces `commitmentKind=PLANNED`, rejects `workType=UNPLANNED`. Validation failures → `400 VALIDATION_ERROR`+`fieldErrors[]`; user text stored RAW (React-escapes, §16) + never logged (§15). B.6 + Appendix E pre-exist — no Appendix edit; a field/cap change pairs a B.6 + Appendix-E edit. (origin: 3.4a) |
| `PatchCommitmentRequest` (E6 request DTO, `api/commitment/dto/`) | Appendix B.6 (E6 request) / §3 / Appendix E Part 1 | E6 partial-update request — a **presence-flag POJO, NOT an `Optional<>` record** (a record can't model PATCH 3-way presence: Jackson collapses absent + present-null into `Optional.empty`, and Hibernate's `OptionalValueExtractor` feeds null into `@NotBlank` — LESSONS §27). Each field carries a "provided" flag; absent=untouched / present-null=clear-nullable / value=set (validated/normalized via the reused 3.4a `TextNormalizer` + constraints). Only the owning IC mutates (`authorizeCommitmentMutation`); post-lock baseline edit → `409 LOCKED_BASELINE_EDIT`, post-lock `alignmentStatus` → `409 ILLEGAL_STATE_TRANSITION`. **4.1 extends it** with presence-flagged `reconciliationOutcome`/`outcomeNote` (the E6 RECONCILING outcome contract — editable only in `RECONCILING` via the per-state editable-field allow-list, LESSONS §27; `outcomeNote` ≤4000 cp via `TextNormalizer`; a direct `CARRIED_FORWARD` → `400 VALIDATION_ERROR`, set only via E12). **4.5 extends it again** — in `RECONCILING`, `supportingOutcomeId` becomes editable for an **UNPLANNED** commitment only (the pre-close SO-link, REQ-F-026); a PLANNED `supportingOutcomeId` stays frozen (`LOCKED_BASELINE_EDIT`) — the editable-field allow-list is now a per-(state × kind) matrix (LESSONS §27; ad-hoc security-reviewer PASS). **5.7 extends it again** with `managerAlignmentNote` — the one **manager-owned** E6 field (the inverse of every IC field): a `managerAlignmentNote` patch routes to a dedicated `authorizeManagerAlignmentNote` chokepoint (manager-of-owner-only; IC-owner → `403 IC_CANNOT_WRITE_MANAGER_NOTE`; unrelated/non-direct → IDOR `404`; §33 commitment-field manager-capability paralleling `authorizeDisputeCreation`), is **rejected if mixed with any IC field** (single-actor-per-patch → `400 VALIDATION_ERROR`, request-shape/IDOR-safe — no resource load), writable `LOCKED`+ only (DRAFT → `409`), validated ≤4000 cp, audited (`COMMITMENT_ALIGNMENT_NOTED`, no note body, §15); the IC path is **byte-for-byte unchanged** (field-level authz on a shared endpoint, LESSONS §36; ad-hoc security-reviewer CLEAN PASS). B.6/Appendix E pre-exist — no Appendix edit. (origin: 3.4b; extended 4.1, 4.5, 5.7) |
| `CreateUnplannedCommitmentRequest` (E11 request DTO, `api/commitment/dto/`) | Appendix B.6 (E11 request) / Appendix E Part 1 / §3 | The **inverse of `CreateCommitmentRequest`** (E5) — a validated `record` for `POST /api/plans/{id}/unplanned-commitments`: `title` (required, ≤255 code points, `@NotBlank`/`@NoControlChars`/`@CodePointSize`), `description?` (≤4000 cp), `supportingOutcomeId?` (**optional at create** — REQ-F-026, link enforced only at close §4.5; a provided value validated in-service, unknown → `400`), `priority`/`confidence`/`alignmentStatus?` (defaults `NEEDS_REVIEW`). **`workType` + `commitmentKind` are deliberately ABSENT** — server-owned, always forced to `UNPLANNED` by `CommitmentService.createUnplanned` (unknown JSON props ignored, so no client value can conflict; mirrors how E5 force-sets `PLANNED` + rejects `workType=UNPLANNED`). Normalized ONCE in the compact constructor (reused `TextNormalizer`: title strip+collapse+NFC / description strip+NFC, blank→null); unknown enum → `400` via `HttpMessageNotReadable`. Allowed in `LOCKED` + `RECONCILING` (DRAFT/RECONCILED → `409 ILLEGAL_STATE_TRANSITION`); parent-plan authz (owning-IC, non-owner → IDOR-safe 404); insert-only ⇒ planned baseline byte-identical (REQ-F-025). B.6 + Appendix E pre-exist — no Appendix edit; a field/cap change pairs a B.6 + Appendix-E edit. (origin: 4.3) |
| `ErrorCodes` (named-code constants, `api/web/`) | §5 / B.21 (RFC-7807 named codes) | The §5/B.21 RFC-7807 `code` vocabulary as `static final String` constants — **NOT an `enums/` member** (free-string codes, no `VARCHAR`+`CHECK`; like `AllowedAction`/the §15 audit-action taxonomy). `VALIDATION_ERROR` + `ILLEGAL_STATE_TRANSITION` (3.4a); `LOCKED_BASELINE_EDIT` (3.4b). **`COMMITMENT_OWNER_REQUIRED`** (3.4b) is an authorizer-local constant in `DomainAuthorizationService` (like `MANAGER_ROLE_REQUIRED`), not in `ErrorCodes`. Each code mirrors a B.21 `code`; a new code pairs a B.21 edit if absent (both `VALIDATION_ERROR` + `COMMITMENT_OWNER_REQUIRED` added to B.21 at 3.4b). **`EMPTY_PLAN_LOCK` + `UNLINKED_PLANNED_COMMITMENT` realized at 3.5** (`UNLINKED…` carries `fieldErrors[]` naming each unlinked `commitments[<commitmentId>].supportingOutcomeId` (keyed by commitment **id**, not array index) + `constraint=planned_commitment_requires_supporting_outcome_at_lock`, B.21). **`PLAN_OWNER_REQUIRED`** (3.5) is an authorizer-local constant in `DomainAuthorizationService` (like `COMMITMENT_OWNER_REQUIRED`/`MANAGER_ROLE_REQUIRED`), NOT in `ErrorCodes` — added to B.21. `OptimisticLockingFailureException` → `409 ILLEGAL_STATE_TRANSITION` in `ProblemDetailsExceptionHandler` (§5 optimistic-lock→409). **`UNPLANNED_MISSING_LINK_AT_CLOSE` realized at 4.5** (E10 close-reconciliation incompleteness — `422` + `fieldErrors[]` keyed `commitments[<commitmentId>].<field>` → a per-violation constraint (`planned_missing_outcome`/`unplanned_missing_outcome`/`unplanned_missing_supporting_outcome`); new `UnplannedMissingLinkAtCloseException`; code already in §5/B.21). **`SECOND_OPEN_DISPUTE` realized at 5.3** (E17 — second unresolved dispute on a commitment → `409`; in `ErrorCodes`, was already in B.21; `SecondOpenDisputeException`). **`IC_CANNOT_OPEN_DISPUTE`** (5.3) is an authorizer-local constant in `DomainAuthorizationService` (like `IC_CANNOT_RESOLVE_DISPUTE`/`PLAN_OWNER_REQUIRED`), NOT in `ErrorCodes` — the open-side parallel to `IC_CANNOT_RESOLVE_DISPUTE` (the IC-owner attempts to open a dispute on their own commitment → `403`; **NEW code added to B.21 at 5.3**). **`MANAGER_CANNOT_RESPOND_DISPUTE`** (5.4) is the respond-side authorizer-local parallel (a direct manager attempts the E18 IC-respond → `403`; the inverse of `IC_CANNOT_RESOLVE_DISPUTE`; **NEW code added to B.21 at 5.4**). (origin: 3.4a; extended 3.4b, 3.5, 4.5, 5.3, 5.4) |
| `SyncJobPointer` (SNS/SQS lifecycle pointer payload, `shared/.../sns/payload/`) | Appendix F.2 / §10 | The exact 4-field wire JSON `{syncRecordId, eventKind, env, traceId}` — **pointer-only, no calendar bodies/secrets/PII** (rule #7, REQ-I-014); in `:shared` (api publishes via the single-path `SnsLifecyclePublisher`; the worker reloads all data by `syncRecordId`). Mirrors F.2 verbatim; a field change pairs an F.2 edit. (origin: 3.5) |
| `AlignmentDisputeDto` (E17/E18/E19 response, `api/dispute/dto/`) | Appendix B.8 / §3 / §5 | The dispute boundary DTO — a `record` mirroring B.8 verbatim (`id`, `commitmentId`, `managerEmployeeId` [the opener = direct manager, exposed per B.8], `status` `DisputeStatus`, `flagType` `FlagType`, `managerNote`, `icResponse?`, `resolvedAt?`, `allowedActions[]`, `version`), never an entity (audit-quartet leak-tested). `allowedActions` empty at 5.3 (RESPOND/RESOLVE emitted by 5.4/5.5 — "no affordance without enforcement"). B.8 pre-exists — no Appendix edit; a field change pairs a B.8 edit. (origin: 5.3) |
| `OpenDisputeRequest` (E17 request DTO, `api/dispute/dto/`) | Appendix B.2 (E17 request) / Appendix E Part 1 | The E17 `POST /api/commitments/{id}/disputes` request — a validated `record`: `@NotNull flagType` (`FlagType {NEEDS_REVISION,MISALIGNED}`, unknown → `400` via `HttpMessageNotReadable`) + `@NotBlank @CodePointSize(max=4000) managerNote` (normalized once via `TextNormalizer.normalizeMultiLine`; blank → `400 VALIDATION_ERROR`). `managerNote` stored RAW (React-escapes, §16), never logged/audited (§15). B.2/Appendix E pre-exist — no Appendix edit. (origin: 5.3) |
| `RespondDisputeRequest` (E18 request DTO, `api/dispute/dto/`) | Appendix B.2 (E18 request) / Appendix E Part 1 | The E18 `POST /api/disputes/{id}/respond` request — a validated `record`: `icResponse?` (`@CodePointSize(max=4000)`, normalized via `TextNormalizer.normalizeMultiLine`, blank→null) + `supportingOutcomeId?` (the re-link). **At-least-one-required, service-checked** (both absent → `400 VALIDATION_ERROR`); neither field has a set-to-null semantic (so a plain `record`, not a PATCH-presence POJO). `icResponse` stored RAW, never logged/audited (§15); the SO id validated via `RcdoReadService` (unknown → `400`). The SO revision is the **rule-#2 gated exception** (§3 / LESSONS §34) — applied only in `DisputeService.respond`, never via E6. B.2/Appendix E pre-exist — no Appendix edit. (origin: 5.4) |
| Manager command-center read DTOs (`ManagerCommandCenterRowDto`/`PageEnvelope`/`ReviewStateFilter`/`CommandCenterFilters`, `api/manager/`) | Appendix B.11 / B.20 / F.5 / §9 | The E13 read DTOs (6.5a) — **`ManagerCommandCenterRowDto`** a `record` mirroring **B.11** verbatim (17 fields incl. the 3 nullables `weeklyPlanId?`/`reviewStatus?`/`reviewDueAt?` + `isReviewOverdue`; never the entity — leak-tested); **`PageEnvelope<T>`** the **B.20** paginated envelope `{content, page{number,size,totalElements,totalPages}, sort[{property,direction}]}` — a **custom record** over `Page<T>` (stock Spring serialization + `VIA_DTO` omit `sort`; LESSONS §39; `List.copyOf` compact ctor, §22; reused by E15/comments); **`ReviewStateFilter`** the E13 review-state filter vocab (`NOT_REVIEWED|REVIEWED_WITH_DISPUTES|REVIEWED|OVERDUE`, OVERDUE→`is_review_overdue=true`) — **computed filter vocab, NOT an `enums/` member** (like `AllowedAction`/`ErrorCodes`); **`CommandCenterFilters`** the bound filter carrier (**4→8 fields at 6.5a-2** — +`definingObjectiveId`/`priority`/`workType`/`alignmentStatus`; an internal carrier, not a boundary DTO, so no Appendix edit). The dynamic read is an **api-layer `@Repository` Criteria query** (`ManagerCommandCenterQuery`) cross-joining `employee` for the display-name + joined-column sort (flat-UUID FK, no association) — N+1-free (page+count); the `shared` projection repo stays a plain upsert repo. IDOR-safe by query-scoping (`manager_employee_id = principal.employeeId()`, never a request value) + coarse `authorizeTeamHeatmapAccess` (security-reviewer CLEAN PASS). The web-orch verified these mirror `dtos.ts` field-for-field (zero drift). B.11/B.20/F.5 pre-exist — no Appendix edit; a field change pairs a B.11/B.20 edit + a coordinated `dtos.ts` mirror. The §9 command-center `summary` field is DEFERRED (known backend follow-up — a server-computed team roll-up accurate across pagination; the frontend computes a client-side demo strip meanwhile per `docs/planning/024`). **6.5a-2 landed the 4 cross-table EXISTS filters** (`definingObjectiveId` via the `manager_heatmap_cell` full grain — manager/report/week/DO; `priority`/`workType`/`alignmentStatus` via `weekly_commitment` correlated on the outer `s.weeklyPlanId`, over the §4 indexes) as **append-only narrowing predicates** in a dedicated `crossTablePredicates` helper reused by the page+count queries — the IDOR scope is **unchanged** (filters only NARROW; the `manager_employee_id` predicate stays the first unconditional WHERE; ad-hoc security-reviewer **CLEAN PASS**), N+1-free preserved (still a fixed 2 statements with all 7 filters set). The **§9/REQ-F-023 command-center filter set is now COMPLETE (all 7)**. (origin: 6.5a; cross-table filters 6.5a-2) |
| Manager heatmap read DTOs (`HeatmapResponseDto`/`HeatmapCellDto`/`HeatmapDrilldownDto`/`DrilldownOutcomeGroup`, `api/manager/`) | Appendix B.12 / B.20 / F.5 / §9 / §6 | The E14/E15 read DTOs (6.5b) — all **records** mirroring **B.12** verbatim, never entities (leak-tested). **`HeatmapResponseDto`** (E14) `{weekStart, cells: HeatmapCellDto[]}` — **NOT paginated** (bounded by reports×DOs). **`HeatmapCellDto`** (16 fields) mirrors `manager_heatmap_cell` — note the deliberate **field-name asymmetry**: the **response** carries `weekStart`, the **cell** carries `weekStartDate` (B.12:637 vs :647; **do NOT harmonize** — the `dtos.ts` mirror matches the split). **`HeatmapDrilldownDto`** (E15, own-cell) `{cellId, employeeId, definingObjectiveId, supportingOutcomes: DrilldownOutcomeGroup[]}`; **`DrilldownOutcomeGroup`** `{supportingOutcomeId, supportingOutcomeTitle, commitments: PageEnvelope<WeeklyCommitmentDto>}` (reuses the 6.5a **B.20** envelope). **E14** = an api-layer `@Repository` Criteria query (`ManagerHeatmapQuery`) cross-joining `employee`+`defining_objective` for the names/titles — N+1-free (one statement); coarse `authorizeTeamHeatmapAccess` + IDOR-scoped (`manager_employee_id = principal.employeeId()`, never a request value); `supportingOutcomeId` resolves to its **parent DO** + narrows (count-honest; unknown/disagreeing → empty). **E15** = `ManagerDrilldownService` — **own-cell** `authorizeHeatmapCellAccess` **chokepoint-first** → IDOR `404`+audit (missing → `404` no-audit, §25); the report's plan resolved from the **authorized cell's** `(employeeId, weekStartDate)` via `WeeklyPlanRepository.findByEmployeeIdAndWeekStartDate` (never the request; decoupled from the summary projection); per-SO commitments paginated (B.20, **`priority ASC, createdAt ASC, id ASC`** — the `id` a deterministic tiebreaker while `createdAt` is unpopulated); commitments use the **context-free `CommitmentMapper.toDto(c)`** → empty `allowedActions` (dispute affordances E3/E4-only, 5.5b/§35). Empty SO groups omitted (revisitable per REQ-F-022). **Ad-hoc security-reviewer CLEAN PASS** (E14 scoping + E15 own-cell IDOR, both with test-teeth). The §9 manager read surface (E13/E14/E15) is now **COMPLETE**. New finders: `SupportingOutcomeRepository.findByDefiningObjectiveIdOrderByIdAsc`, `WeeklyCommitmentRepository.findByWeeklyPlanIdAndSupportingOutcomeId(…, Pageable)`. B.12/B.20/F.5 pre-exist — no Appendix edit; a field change pairs a B.12 edit + a coordinated `dtos.ts` mirror. **6.6 completed the §6/§17 endpoint-level IDOR coverage** for the manager reads — each denial positively asserts its `AUTHORIZATION_DENIED` audit row (IC→E13/E14 `403`/`Heatmap`; IC or not-own-cell→E15 `404`/`HeatmapCell`) + actor, plus the IC-team-heatmap denial (REQ-F-030); the service-level `AuthorizationIdorMatrixTest` retains the comprehensive rule-#7 SENTINEL-no-leak sweep (these endpoint surfaces have no text columns to leak). (origin: 6.5b; §6/§17 endpoint IDOR coverage 6.6) |

<!-- Starts empty (or with the first model if one exists). Populated as contract models land. -->

## Module organization

<!-- ▼ EXAMPLE BLOCK [id=module-layout]: module layout + layer dependency rule. Replace with the project's real directory tree and import-direction DAG. ▼ -->

```
apps/wc-api/
  settings.gradle               # include 'shared','api','worker'
  shared/  src/main/java/com/st6/wc/   # JPA entities, enums, DTOs, repos (depended on by api + worker)
  api/     src/main/java/com/st6/wc/   # controllers, services, config (security/cors/jwt/flyway), jobs (generation, migration), auth, sns, web
  worker/  src/main/java/com/st6/wc/worker/   # SQS listener, graph adapters, sync-record service
```

Dependency direction (top depends on bottom, never reverse):

```
api  →  shared
worker → shared          (no api ↔ worker edge)
controllers → services → repositories → entities   (controllers never call repositories directly)
```

Cross-cutting layers can be imported from anywhere. Enforce the rule mechanically with a test where possible — the test *is* the spec for the rule.

<!-- ▲ END EXAMPLE BLOCK [id=module-layout] ▲ -->

## Subagents

See `.claude/agents/README.md` for the canonical inventory + integration points.

<!-- ▼ EXAMPLE BLOCK [id=area-subagent-candidates]: area-specific subagent candidates — list candidates that would earn their keep specifically in this area (e.g. an ABI/types syncer for a frontend area, a Pyth/feed verifier for a contracts area). Build only on real friction. ▼ -->

Candidates (build only on real friction): an **IDOR/authorization property-test writer** (generates the per-denial-case matrix); an **invariant test writer** for lifecycle/baseline-immutability; a **DTO↔entity mapper checker**.

<!-- ▲ END EXAMPLE BLOCK [id=area-subagent-candidates] ▲ -->

## Lessons logged from prior sessions

The full prose for each lesson lives in `apps/wc-api/LESSONS.md`. This index is the compact orientation surface.

**Lesson numbers are stable IDs** — once assigned, they don't change. New lessons get the next sequential number. `/session-end` proposes additions when it detects them; the user approves before the entry is written and a row is added here.

Lessons start at §1.

| # | Date | Topic | Rule (one-liner) |
|--:|---|---|---|
| 1 | 2026-06-02 | [Gradle gate-wiring recipe](LESSONS.md#1) | Bind Spotless+SpotBugs+JaCoCo (≥80% line+branch) into `check` per module, assert the aggregation, prove the module boundary with a task, resolve the JDK 21 toolchain via foojay+`JAVA_HOME` (never a committed machine path). |
| 2 | 2026-06-02 | [SpotBugs `Confidence` in Groovy](LESSONS.md#2) | For Kotlin enums with per-constant bodies referenced from Groovy (e.g. SpotBugs `Confidence`), use `Type.valueOf('NAME')`, not `Type.NAME`. |
| 3 | 2026-06-02 | [Enum ↔ contract pinning](LESSONS.md#3) | Pin enum-to-contract with a parameterized value-set test over all enums + `valueOf` drift-trap negatives; Phase 1 extends it to the DB `CHECK` list. |
| 4 | 2026-06-02 | [Spring Boot app-skeleton pattern](LESSONS.md#4) | Enable probe groups in base config; cover via `@SpringBootTest` + exclude `*Application` from JaCoCo (keep bundle non-empty with a real `@Configuration`); static/reusable env fail-safes; per-profile property tests via `ApplicationContextRunner` + `ConfigDataApplicationContextInitializer` (`o.s.boot.test.context`). |
| 5 | 2026-06-02 | [Flyway + Testcontainers PG16 harness](LESSONS.md#5) | Pin `testcontainers-bom:1.21.4` over the SB 3.3.5 BOM (Docker Engine 29 compat); migrate via the Flyway API on a shared TC PG16 container; pin enum↔CHECK by parsing `pg_get_constraintdef` vs `enum.values()`; assert violations by SQLSTATE. |
| 6 | 2026-06-02 | [Backend gate = `./gradlew check` from `apps/wc-api/`](LESSONS.md#6) | The §13 gate is `./gradlew check` run from `apps/wc-api/` (composite root exposes only aggregate check/build) — not the generic per-task `/preflight` list, not `build -x test` (jacoco verification needs tests). |
| 7 | 2026-06-02 | [JPA entity mapping conventions](LESSONS.md#7) | Pick the entity base by row shape (`PersistableUuidEntity` mutable+versioned / `AbstractAuditingEntity`+inline `@Id` audited / inline `@Id`+own-timestamp minimal); map every FK as a flat `UUID` (never `@ManyToOne`); entities mirror DDL type/nullability while `varchar` length caps stay in DTO validation. |
| 8 | 2026-06-02 | [Hibernate-6 non-scalar mappings](LESSONS.md#8) | `text[]`→`List<enum>` via `@JdbcTypeCode(SqlTypes.ARRAY)`+`@Enumerated(STRING)`+`columnDefinition="text[]"`; `jsonb`→`String` via `@JdbcTypeCode(SqlTypes.JSON)` (needs `hibernate-core` on the entity module's compile path — use `starter-data-jpa`, not bare `spring-data-jpa`); prove each with a round-trip test, compare jsonb structurally. |
| 9 | 2026-06-02 | [`@DataJpaTest` fidelity harness](LESSONS.md#9) | Prove entity↔DDL fidelity with `@DataJpaTest`+`@DynamicPropertySource` singleton PG16+Flyway+`ddl-auto=validate`; add explicit `@EntityScan`/`@EnableJpaRepositories` for cross-module sliced tests; a JPA starter on a shared lib activates DataSource autoconfig in every downstream module → `spring.autoconfigure.exclude` it on DB-less skeleton boots. |
| 10 | 2026-06-02 | [SpotBugs EI/EI2 on Lombok mutable collections](LESSONS.md#10) | For mutable-collection fields on Lombok entities, hand-write defensive-copy getters/setters — `EI_EXPOSE_REP`/`EI2` are NOT covered by the `@lombok.Generated` skip; field-access JPA makes the copies harmless to persistence. |
| 11 | 2026-06-02 | [Spring Data repo-layer finders + `@DataJpaTest` proofs](LESSONS.md#11) | Back partial-unique lookups with derived-name finders returning `Optional` (test the empty branch); prove repo-layer invariants under `@DataJpaTest` with `saveAndFlush` (coexistence-first/violation-last), assert Spring `ObjectOptimisticLockingFailureException` for `@Version` conflicts, and fire a set-valued partial unique across its status set (OPEN+IC_RESPONDED), not a same-value duplicate. |
| 12 | 2026-06-02 | [SS6 OAuth2 resource-server JWT decoder](LESSONS.md#12) | `withIssuerLocation`(EAGER OIDC discovery)+`jwsAlgorithm(RS256)`+`DelegatingOAuth2TokenValidator`(default-with-issuer + custom `.contains()`-audience validator), fail-fast on blank config; test via `withPublicKey` decoder wired to the SAME production validator + Nimbus tokens + MockWebServer discovery stub; a Security starter needs BOTH `autoconfigure.exclude` AND a separate gate for component-scanned `@Configuration`; fail-secure `@ConditionalOnProperty(demo-auth.enabled, havingValue=false, matchIfMissing=true)`. |
| 13 | 2026-06-02 | [Config-driven claim mapper + all-profile-binding gotcha](LESSONS.md#13) | Bind claim *names* via `@ConfigurationProperties` (nothing hardcoded) + `sub` fallback + validate-when-present/tolerate-absent-or-blank role parsing (reject non-blank-invalid as a generic `OAuth2AuthenticationException`→401, null on absent/blank — no silent default, no value leak); an always-present `@Component` injecting a `@ConfigurationProperties` bean forces all-profile binding, so every `${...}` default in base yaml needs a resolvable fallback (`${ROOT_DOMAIN:localhost}`) or skeleton boots break. |
| 14 | 2026-06-02 | [Backdoor-control filter (rule #5) + AuditService + DB-backed boots](LESSONS.md#14) | An env-gated backdoor filter runs in ALL modes to *reject* the disabled header (never gated out), rejects the untrusted value WITHOUT a DB query (`verifyNoInteractions`); disabled+header → 403+one-safe-audit, enabled+unknown → convergent IDOR-safe 401 (no audit); register it as a plain-class `@Bean` not `@Component` (double-registration); central `AuditService` is safe-metadata-only (SENTINEL-pinned, never echo untrusted input; denial audits need `REQUIRES_NEW`); once an always-scanned DB-dependent `@Service` lands, migrate the DB-less boots to a shared Testcontainers PG (Option A), don't accrete mocks. |
| 15 | 2026-06-02 | [Relationship-driven principal resolution](LESSONS.md#15) | Resolve identity→`UserPrincipal` with the authoritative `Employee.role` (never the nullable JWT role hint) + relationship-driven `isManager` (`existsByManagerEmployeeIdAndActiveTrue`, never the role claim — MANAGER-role-but-no-active-report ⇒ `isManager=false`); identity→no-`Employee` ⇒ IDOR-safe `Optional.empty()`(→401 at the 2.6 chain, no existence leak / no further DB probe via `verifyNoInteractions`); keep resolution separate from authorization (2.5); `implements` Spring `AuthenticatedPrincipal` so `authentication.getName()`=employeeId; `SystemPrincipal` = distinct no-HTTP boundary singleton. |
| 16 | 2026-06-02 | [Framework-interface name shadow fails SpotBugs](LESSONS.md#16) | A domain type implementing a framework interface must not shadow its simple name — `NM_SAME_SIMPLE_NAME_AS_INTERFACE` (SpotBugs MAX/MEDIUM) fails `check` *even via FQN-`implements`* (the shadow is the finding, not the import); rename distinctly (`UserPrincipal`, not `AuthenticatedPrincipal`), never suppress on production code. |
| 17 | 2026-06-02 | [Central domain-authorization service (rule #3, IDOR-safe)](LESSONS.md#17) | One central `@Service` authorizes every resource access (controllers coarse-only); cross-owner/cross-team/missing → IDOR-safe `404`, capability/role → `403`+named code; manager scope = active `manager_relationship` (owner short-circuits as IC); each genuine denial writes one safe-metadata `audit_event` in `REQUIRES_NEW` (survives rollback), genuinely-missing → 404 no-audit; resolve owners via flat-FK `findById` chains; gate the SYSTEM exemption behind a sealed-marker `instanceof` a request-built principal can't satisfy. |
| 18 | 2026-06-02 | [Build-and-throw deny helpers + escaped-JSON audit metadata](LESSONS.md#18) | Have deny/error helpers **build-and-return** the exception (`throw` at the call site) to dodge the JaCoCo always-throwing-helper coverage artifact; build any audit/log JSON via an **escaped JSON node** (Jackson `ObjectNode`), never string-concat, so rule-#7 safety is structural not caller-dependent (pairs w/ §14). |
| 19 | 2026-06-03 | [SS6 per-mode SecurityFilterChain wiring](LESSONS.md#19) | One `@ConditionalOnProperty(demo-auth.enabled)` chain per mode (mutually exclusive → bearer-XOR-demo structural; DemoAuthFilter = real-mode backdoor rejector); resolve every identity to a `UserPrincipal` both modes (JWT-converter + demo-filter-via-`PrincipalResolver`, empty→401); render RFC-7807 at **3 points** (entry-point 401 + access-denied 403 + `@RestControllerAdvice` 404/403+code/500 — chain-filter exceptions never hit the advice); prove custom decoder wins over autoconfig; **fail-fast on a non-boolean security gate** (malformed value silently falls to Boot's default chain). |
| 20 | 2026-06-03 | [Three SpotBugs/Jackson gotchas on the RFC-7807 path](LESSONS.md#20) | Body builder = pure static util (no stored mutable `ObjectMapper` → EI2); validating `@Configuration` needs `proxyBeanMethods=false`+`final` (→ CT_CONSTRUCTOR_THROW); flatten `ProblemDetail`'s custom props yourself on any **filter-level** (non-MVC) write — the flattening mixin isn't applied off the MVC path (else `safeMessage`/`code`/`traceId` nest under `properties`). |
| 21 | 2026-06-03 | [First controller + DTO (record-not-entity) + CORS via `http.cors()`](LESSONS.md#21) | First endpoint = thin `@RestController`→`@Service`→**DTO `record`** (entity never crosses the boundary — assert via a `.doesNotExist()` leak test); `/me`-style self endpoints authenticated-only (no authorizer call), all other resource endpoints call the authorizer first; wire CORS as a `CorsConfigurationSource` via `http.cors()` into every mode chain — exact-origin + `allowCredentials=false` + **mode-independent** (read only `app.cors.allowed-origins` so demo can't widen). |
| 22 | 2026-06-03 | [Nested read-DTO pattern (RCDO tree)](LESSONS.md#22) | Nested reads = N flat id-ordered finders + an order-preserving in-memory parent-id mapper (no fetch-joins), `active` mapped verbatim, object-wrapper-not-bare-array; org-wide reference reads authenticated-only (no authorizer call); logical wire order from id-asc over sequential seed UUIDs (no schema column), pinned by a title-order assertion; prove read-only domains carry no mutation verb via a `RequestMappingHandlerMapping` reflection test; an internal lookup seam may return the entity (not a boundary leak); guard record-DTO collections with `List.copyOf()`, qualify `requestMappingHandlerMapping`, read `@Nullable` getters once. |
| 23 | 2026-06-03 | [One-shot `--app.job` runner pattern (SYSTEM batch job)](LESSONS.md#23) | One-shot jobs = a `@Component` logic class + a thin `@ConditionalOnProperty("app.job")` `ApplicationRunner` that delegates (split for testability — `ApplicationContextRunner` proves gating without auto-firing the runner); terminate via `--spring.main.web-application-type=none` launch arg (never a stored context + `SpringApplication.exit` → `EI_EXPOSE_REP2`); audit one SYSTEM null-actor row per run (reuse `record(...)`, safe metadata, even on 0 mutations); idempotency = existence pre-filter + unique backstop (no exception control-flow); assert `jsonb` metadata structurally (`readTree`); reuse the org-tz fail-safe + inject `Clock`. |
| 24 | 2026-06-03 | [Plan-DTO-with-nested-commitments + server-authoritative `allowedActions[]`](LESSONS.md#24) | Nested plan/commitment DTOs = a mapper assembling children + resolving the RC→DO→SO breadcrumb via the RCDO service (not duplicated); `allowedActions[]` server-authoritative (§15), computed from the SAME predicate the lifecycle service enforces, under "no affordance without enforcement" (emit only enforced actions, e.g. `LOCK`); `AllowedAction` = neutral-pkg computed vocab, never `enums/`; self-scoped reads skip the authorizer (per-resource `{id}` reads don't); a named-coded 404 is fine self-scoped but the IDOR 404 stays codeless; a DTO may carry a documented transitional subset for a pending-approved contract change. |
| 25 | 2026-06-03 | [Per-resource read via the authorizer chokepoint (rule #3 consumer)](LESSONS.md#25) | Per-resource `{id}` reads call `authorize…Access(principal, id)` as the **first** service statement (chokepoint, pinned by `verify(repo, never()).findById` on a denied authorize); missing + unauthorized both return the **identical codeless** IDOR 404 (audit only on genuine denial, never on missing); a per-resource 404 is codeless while a self-scoped 404 may be named (`PLAN_NOT_FOUND`); the existence-hiding body-equality test strips `traceId` + `instance` (server-determined fields only); the authorizer stays **void** (the double-read is the right tradeoff); authorized reads write no audit. **Security-reviewer PASS.** |
| 26 | 2026-06-03 | [Server-side input validation pattern (Appendix E Part 1)](LESSONS.md#26) | Normalize ONCE in the request record's compact constructor (`TextNormalizer`: strip+collapse+NFC, blank→null) + a custom `@CodePointSize` (code points, not UTF-16 `@Size`) + `@NoControlChars` (title rejects / description strips C0 except `\n\t`) → uniform `400 VALIDATION_ERROR`/`fieldErrors`; route unknown-enum/malformed-JSON via `HttpMessageNotReadableException` extracting ONLY the safe field name (never the value, §15); no `ConstraintViolationException` handler until a `@Validated` method-param trigger exists; store user text RAW (React escapes, §16) + never log sensitive text; force server-owned fields server-side; authorize the parent plan before a child create (non-DRAFT → `409 ILLEGAL_STATE_TRANSITION`). **Security-reviewer PASS.** |
| 27 | 2026-06-03 | [PATCH 3-way presence + per-resource mutation authz + SpotBugs mutator-name trap](LESSONS.md#27) | PATCH partial-update DTO = a **presence-flag POJO** (NOT an `Optional<>` record — Jackson + Hibernate `OptionalValueExtractor` collapse absent/null, breaking absent-vs-clear + spuriously rejecting absent `@NotBlank`); the provided-flag is load-bearing for state gates. Per-resource mutations use a dedicated `authorize…Mutation` (access-chokepoint + owner-check → `403 …_OWNER_REQUIRED`+audit, mirroring `authorizeDisputeResolution`, §17 central). Two distinct post-lock gates: `LOCKED_BASELINE_EDIT` (baseline-first) vs `ILLEGAL_STATE_TRANSITION`+`constraint` for `alignmentStatus`. Dodge the SpotBugs-4.8 `MutableClasses` mutator-name `EI2` heuristic by renaming (`delete`→`discard`), never suppressing; re-derive editable-field allow-lists per plan state, don't widen one gate. **Security-reviewer PASS.** |
| 28 | 2026-06-03 | [Plan-LOCK transaction pattern](LESSONS.md#28) | Lock = one `@Version` txn; precondition reuses `canLock` as a single accept-gate + fail-closed diagnosis for the granular 409s (§15 single-source); calendar publish is `afterCommit`→`REQUIRES_NEW` swallow-never-rethrow leaving `PENDING_PUBLISH` (rule #4 non-blocking); a concurrent conflict→`409` (decomposed proof, not a threaded race); §9 projections recompute-from-source as inserts at lock, skipped entirely when no active manager. **Security-reviewer PASS.** |
| 29 | 2026-06-03 | [`@SpringBootTest` context-cache Hikari-pool cap](LESSONS.md#29) | Cap `spring.datasource.hikari.maximum-pool-size` (e.g. 2) in the shared `@SpringBootTest` base — each distinct (esp. `@MockBean`) context holds its own live Hikari pool in Spring's context cache; the SUM exhausts PG `max_connections` as the suite grows, surfacing as non-local `too many clients` context-load failures in unrelated tests. |
| 30 | 2026-06-03 | [Carry-forward (E12) transaction pattern](LESSONS.md#30) | Carry-forward = a commitment-keyed, owner-only (`authorizeCommitmentMutation`) cross-week write: overwrite the source outcome→`CARRIED_FORWARD` (sole writer), create a self-linked successor in the next Mon–Sun DRAFT plan (create-if-absent shell, §23 pattern), idempotent per source via existence-prefilter on the self-link + source-`@Version` serialization (no migration); `carry_forward_count` is the carried-IN/successor-link reading so the source-week count is unchanged + needs no `ProjectionService` change (it materializes at next-week lock); successor copies content + a planned `workType` (UNPLANNED→`STRATEGIC`); locked baseline byte-identical (REQ-E-005); no Outlook sync record (§10). |
| 31 | 2026-06-03 | [Affordance-subset pattern](LESSONS.md#31) | A per-commitment `allowedActions` affordance may deliberately NARROW enforcement for UX (a subset, not literal-identity like §24/`canLock`): pin the **subset direction** (`affordance-eligible ⟹ enforcement-accepts`, an eligibility⟹preconditions sweep over state×outcome×owner×kind), and keep the service's own broader gate (never reuse the narrowed predicate — it would break E12 idempotent re-carry / REQ-D-006); realize via a context-aware `toDto(commitment, plan, actor)` overload (the no-arg `toDto` stays empty) threaded through `PlanMapper` on the read path (E3/E4). |
| 32 | 2026-06-03 | [Authorship/mutation uses `authorize…Mutation`, never `authorize…Access`](LESSONS.md#32) | Write endpoints (authorship + mutation) authorize owner-only via `authorize…Mutation` (→ `403 …_OWNER_REQUIRED`+audit for a manager-of-owner; IDOR `404` for cross/missing); `authorize…Access` admits managers (correct for READS only — a §6 hole for writes). Real shipped bug (3.4a E5 create → a manager could author on a report's plan; fixed brief 055). Every write endpoint needs a manager-of-owner-403 test (seed an active relationship → 403-not-404 — its absence is how E5 shipped). Forbidden-pattern #7. |
| 33 | 2026-06-03 | [Manager-capability mutation authz + 404-vs-403 namespace-legitimacy](LESSONS.md#33) | Manager-side mutations (`authorizeReviewMutation`, coming dispute-resolve/managerAlignmentNote) are active-direct-manager-only (the IC-owner is denied — inverse of `authorize…Mutation`). A genuine capability denial is `403`+code when the principal **legitimately uses the resource's API namespace** (manager reads via E4 → `403 …_OWNER_REQUIRED`; IC uses `/api/disputes` → `403 IC_CANNOT_RESOLVE`), else `404` IDOR (IC has no `/api/manager/*` endpoint → `404`); **both AUDIT** (audited `deny404(…,reason)` paralleling `deny403`; only truly-missing skips, §25). `MANAGER_ROLE_REQUIRED` is the COARSE not-a-manager-at-all gate only. Drained the `MANAGER_ROLE_REQUIRED` carry-forward. Extends §27/§32. |
| 34 | 2026-06-03 | [Gated exception to a safety invariant + IC-owner-capability authz (dispute respond)](LESSONS.md#34) | A new path may carve a SAFE exception to a safety invariant (rule #2 locked-baseline immutability) **iff it is escape-proof**: exactly ONE writable field (the request DTO carries no other baseline field), **double-gated** (authz chokepoint-first + a state guard — dispute `OPEN`), the target entity loaded by an OWNING id from the trusted entity (`dispute.getCommitmentId()`), **never from the request** (no cross-resource vector), and audited (safe ids + the new value, no free text). Authz is the §33 namespace-legitimacy tree in its **IC-owner-capability** direction (the inverse of manager-capability): respond is owning-IC-only, so the direct manager (who legitimately uses `/api/disputes` via open/resolve) → `403 MANAGER_CANNOT_RESPOND_DISPUTE`, unrelated/non-owning → `404`, both audited. Ad-hoc security-reviewer CLEAN PASS. Extends §32/§33. |
| 35 | 2026-06-03 | [Per-viewer affordances on a shared read — compute relationship-context once at the mapper root](LESSONS.md#35) | A read DTO whose `allowedActions[]` depend on the VIEWER (owner-IC vs active-direct-manager) resolves `viewerIsDirectManager` ONCE at the `PlanMapper` root (single `findByDirectReportEmployeeIdAndActiveTrue` lookup, `verify(times(1))`) + threads booleans down — keep the resolver **repo-free** (never inject the repo per-child → N-query; the caller passes the booleans). Reuse an already-resolved child (the 5.3b nested dispute → `hasUnresolvedDispute`) for derived conditions (no N+1). Affordance predicates **MIRROR — not share —** the void-throw authorizers (parallel boolean form, subset-pinned via an eligibility⟹preconditions sweep, §24/§31). **TRACE every caller** of the shared mapper to confirm no affordance leaks where the viewer isn't entitled (lifecycle lock/start/close responses are owner-only; E15 drilldown doesn't use `PlanMapper`). Extends §24/§31/4.4b; Phase-6 MARK_REVIEWED reuses the threading. |
| 36 | 2026-06-03 | [Field-level authz on a shared mutation endpoint](LESSONS.md#36) | When one field of a shared PATCH is owned by a DIFFERENT actor than the rest (E6's `managerAlignmentNote` is manager-of-owner-only; every other field is IC-owner-only), branch the authz **by field**: if the manager-field is provided, require it be the ONLY field (else `400 VALIDATION_ERROR` — single-actor-per-patch; placed as **request-shape validation BEFORE any resource load**, so it's IDOR-safe like `@Valid`), then route to the manager-capability chokepoint (`authorizeManagerAlignmentNote`, §33 commitment-field manager-capability: IC-owner → `403 IC_CANNOT_WRITE_MANAGER_NOTE`, unrelated → `404`); else the existing IC-owner path stays **byte-for-byte unchanged** (no regression — the manager-field's sole prod setter is the manager branch, the IC path structurally can't set it). Pin: `doesNotTouchBaseline` + the no-regression IC-path test + the mixing-`400` + audit-no-note-body. Distinct from §27 (gates the SAME actor by state); here the gate is by actor/capability. Extends §27/§33. |
| 37 | 2026-06-03 | [Sibling-table projection counts: load-once-derive-in-memory + pin the source against the binding seed](LESSONS.md#37) | A §9 projection count spanning a sibling table (the dispute-union `misaligned_count` + `unresolved_dispute_count`, both reading `alignment_dispute`) loads that table **ONCE** over the grain's id set (one `findByCommitmentIdInAndStatusIn` finder serving both the union set + the count; **empty-`IN`-guarded**; no N+1), loaded **internally** so the 5 `recompute(...)` caller signatures stay unchanged (ctor +1 arg, only the unit test constructs it), **deduped per grain row** (the union feeds count AND badge so they can't disagree). **The source-predicate trap:** correct a count's SOURCE against the binding seed (`rebuild==seed`, §9/§17), NOT a similarly-named field — `blocked_count` shipped as `work_type=BLOCKER` (a planning category) but R5 Grace's `BLOCKED` badge comes from `reconciliation_outcome=BLOCKED` (the risk-outcome, sibling of `carry_forward_count`); the seed decides an ambiguous source. A wrong source is a latent bug even when green. (origin: 6.2) |
| 38 | 2026-06-03 | [A new synchronous FK-writing side-effect ripples into every driver test's teardown](LESSONS.md#38) | Wiring a new synchronous side-effect that persists **FK-referencing rows** (6.3b: dispute/review ops now write `manager_plan_summary`/`heatmap_cell` → FK employee/plan/DO) into an already-tested service method makes **every driver test leak** those child rows — its `@AfterEach` `plans/employees.deleteAll()` FK-fails, aborts cleanup, and the leak cascades into UNRELATED classes as opaque `ConstraintViolationException`s (6.3b: 64 cross-class failures in `MeEndpointTest`/`ReviewStatusDeriverTest`/…). Fix: trace ALL drivers, add the new child-table deletes to their teardown in FK-safe order (children before parents). Distinct from §29 (pool exhaustion); this is teardown-FK-order. Also §37 gained a 6.3b addendum (recompute-then-prune: a sync recompute must DELETE grain-rows the new source no longer produces). (origin: 6.3b) |
| 39 | 2026-06-04 | [Paginated response with a `sort` field needs a custom envelope record (stock Page / VIA_DTO omit it)](LESSONS.md#39) | The B.20 contract pins `{content, page:{number,size,totalElements,totalPages}, sort:[{property,direction}]}`; Spring Boot 3.x deprecated direct `Page` serialization and `VIA_DTO`'s `PagedModel` omits `sort` — so build a **custom `PageEnvelope<T>` record** over `Page<T>` (`List.copyOf` ctor §22; version-agnostic; reused by E13/E15/comments). Request side stays stock `Pageable` + `spring.data.web.pageable` 25/100. **Sort-by-a-joined-non-association-column** (displayName over a flat-UUID FK) ⇒ a **Criteria query** (`JpaSpecificationExecutor` can't sort a non-association join), kept an **api-layer query component** with its tests (a Criteria impl in `shared` exercised only by `api` tests tanks `shared` JaCoCo). (origin: 6.5a) |
| 40 | 2026-06-04 | [From-source rebuild = truncate + recompute-all via the incremental entrypoint; prove with corrupt-then-rebuild](LESSONS.md#40) | A read-model rebuild reuses the SAME per-unit derivation entrypoint the incremental triggers call (`recomputeForPlan`) — truncate + recompute-all ⇒ **`rebuild==incremental` by construction** + drift/orphan correction for free; one `@Transactional` atomic swap; the §23 one-shot runner (`@ConditionalOnProperty(app.job)` + plain logic, `web-application-type=none`, one SYSTEM audit/run). **Test teeth:** `build→rebuild→assert-equal` is near-tautological — use **corrupt-then-rebuild-then-assert-corrected** (mutate count / delete cell / inject orphan → rebuild → back-to-source, orphan pruned). `deleteAllInBatch` needs no defensive flush when nothing pre-loads before the truncate (a `@PersistenceContext` field also breaks the no-JPA gating-test seam). Composes §23+§37. (origin: 6.7) |
| 41 | 2026-06-04 | [Demo seed in a separate `db/demo-seed` Flyway location](LESSONS.md#41) | Demo/fixture seed (V5 personas, V6 fixtures) lives in a dedicated `classpath:db/demo-seed` the deploy migrate Job loads via two-location `locations: classpath:db/migration,classpath:db/demo-seed`, while the shared `SharedPostgres` integration harness loads `db/migration` only → demo rows never pollute its clean-baseline count assertions; read-only REFERENCE seed (V4 RCDO) stays in `db/migration` (tests depend on it). Flyway orders by VERSION across locations (V5/V6 still run after V4). Pin the migrate-profile `locations` as production config (`FlywayProfilePropertyTest`, forbidden-#1). Verify the seed in an ISOLATED per-class Testcontainer (both locations), never the shared harness. Gotcha: a root `test --tests <filter>` fails `:worker:test` "No tests found" → scope per-module. (origin: V5/10.2) |
| 42 | 2026-06-04 | [configtree deploy-secret binding + profile-filename gotcha](LESSONS.md#42) | Deploy secrets bind via `spring.config.import=optional:configtree:/mnt/secrets/` (the SPC mounts each key as a file named by the property key, so ONE import binds db datasource + auth0 + graph; `configtree:` NOT `file:`; `optional:` for local; configtree overrides base `${ENV:}` literals which stay as sane defaults). AND `application-<profile>.yml` only loads under THAT active profile — `application-prod.yml` was DEAD under `SPRING_PROFILES_ACTIVE=aws` (blank auth0.audience + no datasource); the config filename MUST match the active profile. `ddl-auto=validate` gates db at boot. (origin: 090; +092 worker 2nd-instance — pin a rename with a profile-filename MARKER test, not a base-default-inheriting one) |
| 43 | 2026-06-04 | [Real AWS-client gateway behind a shipped no-op seam](LESSONS.md#43) | To make a shipped no-op seam real, swap ONLY the adapter — it **PROPAGATES** (the pre-built single-swallow `REQUIRES_NEW` afterCommit orchestration from 3.5/§28 is reused byte-for-byte; a 2nd gateway-internal catch would hide failures from the `PENDING_PUBLISH` retry — rule #4); **property-select** the real-vs-stub impls mutually-exclusively (`@ConditionalOnProperty("app.sns.topic-arn")` vs `havingValue=false,matchIfMissing=true` — NEVER `@ConditionalOnMissingBean` between two `@Component`s, scan-order-dependent); inject the framework `*Operations` interface, not the `*Template` concrete (DI + EI_EXPOSE_REP2); the payload stays the bare 4-field pointer (rule #7, ArgumentCaptor-pinned); and give any AWS starter a `spring.cloud.aws.region.static:${AWS_REGION:us-east-1}` fallback so a clean CI runner boots (the starter auto-configures its client in EVERY profile, ungated by your bean's conditional). (origin: Wave-2 s7) |
| 44 | 2026-06-04 | [SQS consumer — cause-less sanitized rethrow + idempotent at-least-once + separate-deployable JPA wiring](LESSONS.md#44) | The §43 consumer counterpart. DLQ-on-failure throws a **cause-less, ids-only** `SyncProcessingException` (single `(UUID)` ctor, no `(…,Throwable)` overload → `getCause()==null`) — the raw Graph exception (PII-bearing) NEVER reaches the rethrow, a log (`log.warn("…={}", id)`, never `(…, e)`), or the persisted `failureCode`/`safeMessage` (fixed constants), else the SQS framework's **redrive logging leaks PII** (rule #7 on logs); pin `.hasNoCause()` + no-PII-message, NOT bare `.isInstanceOf(RuntimeException)`. **Idempotent at-least-once:** skip on the strongest already-done signal (`graphEventId != null`), no `@Transactional` across the network call. A JPA-consuming **separate deployable** needs explicit `@EntityScan("com.st6.wc")` + `@EnableJpaRepositories("com.st6.wc.sync.repo")` (its scan root excludes the shared entity/repo — the §9 prod analogue) + its own Testcontainers PG harness for the datasource boot test (H2 banned §17) — **reverses the DB-less 092 exclude**. Composes §43 + §9. (origin: Wave-2 s8) |
| 45 | 2026-06-04 | [Real external-SDK (MS Graph) adapter behind the §43 seam + §10 startup fail-safe + the EI_EXPOSE_REP2-on-an-SDK-client exclude](LESSONS.md#45) | A real external-SDK adapter goes behind a **self-authored 1-method gateway interface** (the lone SDK-importing class is mock-isolated; a non-PII spec record keeps SDK types + PII out of the pure-unit-testable adapter; mock the gateway — NO live call; event content non-PII, the owner email is the call ADDRESS not event data). The **§10/REQ-I-004 fail-safe degrades AT STARTUP** — `@ConditionalOnProperty(app.graph.mode=real)` returns a `DegradedGraphCalendarPort` **before** building the credential/client when any `GRAPH_*` is blank (no live client / crash / network; `ctx.hasNotFailed()` pinned; creds logged as presence-booleans only); the degrade OUTCOME = **safe-FAILED** per the binding **Appendix D.6** ("records a safe failure"), NOT demo-success (which would mask a misconfig as fake-SYNCED) — resolve the outcome against the architecture, not ambiguous brief wording. An `EI_EXPOSE_REP2` on a **no-interface SDK client** (`GraphServiceClient`) warrants a **narrow AND-scoped** `config/spotbugs/exclude.xml` entry (exact class + exact pattern; confirm it masks only the SDK transport, no PII) — the §43 interface-injection dodge can't apply. Composes §43 + §44 + §16. (origin: Wave-2 s9) |

<!-- Starts empty. Each row links to its `LESSONS.md` anchor. -->

<!-- Slash commands: see root CLAUDE.md "Slash commands available." Implementer pair: /session-start + /session-end. -->
