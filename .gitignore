claude-code-tdd-agent-crew-scaffold
scaffolding/
.DS_Store

# JS / Yarn (Berry, node-modules linker via Corepack — no committed release binary) / Nx
node_modules/
.yarn/
.pnp.*
.nx/
yarn-error.log
# NOTE: yarn.lock is intentionally NOT ignored — it is the reproducible-install contract.

# Gradle (build outputs + local caches). The committed wrapper (gradlew, gradle/wrapper/*) is NOT ignored.
.gradle/
build/

# Code-intelligence index (CodeGraph MCP) — local SQLite knowledge-graph cache, never committed.
.codegraph/
.gstack/
